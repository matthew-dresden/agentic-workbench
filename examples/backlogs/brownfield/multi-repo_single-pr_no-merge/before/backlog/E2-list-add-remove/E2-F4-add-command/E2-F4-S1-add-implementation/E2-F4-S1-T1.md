# E2-F4-S1-T1: `mpm add` core -- triple writing from `<catalog-metadata>`

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e2-f4-s1-t1`

## Description

Introduce the new top-level `mpm add` subcommand. This task lands the
command shell, the core entry resolution path, the triple writer, the
standard-header creation for missing destination files, and the
registration on the top-level argparse parser.

New command module `src/mpm_cli/commands/add.py`:

- Argparse subparser `add` with the following arguments:
  - Positional `<name>[@<spec>]` repeated one-or-more times.
  - `--catalog-source` (imported from `core/cli_args.py`).
  - `--mpm-file <path>` (default `./.mpm`; env var
    `MPM_MPM_FILE`).
  - `--no-color`.
  - `--force` and `--dry-run` are reserved for E2-F4-S1-T2 (this task
    does NOT implement them yet but registers them at argparse level so
    later tasks need not re-thread argparse).
- Entry-point function (e.g., `run_add(args) -> int`) that:
  1. Resolves the catalog source via the shared resolver (per spec
     Section 4 header).
  2. Walks `repo-specs/**/*-marketplace.xml` and builds the entry index
     by calling `_parse_catalog_metadata` on each file.
  3. If soft-spot rule 1 or 3 fires for any entry, hard-errors with the
     spec-canonical "manifest repo `<url>@<ref>` has integrity issues"
     message naming every offending XML path.
  4. For each requested `<name>[@<spec>]`:
     - Locates the matching catalog entry by name. If not found, hard
       error naming the missing entry and suggesting `mpm list` to
       discover legal names.
     - Splits on the LAST `@` (per spec Section 4.0). If `<spec>` is
       absent, defaults to the manifest repo's highest PEP 440-valid git
       tag (queried via `git ls-remote --tags`). E2-F4-S1-T3 adds the
       zero-tags loud-error path; this task assumes the manifest repo has
       at least one PEP 440-valid tag.
     - Resolves the constraint via the existing `resolve_version()` path
       in `src/mpm_cli/version.py` (uses E1-F1-S1-T1's widened
       `_normalize_bare_semver_to_tag`).
     - Derives the source name via `derive_source_name()`
       (`src/mpm_cli/core/metadata.py`).
     - Constructs the three triple lines:
       ```
       MPM_SOURCE_<source_name>_URL=<manifest_repo_url>
       MPM_SOURCE_<source_name>_REVISION=<resolved_spec>
       MPM_SOURCE_<source_name>_PATH=<path_in_repo_to_marketplace_xml>
       ```
       (NO legacy `catalog/<name>/.mpm` template lookup.)
  5. If the destination `.mpm` file does not exist, creates it with the
     standard header lines (today's
     `src/mpm_cli/catalog/mpm/.mpm` template content), namely:
     ```
     GITBASE=<YOUR_GIT_ORG_BASE_URL>
     CLAUDE_MARKETPLACES_DIR=${HOME}/.claude-marketplaces
     MPM_MARKETPLACE_INSTALL=<true|false>
     ```
     The standard-header constants live in `src/mpm_cli/constants.py`
     (extend the existing module).
  6. Appends every triple block to the destination file.
  7. Prints one summary line per written block to stdout (e.g.,
     "Wrote MPM_SOURCE_package_a_URL, _REVISION, _PATH to ./.mpm").

The shell-quoting friendly-error path for unquoted PEP 440 ranges (spec
Section 4.2 "Shell quoting reminder") is detected by argparse seeing an
unexpected empty positional and surfaced with a remediation pointer to
quote the spec.

`src/mpm_cli/cli.py` change:

- Register the new `add` subcommand on the top-level argparse parser.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at
      `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] The branch `backlog/e2-f4-s1-t1` does not already exist

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E2-F4-S1-T2 | `mpm add --dry-run` + `--force` + collision detection | in-queue |
| E2-F4-S1-T3 | `mpm add` zero-PEP-440-tags loud error | in-queue |

### Approach

1. **TDD RED:** Write failing unit tests in `tests/unit/test_add.py`
   covering the triple constructor, the default-spec path against a
   fixture manifest repo with tags, the source-name derivation, the
   standard-header creation when the destination file is absent, and the
   per-block stdout summary. Write failing integration tests in
   `tests/integration/test_add_core.py` invoking
   `mpm add <name> --catalog-source <file>@<ref>` via subprocess and
   asserting on the resulting `.mpm` file content.
2. **TDD GREEN:** Implement `src/mpm_cli/commands/add.py` and register
   the subcommand in `src/mpm_cli/cli.py` until every test passes.
3. **TDD REFACTOR:** Extract the triple-writer into a private helper if
   T2's `--dry-run` will share the rendering path (it will; render once
   to an in-memory buffer, then either write to disk or print to stdout).
4. **Verify:** Run `uv run pytest tests/ -v`,
   `uv run ruff check src/ tests/`, `uv run mypy src/`,
   `uv run bandit -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Spec:** `spec/mpm-list-add-lock-features-spec.md` -- Section 4.2 "Behaviour" steps 1-5; Section 4.2 argument/flag table; Section 2.1 worked-example step 3; Section 1.1 terminology row ".mpm file".

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E2-F1-S1-T1 | Implement `_parse_catalog_metadata()` and `CatalogMetadata` dataclass | in-queue |
| E2-F3-S1-T1 | Implement `derive_source_name()` helper | in-queue |
| E1-F4-S1-T1 | Extract `--catalog-source` definition to shared `core/cli_args.py` | in-queue |
| E2-F2-S1-T1 | (auto) | proposed |
| E2-F2-S1-T4 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 New module `src/mpm_cli/commands/add.py` exists and exposes an argparse subparser builder + an entry-point function (e.g., `run_add(args) -> int`).
- [ ] AC-FUNC-002 The `add` subcommand is registered on the top-level argparse parser in `src/mpm_cli/cli.py`; `mpm add --help` prints the subcommand's help text.
- [ ] AC-FUNC-003 The subparser imports `--catalog-source` from `src/mpm_cli/core/cli_args.py` (ref) and registers `--mpm-file <path>` (default `./.mpm`, env var `MPM_MPM_FILE`) and `--no-color`.
- [ ] AC-FUNC-004 With one requested `<name>` (no spec), an existing manifest repo with PEP 440-valid tags, and a destination `.mpm` file that does not exist, the command creates the file with the standard header (`GITBASE`, `CLAUDE_MARKETPLACES_DIR`, `MPM_MARKETPLACE_INSTALL`) followed by the three `MPM_SOURCE_<source_name>_{URL,REVISION,PATH}` lines and exits 0.
- [ ] AC-FUNC-005 With one requested `<name>@<spec>` and an existing destination file, the command appends only the three triple lines (does NOT duplicate the standard header).
- [ ] AC-FUNC-006 With multiple requested entries, the command processes them in argument order and writes one triple block per entry.
- [ ] AC-FUNC-007 The source name in each `MPM_SOURCE_<source_name>_*` line is derived via `derive_source_name()` (E2-F3-S1-T1); confirmed by passing an entry named `Foo-Bar` and asserting the lines read `MPM_SOURCE_foo_bar_*`.
- [ ] AC-FUNC-008 The triples are constructed directly from the `<catalog-metadata>`; no `catalog/<name>/.mpm` template is consulted. Verified by running the command against a manifest repo with no legacy `catalog/` directory present.
- [ ] AC-FUNC-009 Default-spec path (no `@<spec>`): the highest PEP 440-valid git tag on the manifest repo is selected via `git ls-remote --tags` + `packaging.version.Version` parsing; the resolved value is written to `MPM_SOURCE_<source_name>_REVISION`.
- [ ] AC-FUNC-010 An unknown entry name (not present in the manifest repo) is a hard error naming the entry and pointing the operator at `mpm list` for discovery.
- [ ] AC-FUNC-011 A soft-spot rule 1 or 3 violation in the manifest repo (detected by `_parse_catalog_metadata` raising or by entry-name duplicates) is surfaced as a hard error matching the spec-canonical "manifest repo `<url>@<ref>` has integrity issues" wording.
- [ ] AC-FUNC-012 Each written triple block emits a one-line summary to stdout naming the source name and the destination file.
- [ ] AC-FUNC-013 Standard-header constants live in `src/mpm_cli/constants.py` (extended); the values are the same as today's `src/mpm_cli/catalog/mpm/.mpm` (ref) template content.
- [ ] AC-TEST-001 Unit tests in `tests/unit/test_add.py` cover the triple constructor, the source-name derivation integration, the standard-header creation path, the per-block stdout summary, the unknown-entry hard error, and the soft-spot 1/3 hard error.
- [ ] AC-TEST-002 Integration tests in `tests/integration/test_add_core.py` invoke `mpm add <name>` and `mpm add 'name@==X.Y.Z'` end-to-end against a fixture manifest repo with PEP 440-valid tags; assert on the resulting `.mpm` file content for both the create-with-header and append-to-existing paths.
- [ ] AC-TEST-003 Tests are written BEFORE the implementation lands; cycle recorded in `## TDD Cycle Log`.
- [ ] AC-TEST-004 Coverage: 100% line + branch on `src/mpm_cli/commands/add.py` for the lines authored by this task; constants module baseline not regressed.
- [ ] AC-TEST-005 No test stubs.
- [ ] AC-CYCLE-001 End-to-end cycle: build a fixture manifest repo with three entries each tagged at `1.0.0`, `1.1.0`, `1.2.0`; run `mpm add entry-a` against an empty workspace and verify the `.mpm` file is created with the standard header followed by `MPM_SOURCE_entry_a_{URL,REVISION,PATH}`; the `REVISION` line equals `refs/tags/1.2.0`; the stdout summary names the three keys and the destination path; re-run `mpm add 'entry-b@==1.0.0'` and verify the file is appended to (no duplicate header) and the new REVISION reads `refs/tags/1.0.0`. Evidence captured in the TDD Cycle Log.
- [ ] AC-DOC-001 `mpm add --help` text describes the positional argument shape, the `--mpm-file` flag with its env var, and references the spec Section 4.0 resolver rules for the `@<spec>` grammar.
- [ ] AC-LINT-001 `uv run ruff check src/ tests/` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src/ tests/` exits zero.
- [ ] AC-SEC-001 No secrets or hard-coded configuration values.
- [ ] AC-SEC-002 No bypass annotations introduced.
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/commands/add.py` | New -- argparse subparser + `run_add()` entry point + core triple-writing path + standard-header creation. |
| `src/mpm_cli/cli.py` | Updated -- register the new `add` subcommand on the top-level parser. |
| `src/mpm_cli/constants.py` | Updated -- add standard-header constants (`GITBASE`, `CLAUDE_MARKETPLACES_DIR`, `MPM_MARKETPLACE_INSTALL` default values). |
| `tests/unit/test_add.py` | New -- unit tests for the triple constructor, source-name derivation integration, standard-header creation, unknown-entry error, and soft-spot 1/3 error. |
| `tests/integration/test_add_core.py` | New -- integration tests for the core `mpm add` path. |
| `tests/unit/test_cli.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/cli.py` per docs/source-test-atomicity.md. |
| `tests/unit/test_constants.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/constants.py` per docs/source-test-atomicity.md. |
## Definition of Done

- [ ] All acceptance criteria checked
- [ ] `uv run pytest tests/ -v` passes
- [ ] `uv run ruff check src/ tests/` passes
- [ ] `uv run ruff format --check src/ tests/` passes
- [ ] `uv run mypy src/` passes
- [ ] Only files in the Changes Manifest are staged with `git add`

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E2-F4-S1-T1 manually blocked on E2-F2-S1-T1 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E2-F2-S1-T1

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E2-F4-S1-T1 manually blocked on E2-F2-S1-T4 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E2-F2-S1-T4
