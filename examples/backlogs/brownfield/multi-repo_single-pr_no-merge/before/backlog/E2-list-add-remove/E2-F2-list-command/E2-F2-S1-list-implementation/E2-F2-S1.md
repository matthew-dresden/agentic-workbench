# E2-F2-S1: `mpm list` implementation across all flags

## Status: in-queue

## Description

Single-story decomposition of the `mpm list` implementation. Six tasks
in strict sequence (T1 -> T2 -> ... -> T6) so each flag lands as an
independent commit on top of the same command module
(`src/mpm_cli/commands/list.py`).

T1 introduces the command shell and the default output. Each subsequent
task extends the command with one flag (or flag-cluster). The story closes
when T6 lands and the full integration suite for `mpm list` is green.

Spec source: spec Section 4.1.

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
