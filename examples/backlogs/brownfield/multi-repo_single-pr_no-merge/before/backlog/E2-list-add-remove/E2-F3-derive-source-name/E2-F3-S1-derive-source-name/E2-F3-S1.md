# E2-F3-S1: Implement `derive_source_name()` helper

## Status: in-queue

## Description

Single-task story landing the shared `derive_source_name()` function. The
helper is extended onto `src/mpm_cli/core/metadata.py` (the module
introduced by E2-F1-S1-T1) rather than getting its own module because it
is one small function and lives in the same logical "catalog-metadata
helpers" namespace.

The matching unit-test file `tests/unit/test_derive_source_name.py` is
authored in the same TDD cycle.

Spec source: spec Section 3.5 soft-spot rule 2 and spec Section 1.1
"source name".

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
