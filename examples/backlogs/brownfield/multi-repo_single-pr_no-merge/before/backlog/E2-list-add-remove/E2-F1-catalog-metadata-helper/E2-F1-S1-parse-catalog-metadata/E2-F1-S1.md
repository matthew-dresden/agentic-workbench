# E2-F1-S1: Implement `_parse_catalog_metadata()` and `CatalogMetadata` dataclass

## Status: in-queue

## Description

Single-task story landing the shared `_parse_catalog_metadata()` helper and
its `CatalogMetadata` dataclass in a new module
`src/mpm_cli/core/metadata.py`. The matching unit-test file
`tests/unit/test_metadata.py` is authored in the same TDD cycle.

The story is one commit because the source file and its tests must be atomic
per `workbench/docs/source-test-atomicity.md` (AC-FINAL-014 requires the test
to land alongside the source).

Spec source: spec Section 3.5 (soft-spot rule 1) and spec Section 3 (existing
primitives table row 5).

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
