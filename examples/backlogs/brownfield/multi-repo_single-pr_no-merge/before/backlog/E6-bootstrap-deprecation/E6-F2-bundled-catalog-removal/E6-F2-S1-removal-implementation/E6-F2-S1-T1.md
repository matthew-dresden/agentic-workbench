# E6-F2-S1-T1: Delete `src/mpm_cli/catalog/` from the wheel and add repo / CI guard

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e6-f2-s1-t1`

## Description

Delete the entire bundled catalog directory tree `src/mpm_cli/catalog/`
from the mpm repo (today contains one entry, `mpm`, providing the
self-bootstrap `.mpm` template). After E6-F1-S1-T1 lands, the only
consumer of that tree is the resolver's third-tier fallback inside
`core/catalog.py::resolve_catalog_dir`; this task removes the tree on
disk and adds tests / guards that prevent regression.

Concretely:

- `git rm -r src/mpm_cli/catalog/`.
- Verify `pyproject.toml`'s `[tool.hatch.build.targets.wheel]` block
  does not reintroduce the dir via globs. Today's `packages = ["src/mpm_cli"]`
  ships every Python package under `src/mpm_cli/`; the deleted
  `catalog/` directory had no `__init__.py` so it was shipped as raw
  package-data, not as an importable package. Confirm via a built-wheel
  inspection that `catalog/` is not present in the archive.
- If `pyproject.toml` needs an explicit exclude rule (e.g.,
  `[tool.hatch.build.targets.wheel.force-include]` or
  `[tool.hatch.build.targets.wheel.exclude]`), add it. Document the
  reasoning in a one-line `# E6-F2-S1-T1` comment so future
  maintainers see why.
- Add `src/mpm_cli/catalog/` to `.gitignore` AND to a repo guard
  (CI workflow step or a pre-commit hook entry) that fails the build
  if any file under `src/mpm_cli/catalog/` appears in a future
  commit. The guard is implementable as a one-line bash check in the
  existing GitHub Actions workflow (`.github/workflows/ci.yml` or
  equivalent) along the lines of:
  ```
  - name: Verify bundled catalog removed
    shell: bash
    run: |
      if [ -d "src/mpm_cli/catalog" ]; then
        echo "ERROR: src/mpm_cli/catalog/ must remain removed (see backlog/E6-F2-S1-T1)" >&2
        exit 1
      fi
  ```
- Author `tests/test_wheel_layout.py` (a NEW test file, not under
  `tests/unit/` because it builds the wheel) that:
  - Runs `uv build --wheel` (or `python -m build --wheel`) into a
    `tmp_path` directory.
  - Opens the built `.whl` archive via `zipfile.ZipFile`.
  - Asserts `not any(name.startswith("mpm_cli/catalog/") for name in
    zf.namelist())`.
  - Also asserts the wheel DOES contain expected files (e.g.,
    `mpm_cli/__init__.py`, `mpm_cli/commands/bootstrap.py`) so a
    catastrophically-empty wheel fails the test loudly.
- Author `tests/integration/test_no_bundled_catalog.py` that asserts
  `importlib.resources.files("mpm_cli")` (or the equivalent
  modern API) does NOT expose a `catalog` subpath at runtime; the
  test imports `mpm_cli` fresh and walks the package resources.

This task does NOT modify `core/catalog.py`'s resolver fallback (that
is T2's scope). After this task lands, calling `resolve_catalog_dir(None)`
in a fresh environment WILL still hit `_get_bundled_catalog_dir()` and
return a path that no longer exists on disk -- which is acceptable as
a transitional state because (a) every shim path in E6-F1 is already
exit-3 before the catalog resolver runs, (b) the integration test
exercises the missing-on-disk behaviour explicitly, and (c) T2 lands
in the same epic to clean up the dead code path.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 0.3
("the bundled `src/mpm_cli/catalog/` directory is deleted") plus
Section 1 context bullet 3 (bundled catalog removed from the wheel)
plus Section 4.9 "Removed behavior" paragraph (bundled catalog
deleted alongside the shim).

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e6-f2-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E6-F2-S1-T2 | Remove third-tier "bundled fallback" from `resolve_catalog_dir` | in-queue |

### Approach

1. **TDD RED (wheel layout)**: write `tests/test_wheel_layout.py`
   that builds the wheel via `subprocess.run(["uv", "build",
   "--wheel", "--out-dir", str(tmp_path)])`, opens the resulting
   `.whl` with `zipfile.ZipFile`, and asserts no member name starts
   with `mpm_cli/catalog/`. Run `uv run pytest
   tests/test_wheel_layout.py -v` and confirm FAIL against today's
   tree (the catalog dir IS shipped today).
2. **TDD RED (runtime integration)**: write
   `tests/integration/test_no_bundled_catalog.py` that imports
   `mpm_cli` and uses `importlib.resources.files("mpm_cli")`
   (or `(pathlib.Path(mpm_cli.__file__).parent / "catalog")`) to
   assert the `catalog` subpath does NOT exist at runtime. Confirm
   FAIL against today's tree.
3. **TDD GREEN (delete the tree)**: `git rm -r src/mpm_cli/catalog/`.
   Run both red tests; confirm they now PASS.
4. **TDD GREEN (verify pyproject)**: inspect
   `[tool.hatch.build.targets.wheel]`; today it ships
   `src/mpm_cli` (the whole package) and includes `repo`,
   `git_ssh`, `hooks/*`, `requirements.jsonc` at the project root.
   The deleted `catalog/` directory had no `__init__.py`; hatchling
   ships subdirectories with `__init__.py` as packages. Confirm by
   rerunning the wheel-layout test that the deleted dir does not
   reappear via any include glob. If it DOES reappear, add an
   explicit `exclude = ["src/mpm_cli/catalog"]` entry under
   `[tool.hatch.build.targets.wheel]` with a one-line `# E6-F2-S1-T1`
   comment.
5. **TDD GREEN (gitignore + CI guard)**: add `src/mpm_cli/catalog/`
   to `.gitignore`. Add a step to the existing GitHub Actions CI
   workflow (the mpm repo has `.github/workflows/*.yml`; the
   exact file name is determined during execution by inspecting the
   workflows dir) that runs the existence-check bash snippet shown
   in the Description. Use `shell: bash` and `set -euo pipefail` per
   the project's GitHub Workflows standards.
6. **TDD REFACTOR**: confirm no other test or source file referenced
   `src/mpm_cli/catalog/` via grep:
   `grep -rn "mpm_cli/catalog" src tests`. The only remaining
   reference SHOULD be inside `src/mpm_cli/core/catalog.py`
   (the `_get_bundled_catalog_dir()` helper, which T2 removes).
7. **Integration**: run the full suite locally; the catalog dir
   removal must not break any pre-existing test (other tests that
   exercise the bundled-fallback path are still allowed in this
   intermediate state because T2 has not yet rewritten the
   resolver; those tests, if any, will still pass because the
   resolver still RETURNS the path -- it just points at a deleted
   directory, which the existing tests do not assert against).
8. **Docs**: update `docs/configuration.md` (Section 8 -- existing
   in the repo) to remove any reference to "bundled catalog" or
   "default catalog source". If no such reference exists today, no
   doc change is required for this task; T2's doc update covers the
   user-facing error.
9. **Verify**: run the full suite locally:
   `uv run pytest tests/ -v && uv run ruff check src tests && uv run
   ruff format --check src tests && uv run mypy src && uv run bandit
   -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 0.3 (review-before-backlog item: bundled `src/mpm_cli/catalog/` directory deleted alongside the shim).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 1 context bullet 3 (bundled `src/mpm_cli/catalog/` removed from the mpm wheel).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.9 "Removed behavior" paragraph (bundled catalog deleted alongside the shim).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E6-F1-S1-T1 | Shim core: WARN + exit 3 + zero boundary calls | in-queue |

## Acceptance Criteria

- [ ] AC-FUNC-001 The directory `src/mpm_cli/catalog/` no longer exists in the repository working tree (`git ls-files src/mpm_cli/catalog/` returns zero matches).
- [ ] AC-FUNC-002 The built wheel does not contain any archive member whose name starts with `mpm_cli/catalog/`; verified by building the wheel into a `tmp_path` and walking `zipfile.ZipFile(...).namelist()`.
- [ ] AC-FUNC-003 The built wheel still contains the expected core files (`mpm_cli/__init__.py` (ref), `mpm_cli/commands/bootstrap.py` (ref), `mpm_cli/core/catalog.py` (ref), `mpm_cli/constants.py` (ref)) so the test catches catastrophically-empty wheels.
- [ ] AC-FUNC-004 At runtime, `importlib.resources.files("mpm_cli")` (or `pathlib.Path(mpm_cli.__file__).parent / "catalog"`) does NOT expose a `catalog` subpath; verified by an integration test that imports `mpm_cli` fresh.
- [ ] AC-FUNC-005 The repo guard (one CI workflow step using `shell: bash` and `set -euo pipefail`) fails the build with exit code 1 and a clear stderr message when `src/mpm_cli/catalog/` reappears in a future commit; verified by a unit test that runs the guard script logic against a fixture directory tree containing `src/mpm_cli/catalog/`.
- [ ] AC-FUNC-006 `.gitignore` is updated to include `src/mpm_cli/catalog/`; verified by the new wheel-layout test reading `.gitignore` and asserting the pattern is present.
- [ ] AC-FUNC-007 `pyproject.toml`'s `[tool.hatch.build.targets.wheel]` block does not introduce any glob that re-ships `src/mpm_cli/catalog/`; if an explicit `exclude` entry is added, it carries a one-line `# E6-F2-S1-T1` comment naming the work unit.
- [ ] AC-TEST-001 New test file `tests/test_wheel_layout.py` builds the wheel and asserts the archive contents per AC-FUNC-002 and AC-FUNC-003; also asserts the `.gitignore` pattern is present per AC-FUNC-006.
- [ ] AC-TEST-002 New test file `tests/integration/test_no_bundled_catalog.py` asserts the runtime absence per AC-FUNC-004 plus an additional negative-control case asserting that a `mpm_cli/commands/` (or `mpm_cli/core/`) subpath IS present (so the "catalog absent" assertion can fail meaningfully).
- [ ] AC-TEST-003 The new CI workflow step is exercised by a unit-level test (or by running the guard's bash script directly via `subprocess.run` against a fixture directory tree); asserts exit code 1 when `src/mpm_cli/catalog/` exists and exit code 0 when it does not.
- [ ] AC-CYCLE-001 End-to-end wheel-build cycle: build the wheel from the post-deletion tree; install the wheel into a fresh `tmp_path` venv via `uv pip install <built-wheel>`; assert `python -c "import mpm_cli; import pathlib; assert not (pathlib.Path(mpm_cli.__file__).parent / 'catalog').exists()"` exits 0. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 If `docs/configuration.md` (ref) references "bundled catalog" or "default catalog source", those references are removed in the same commit. If no such references exist, this AC is satisfied by inspection (recorded in `## TDD Cycle Log`).
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files -- N/A for this task's no-new-`src/*.py`-source delete-only scope (no production Python source authored; the new tests cover repo-layout assertions, not new src lines).
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/catalog/` | Delete -- entire directory tree (the one entry `mpm/` and any `.mpm`, README, or stub files under it). |
| `.gitignore` | Update -- add `src/mpm_cli/catalog/` line so the deleted tree cannot accidentally be re-added. |
| `pyproject.toml` | Update -- iff the post-delete wheel-layout test fails, add an explicit `exclude = ["src/mpm_cli/catalog"]` entry under `[tool.hatch.build.targets.wheel]` with a one-line `# E6-F2-S1-T1` comment; otherwise no change. |
| `.github/workflows/ci.yml` | Update -- add a `shell: bash` step (with `set -euo pipefail`) that fails the build if `src/mpm_cli/catalog/` reappears; exact workflow file name to be confirmed by inspecting the mpm repo's `.github/workflows/` directory at execution time. |
| `tests/test_wheel_layout.py` | New -- builds the wheel via `uv build --wheel`, asserts no `mpm_cli/catalog/` archive members, asserts expected core files are present, asserts `.gitignore` pattern present. |
| `tests/integration/test_no_bundled_catalog.py` | New -- imports `mpm_cli`, asserts runtime absence of the `catalog` subpath via `importlib.resources` or `pathlib`. Includes a wheel-install-and-import end-to-end cycle case. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] `git ls-files src/mpm_cli/catalog/` returns zero matches.
- [ ] `grep -rn "mpm_cli/catalog" src tests` returns only matches inside `src/mpm_cli/core/catalog.py` (ref) (the `_get_bundled_catalog_dir()` helper -- T2's scope to delete).
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run plus the wheel-build invocation output.

## TDD Cycle Log

## Comments
