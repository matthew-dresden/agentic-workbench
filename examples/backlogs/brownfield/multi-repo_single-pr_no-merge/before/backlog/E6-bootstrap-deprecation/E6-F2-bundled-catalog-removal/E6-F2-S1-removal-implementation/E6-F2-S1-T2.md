# E6-F2-S1-T2: Remove third-tier "bundled fallback" from `resolve_catalog_dir`

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e6-f2-s1-t2`

## Description

Modify `src/mpm_cli/core/catalog.py::resolve_catalog_dir` so when
neither `--catalog-source` nor `MPM_CATALOG_SOURCE` is set, the
function raises the canonical missing-catalog error from spec
Section 4 (verbatim text) instead of returning the bundled directory.
Today's lines 40-49 implement the fallback as
`return _get_bundled_catalog_dir()`; after this task, the function
hard-fails fast.

The canonical missing-catalog error message (verbatim, including
newlines and exact wording per spec Section 4) -- the calling
command interpolates its own command name in place of `<command>`:

```
ERROR: <command> requires a catalog source.
Provide one of:
  --catalog-source <git-url>@<ref>      # e.g. --catalog-source https://example.com/org/manifest-repo.git@main
  MPM_CATALOG_SOURCE=<git-url>@<ref>  # set as env var, then re-run
The CLI flag takes precedence when both are set.
A catalog source identifies a manifest repo (a git repository whose
repo-specs/ directory exposes installable mpm dependencies).
See docs/catalogs-explained.md for what a manifest repo is and how to find one.
See docs/configuration.md for the full configuration reference.
```

The error is implemented as a new exception class
`MissingCatalogSourceError(ValueError)` inside `core/catalog.py`. The
calling command (today only `mpm bootstrap`, which is now an
exit-3 shim and never reaches the resolver; tomorrow `mpm list`,
`mpm add`, `mpm outdated`, etc. landing in later epics) catches
the exception, formats the canonical text with its own command name,
and writes to stderr / exits 1. Per the spec Section 4 header, the
following commands hard-error on missing source: `list`, `add`,
`outdated`, `why`, `catalog audit`. `install` and `doctor` retain a
lockfile fallback (separate code path, not affected by this task).

Concretely:

- Add `MissingCatalogSourceError(ValueError)` class to
  `core/catalog.py` carrying no fields beyond the exception message
  (the calling command does the message formatting; the exception
  itself is a marker indicating the resolver could not produce a
  source).
- Rewrite `resolve_catalog_dir`: drop the
  `return _get_bundled_catalog_dir()` line; when `source` is
  falsy after the CLI / env merge, raise `MissingCatalogSourceError()`
  with no args (the caller supplies the formatted message).
- Delete the `_get_bundled_catalog_dir()` helper entirely. Verify
  zero remaining consumers via `grep -rn "_get_bundled_catalog_dir"
  src tests` -- only the pre-task definition + the imports it
  triggers should match before deletion; after deletion, zero
  matches.
- Update / replace every existing test that exercised the bundled-
  fallback success path. Today's `tests/unit/test_catalog.py` (if
  present) likely has a parametrised case asserting
  `resolve_catalog_dir(None)` returns a path under
  `mpm_cli/catalog`; that case becomes `resolve_catalog_dir(None)
  raises MissingCatalogSourceError`.
- Add new integration test
  `tests/integration/test_no_default_catalog.py` that confirms the
  end-to-end behaviour: with `MPM_CATALOG_SOURCE` unset and no
  `--catalog-source` flag, the resolver raises and the CLI command
  surfaces the canonical missing-catalog error to stderr.

After this task lands, the only ways to invoke a command needing a
catalog source are (a) the `--catalog-source` flag, (b) the
`MPM_CATALOG_SOURCE` env var, or (for `install` / `doctor`) (c)
the lockfile's `[catalog].source` field. There is no implicit
default. This matches spec Section 13 decision 19 ("no default
manifest repo").

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 4
header (verbatim missing-catalog error message + "The bundled
`src/mpm_cli/catalog/` directory is deleted by this spec's
bootstrap-deprecation epic; the existing third-tier 'bundled
fallback' in `resolve_catalog_dir()` is removed at the same time.")
plus Section 13 decision 19 (no default manifest repo).

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e6-f2-s1-t2` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|

### Approach

1. **TDD RED (unit)**: update
   `tests/unit/test_resolve_catalog_dir.py` (existing or new --
   determined by inspecting the mpm repo at execution time) to
   parametrise the hard-error behaviour: `resolve_catalog_dir(None)`
   with `MPM_CATALOG_SOURCE` unset MUST raise
   `MissingCatalogSourceError`; `resolve_catalog_dir(None)` with
   `MPM_CATALOG_SOURCE` SET to a fixture file:// URL MUST still
   succeed via the env-var branch (unchanged behaviour);
   `resolve_catalog_dir("file:///some/path")` MUST still succeed via
   the flag branch (unchanged behaviour). Run `uv run pytest
   tests/unit/test_resolve_catalog_dir.py -v` and confirm the new
   `MissingCatalogSourceError` case FAILS against today's code (it
   currently returns the bundled path).
2. **TDD RED (integration)**: write
   `tests/integration/test_no_default_catalog.py` that invokes a
   catalog-requiring command (e.g., `mpm list` once E2 lands; for
   THIS task it is sufficient to exercise `resolve_catalog_dir`
   directly via `subprocess.run` of a one-liner Python script that
   imports the function and calls it without args). Asserts the
   exception is raised AND that when the exception is caught and
   formatted by a hypothetical command handler (this task's
   integration test includes a small dummy command in the fixture
   that catches the exception and writes the canonical error text),
   the stderr text matches the verbatim spec Section 4 message.
   Confirm FAIL.
3. **TDD GREEN (exception class)**: add `MissingCatalogSourceError`
   to `src/mpm_cli/core/catalog.py`. It is a subclass of
   `ValueError` (consistent with the existing
   `_parse_catalog_source` `ValueError` raises) and carries no
   additional fields.
4. **TDD GREEN (resolver)**: rewrite `resolve_catalog_dir`:
   ```
   def resolve_catalog_dir(catalog_source: str | None = None) -> pathlib.Path:
       source = catalog_source or os.environ.get(CATALOG_ENV_VAR)
       if source:
           return _clone_remote_catalog(source)
       raise MissingCatalogSourceError()
   ```
   Update the docstring to reflect the new contract (raises on
   missing source; no third-tier fallback) and reference spec
   Section 4.
5. **TDD GREEN (delete helper)**: `git rm` is not needed (the
   helper is a function inside `core/catalog.py`, not a file).
   Delete the `_get_bundled_catalog_dir` function body. Verify via
   `grep -rn "_get_bundled_catalog_dir" src tests`: zero matches
   after the edit. Adjust imports: the function no longer needs
   `pathlib.Path(__file__).parent / "catalog"` construction so the
   `pathlib` import remains (used by `_clone_remote_catalog`).
6. **TDD REFACTOR**: confirm no dead code remains; confirm the new
   `MissingCatalogSourceError` is reachable from every callsite
   today (the only existing caller is `commands/bootstrap.py`,
   which E6-F1-S1-T1 has already converted into a shim that never
   reaches the resolver; future commands in E2+ will catch the
   exception explicitly).
7. **Integration**: run the end-to-end test from step 2; confirm
   green.
8. **Docs**: update `docs/catalogs-explained.md` (NEW per spec
   Section 8 -- if the file does not exist yet, this task creates
   the minimal first version with the missing-source remediation
   pointer; later docs-epic tasks expand it). Update
   `docs/configuration.md` to remove any "bundled catalog" or
   "default catalog source" references AND add a paragraph noting
   "There is no default catalog source; one of `--catalog-source`
   or `MPM_CATALOG_SOURCE` is required (except for `mpm install`
   and `mpm doctor`, which may use the lockfile's `[catalog].source`
   field as a fallback)."
9. **Verify**: run the full suite locally:
   `uv run pytest tests/ -v && uv run ruff check src tests && uv run
   ruff format --check src tests && uv run mypy src && uv run bandit
   -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4 header (verbatim missing-catalog error message + third-tier fallback removed).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 13 decision 19 (no default manifest repo; no implicit default catalog source).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.9 "Removed behavior" paragraph (the third-tier 'bundled fallback' in `resolve_catalog_dir()` is removed alongside the shim).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E6-F2-S1-T1 | Delete `src/mpm_cli/catalog/` from the wheel and add repo / CI guard | in-queue |
| E5-F1-S1-T4 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 `MissingCatalogSourceError(ValueError)` exception class is defined in `src/mpm_cli/core/catalog.py`; it carries no additional fields beyond the standard `ValueError` message.
- [ ] AC-FUNC-002 `resolve_catalog_dir(None)` with `MPM_CATALOG_SOURCE` unset raises `MissingCatalogSourceError`; the resolver no longer returns any bundled path.
- [ ] AC-FUNC-003 `resolve_catalog_dir(None)` with `MPM_CATALOG_SOURCE` SET to a valid `<url>@<ref>` value still succeeds and returns the cloned catalog path (unchanged env-var branch behaviour).
- [ ] AC-FUNC-004 `resolve_catalog_dir("file:///some/path@main")` (or any non-None catalog source) still succeeds and returns the cloned catalog path (unchanged flag-branch behaviour); CLI flag still wins over env var.
- [ ] AC-FUNC-005 The `_get_bundled_catalog_dir()` helper function is deleted from `src/mpm_cli/core/catalog.py`; `grep -rn "_get_bundled_catalog_dir" src tests` returns zero matches after this task lands.
- [ ] AC-FUNC-006 The canonical missing-catalog error text per spec Section 4 (verbatim multiline string ending with `See docs/configuration.md for the full configuration reference.`) is documented as the expected stderr output a calling command produces when it catches `MissingCatalogSourceError`; an integration test exercises this via a fixture command and asserts the stderr matches the verbatim spec text.
- [ ] AC-FUNC-007 `resolve_catalog_dir`'s docstring is updated to describe the new contract: "Resolves the catalog directory from the `--catalog-source` flag or `MPM_CATALOG_SOURCE` env var. Raises `MissingCatalogSourceError` when neither is set. See spec Section 4."; the old reference to a "Bundled catalog shipped with the `mpm_cli` package" is removed.
- [ ] AC-TEST-001 `tests/unit/test_resolve_catalog_dir.py` (new or updated) parametrises three cases: (a) `resolve_catalog_dir(None)` with no env var -> raises `MissingCatalogSourceError`; (b) `resolve_catalog_dir(None)` with env var set -> returns a path under `tmp_path`; (c) `resolve_catalog_dir("<url>@<ref>")` -> returns a path under `tmp_path` regardless of env var.
- [ ] AC-TEST-002 New integration test file `tests/integration/test_no_default_catalog.py` runs a subprocess invocation of `python -c "import os; os.environ.pop('MPM_CATALOG_SOURCE', None); from mpm_cli.core.catalog import resolve_catalog_dir, MissingCatalogSourceError; ..."`; asserts the exception is raised AND that a fixture command handler in the test produces the verbatim spec Section 4 missing-source error text on stderr.
- [ ] AC-TEST-003 All pre-existing tests that asserted `resolve_catalog_dir(None)` returned a path ending in `mpm_cli/catalog` are UPDATED in the same commit to assert the new raise-behaviour; `grep -rn "_get_bundled_catalog_dir\|bundled catalog" tests` returns zero matches after this task lands.
- [ ] AC-CYCLE-001 End-to-end no-default-catalog cycle: from a fresh shell with `MPM_CATALOG_SOURCE` unexported, run a subprocess that imports `resolve_catalog_dir` and calls it with `None`; assert the subprocess captures `MissingCatalogSourceError` and its handler writes the verbatim spec Section 4 missing-source error text to stderr; subprocess exit code is 1. Repeat with `MPM_CATALOG_SOURCE` set to a local `file://` fixture URL; assert the resolver returns a non-error path (env-var branch still works). Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/catalogs-explained.md` is created (NEW per spec Section 8) with at minimum a first-paragraph definition of "manifest repo" and a section "What does the missing-catalog-source error mean?" reproducing the verbatim Section 4 error message. Later docs-epic tasks may expand it; this task lands the minimal file required by the missing-source error's `See docs/catalogs-explained.md` remediation pointer.
- [ ] AC-DOC-002 `docs/configuration.md` is updated to add a paragraph: "There is no default catalog source; one of `--catalog-source` or `MPM_CATALOG_SOURCE` is required (except for `mpm install` and `mpm doctor`, which may use the lockfile's `[catalog].source` field as a fallback)."; any pre-existing reference to "bundled catalog" or "default catalog" is removed.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/core/catalog.py` | Update -- add `MissingCatalogSourceError(ValueError)` class; rewrite `resolve_catalog_dir` to raise `MissingCatalogSourceError` when neither flag nor env var is set (no third-tier fallback); delete `_get_bundled_catalog_dir()` helper; update docstring to reference spec Section 4. |
| `tests/unit/test_resolve_catalog_dir.py` | New or Update -- parametrised cases for the three resolver paths (missing-source raises, env-var-set succeeds, flag-set succeeds); existing pre-task cases asserting bundled-fallback success are updated to assert the new raise behaviour. |
| `tests/integration/test_no_default_catalog.py` | New -- subprocess invocation asserting `MissingCatalogSourceError` is raised and the canonical Section 4 error text reaches stderr through a fixture command handler. |
| `docs/catalogs-explained.md` | New -- minimal first version defining "manifest repo" and reproducing the verbatim Section 4 missing-source error text; the file is referenced by the error's `See docs/...` remediation pointer. |
| `docs/configuration.md` | Update -- remove any reference to "bundled catalog" / "default catalog source"; add a paragraph stating there is no default catalog source. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on `src/mpm_cli/core/catalog.py` for new + modified lines.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] `grep -rn "_get_bundled_catalog_dir" src tests` returns zero matches.
- [ ] `grep -rn "bundled catalog\|bundled-catalog" src tests docs` returns zero matches (other than inside this work-unit's own pre-task reference removal commit).
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E6-F2-S1-T2 manually blocked on E5-F1-S1-T4 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E5-F1-S1-T4
