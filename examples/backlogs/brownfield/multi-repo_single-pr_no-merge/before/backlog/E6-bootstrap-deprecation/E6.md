# E6: `mpm bootstrap` deprecation shim and bundled `src/mpm_cli/catalog/` removal

## Status: in-queue

## Description

Convert `mpm bootstrap` and `mpm bootstrap list` into hard-stop
deprecation shims that print a WARN to stderr (with the verbatim
replacement command) and exit with status 3 without performing any
work, AND delete the bundled `src/mpm_cli/catalog/` directory from
the mpm wheel.

The shim does NOT delegate to `mpm add` / `mpm list`; the operator
runs the suggested replacement command explicitly. The shim does NOT
read from `<catalog-metadata>`, does NOT touch the filesystem, and
does NOT import `core/catalog.py` or `core/bootstrap.py`. The legacy
business module `src/mpm_cli/core/bootstrap.py` is deleted entirely.

The bundled catalog removal pairs with the shim: the third-tier
"bundled fallback" path inside `core/catalog.py::resolve_catalog_dir`
(today's lines 40-49, returning `_get_bundled_catalog_dir()`) is
deleted in the same epic so that no command silently picks up the
bundled tree after deletion. Once both land, the only valid catalog
sources are the `--catalog-source` CLI flag and the
`MPM_CATALOG_SOURCE` env var; missing-source invocations hard-fail
with the canonical error from spec Section 4.

E6 has NO upstream dep on E2/E3/E4/E5; it stands alone after E1-F4
(shared `core/cli_args.py`) lands. Splitting the two halves into F1
(shim) and F2 (bundled removal) keeps each PR reviewable in
isolation, and F2 depends on F1 because the self-bootstrap path
(`mpm bootstrap mpm` reading the bundled `catalog/mpm/.mpm`
template) must be neutralised before the bundled tree can go.

Spec source:
- `spec/mpm-list-add-lock-features-spec.md` Section 0.3 (review-
  before-backlog item: bootstrap deprecation + bundled catalog
  removal)
- `spec/mpm-list-add-lock-features-spec.md` Section 4.9 (`mpm
  bootstrap` deprecated -- behaviour table, flag translation table,
  exit code 3, `--help` DEPRECATED prefix)
- `spec/mpm-list-add-lock-features-spec.md` Section 1 context bullet
  3 (bundled `src/mpm_cli/catalog/` removed from the wheel)
- `spec/mpm-list-add-lock-features-spec.md` Section 13 decision 19
  (no default manifest repo; resolver fallback removed)
- `spec/mpm-list-add-lock-features-spec.md` Section 14 (`mpm
  bootstrap --help` snapshot rule: DEPRECATED prefix + inline flag
  translation table)

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E1-F4-S1-T1 | Move `--catalog-source` flag definition to `core/cli_args.py` | in-queue |

## Target Repository

- **Repo:** `matthew-dresden/mpm`
