# E6-F1-S1-T2: Flag translation table (static argv translation)

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e6-f1-s1-t2`

## Description

Add a pure-function flag translator inside
`src/mpm_cli/commands/bootstrap.py` that maps today's `mpm
bootstrap` argv into the equivalent `mpm add <name>` or `mpm
list` argv string. The translation is a static string transformation
ONLY -- the shim does NOT execute the translated command, does NOT
parse semantically, does NOT validate flag values; it simply prints
the translation inside the WARN body so the operator can copy-paste
the replacement command.

T1 supplied the WARN-text helper `_format_deprecated_warn(invocation,
replacement_argv_tail)` with `replacement_argv_tail` defaulted to the
empty string. T2 wires in the real translator: a new
`_translate_bootstrap_argv_tail(args: argparse.Namespace) -> str`
function whose output becomes the `[...translated flags]` portion of
the WARN body.

The flag translation table per spec Section 4.9 (verbatim):

| Bootstrap flag (today) | Replacement (`mpm add`) | Replacement (`mpm list`) | Notes |
|---|---|---|---|
| `<package>` positional | `<name>` positional | (n/a -- `bootstrap list` triggers `mpm list`) | identical semantics |
| `--catalog-source <v>` | `--catalog-source <v>` | `--catalog-source <v>` | identical |
| `--output-dir <v>` | (no equivalent) | (no equivalent) | WARN includes: `"--output-dir has no direct equivalent in 'mpm add'; the install workspace is the current directory or MPM_WORKSPACE_DIR if set."` |

`mpm bootstrap list` flag set: only `--catalog-source` is
meaningful. Any other flag (today, only `--output-dir` exists)
triggers a generic `"--<flag> has no equivalent in 'mpm list'"`
notice appended to the WARN body.

Worked translation examples (verbatim output of
`_translate_bootstrap_argv_tail`):

- `mpm bootstrap mpm` -> tail `""` (no flags to translate). WARN
  body shows `    mpm add mpm`.
- `mpm bootstrap mpm --catalog-source https://example.com/x.git@main` ->
  tail `"--catalog-source https://example.com/x.git@main"`. WARN body
  shows `    mpm add mpm --catalog-source
  https://example.com/x.git@main`.
- `mpm bootstrap mpm --output-dir ./scratch` -> tail `""` plus an
  appended notice line:
  `Note: --output-dir has no direct equivalent in 'mpm add'; the
  install workspace is the current directory or MPM_WORKSPACE_DIR
  if set.`
- `mpm bootstrap list --catalog-source https://example.com/x.git@main` ->
  tail `"--catalog-source https://example.com/x.git@main"`. WARN
  body shows `    mpm list --catalog-source
  https://example.com/x.git@main`.
- `mpm bootstrap list --output-dir ./scratch` -> tail `""` plus an
  appended notice line:
  `Note: --output-dir has no equivalent in 'mpm list'.`

The translator is a pure function (no side effects, deterministic for
identical input) so it is exhaustively covered by parametrised unit
tests. Each table row above is one or more parametrised cases.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 4.9
flag translation table + `mpm bootstrap list` flag set paragraph.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e6-f1-s1-t2` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E6-F1-S1-T3 | `--help` DEPRECATED prefix and retained discoverability | in-queue |

### Approach

1. **TDD RED (unit)**: write
   `tests/unit/test_bootstrap_flag_translation.py` parametrising
   every translation case from the table above. Each case asserts
   the exact return string of `_translate_bootstrap_argv_tail`
   (including any appended `Note:` lines for no-equivalent flags).
   Run `uv run pytest tests/unit/test_bootstrap_flag_translation.py
   -v` and confirm FAIL (function does not exist yet).
2. **TDD RED (integration)**: write
   `tests/integration/test_bootstrap_shim_translation.py` that
   invokes `python -m mpm_cli bootstrap mpm --catalog-source
   <url>` via `subprocess.run` and asserts the stderr WARN body
   contains the translated `mpm add mpm --catalog-source <url>`
   replacement line verbatim. Repeat for the four other translation
   cases. Confirm FAIL against today's code.
3. **TDD GREEN**: implement
   `_translate_bootstrap_argv_tail(args: argparse.Namespace) -> str`
   in `src/mpm_cli/commands/bootstrap.py`:
   - Inspect `args.package` to choose between the `add` and `list`
     translation arm.
   - For `args.catalog_source` not None, append `--catalog-source
     <value>` to the tail (shell-quoting is the operator's
     responsibility; the translator emits the value verbatim).
   - For `args.output_dir` not equal to the default
     `pathlib.Path(".")`, append the appropriate `Note:` line
     (different text per arm).
   - Return the assembled tail string (empty if no flags translated).
   Wire the new helper into the existing `_run` from T1 so the WARN
   body now includes the translated tail.
4. **TDD REFACTOR**: extract the `Note:` line templates as private
   module constants `_NOTE_OUTPUT_DIR_ADD` and `_NOTE_OUTPUT_DIR_LIST`
   so test assertions reference the constants (DRY); confirm no
   duplication of the verbatim Section 4.9 strings between source
   and test files.
5. **Integration**: run the end-to-end suite from step 2; confirm
   green.
6. **Docs**: update `docs/migration-bootstrap-to-add.md` (NEW, created
   in this task) with the verbatim flag translation table from spec
   Section 4.9 plus worked examples mirroring this task's Description.
7. **Verify**: run the full suite locally:
   `uv run pytest tests/ -v && uv run ruff check src tests && uv run
   ruff format --check src tests && uv run mypy src && uv run bandit
   -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.9 flag translation table (verbatim) and `mpm bootstrap list` flag-set paragraph.
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 8 (`docs/migration-bootstrap-to-add.md` NEW, including the flag translation table).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E6-F1-S1-T1 | Shim core: WARN + exit 3 + zero boundary calls | in-queue |
| E5-F2-S1-T7 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 The pure function `_translate_bootstrap_argv_tail(args: argparse.Namespace) -> str` exists in `src/mpm_cli/commands/bootstrap.py` and is deterministic (identical input always yields identical output).
- [ ] AC-FUNC-002 For `args.package == "mpm"`, `args.catalog_source is None`, `args.output_dir == pathlib.Path(".")`, the translator returns the empty string (no flags to translate).
- [ ] AC-FUNC-003 For `args.package == "mpm"`, `args.catalog_source == "https://example.com/x.git@main"`, default `output_dir`, the translator returns `"--catalog-source https://example.com/x.git@main"` exactly.
- [ ] AC-FUNC-004 For `args.package == "mpm"`, default `catalog_source`, `args.output_dir == pathlib.Path("./scratch")`, the translator returns a string containing the verbatim notice `Note: --output-dir has no direct equivalent in 'mpm add'; the install workspace is the current directory or MPM_WORKSPACE_DIR if set.`.
- [ ] AC-FUNC-005 For `args.package == "list"`, `args.catalog_source == "https://example.com/x.git@main"`, default `output_dir`, the translator returns `"--catalog-source https://example.com/x.git@main"` exactly (identical translation for both arms).
- [ ] AC-FUNC-006 For `args.package == "list"`, default `catalog_source`, `args.output_dir == pathlib.Path("./scratch")`, the translator returns a string containing the verbatim notice `Note: --output-dir has no equivalent in 'mpm list'.` (distinct from the `add`-arm notice).
- [ ] AC-FUNC-007 The translator emits the `--catalog-source` value byte-for-byte as supplied by the operator; no shell quoting, no value rewriting (shell-quoting is the operator's responsibility per spec Section 4.9 note).
- [ ] AC-FUNC-008 The `Note:` line templates `_NOTE_OUTPUT_DIR_ADD` and `_NOTE_OUTPUT_DIR_LIST` are defined as module-level private constants in `src/mpm_cli/commands/bootstrap.py`; test files import the constants rather than restating the verbatim text (DRY).
- [ ] AC-FUNC-009 The T1 helper `_format_deprecated_warn(invocation, replacement_argv_tail)` is now called with the real translator output (not the empty string); the WARN body printed to stderr therefore now contains the translated replacement command.
- [ ] AC-TEST-001 New unit test file `tests/unit/test_bootstrap_flag_translation.py` exists and parametrises every translation case from the table above (at least the six AC-FUNC cases plus combinations: catalog-source + output-dir together for both arms).
- [ ] AC-TEST-002 New integration test file `tests/integration/test_bootstrap_shim_translation.py` exists and invokes `python -m mpm_cli bootstrap ...` via `subprocess.run` for each translation case; asserts the stderr WARN body contains the translated replacement line verbatim.
- [ ] AC-TEST-003 An additional parametrised unit case asserts the translator is deterministic by calling it twice on identical input and asserting both return values are equal.
- [ ] AC-CYCLE-001 End-to-end translation cycle: invoke `mpm bootstrap mpm --catalog-source https://example.com/x.git@main --output-dir ./scratch` via `subprocess.run` from a `tmp_path` working directory; assert stderr contains both the translated `mpm add mpm --catalog-source https://example.com/x.git@main` line AND the verbatim `Note: --output-dir has no direct equivalent in 'mpm add'; ...` notice; assert exit code 3; assert `./scratch` was NOT created (no filesystem mutation). Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/migration-bootstrap-to-add.md` (NEW) is created in the same commit containing: the verbatim flag translation table from spec Section 4.9, all worked translation examples from this task's Description, and a "why bootstrap is deprecated" paragraph (forced migration at the CI / script boundary).
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/commands/bootstrap.py` | Update -- add `_translate_bootstrap_argv_tail` pure-function translator and the private constants `_NOTE_OUTPUT_DIR_ADD` / `_NOTE_OUTPUT_DIR_LIST`; wire the translator output into the existing T1 WARN-body helper. |
| `tests/unit/test_bootstrap_flag_translation.py` | New -- parametrised unit tests covering every flag translation case (catalog-source identity, output-dir no-equivalent for both arms, determinism). |
| `tests/integration/test_bootstrap_shim_translation.py` | New -- subprocess-based end-to-end tests asserting stderr WARN body contains the verbatim translated replacement command for each case. |
| `docs/migration-bootstrap-to-add.md` | New -- operator-facing migration guide including the verbatim flag translation table from spec Section 4.9 and worked translation examples. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on `src/mpm_cli/commands/bootstrap.py` for new + modified lines (the translator is a pure function so 100% is achievable through the parametrised unit tests alone).
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E6-F1-S1-T2 manually blocked on E5-F2-S1-T7 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E5-F2-S1-T7
