# E6-F1-S1-T3: `--help` DEPRECATED prefix and retained discoverability

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e6-f1-s1-t3`

## Description

Adjust the argparse parser in `src/mpm_cli/commands/bootstrap.py` so
that `mpm bootstrap --help` (or `mpm bootstrap` with no positional
arg) prints argparse's normal help output PREPENDED with the verbatim
DEPRECATED notice from spec Section 4.9 behaviour table (last row):

```
DEPRECATED: 'mpm bootstrap' is replaced by 'mpm add' and 'mpm list'. See docs/migration-bootstrap-to-add.md.
```

The help output ALSO embeds the verbatim flag translation table
from spec Section 4.9 inline (in the argparse `description` /
`epilog` field, rendered using `argparse.RawDescriptionHelpFormatter`
so the pipe-delimited table format is preserved byte-for-byte). The
inline table mirrors T2's translator output table; both reference
the same canonical text (a single module-level constant
`_FLAG_TRANSLATION_TABLE_HELP` is the source of truth, imported by
T3's parser configuration and also referenced by T2's docs).

Exit code for `--help` is 0 (help is informational; spec Section 4.9
behaviour table last row, explicit `0 (help is informational)`).
This contrasts with all non-`--help` invocations (T1) which exit 3.

The retained discoverability requirement (spec Section 14: "the
DEPRECATED prefix MUST be prepended" AND "include the verbatim
flag-translation table from Section 4.9") is satisfied by:

- The DEPRECATED notice is the first line of argparse's
  `description` text, so it appears at the very top of `--help`
  output (before "Usage: ...").
- The flag translation table is embedded as part of `description`
  (or as `epilog`, depending on argparse's rendering order); the
  current `commands/bootstrap.py:31` already uses
  `RawDescriptionHelpFormatter` to preserve newlines in the example
  list, so the same approach renders the table verbatim.
- The "Exit codes" section at the bottom of `--help` lists 0 (help
  output) and 3 (any non-`--help` invocation -- deprecated) for
  this command specifically (other commands list 0 and 1; per spec
  Section 14, bootstrap is the only command that lists 3).

Snapshot test: spec Section 14 ("Verbatim help text for each is
snapshot-tested in `tests/fixtures/help/`") -- this task adds the
`tests/fixtures/help/bootstrap-help.txt` fixture file containing the
exact expected help output. The snapshot test loads the fixture and
compares it byte-for-byte to the captured `subprocess.run([...,
"bootstrap", "--help"])` stdout.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 4.9
behaviour table (last row) + Section 14 (`mpm bootstrap --help`
snapshot rule).

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e6-f1-s1-t3` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|

### Approach

1. **TDD RED (snapshot fixture)**: author the fixture
   `tests/fixtures/help/bootstrap-help.txt` containing the exact
   expected `--help` output: the DEPRECATED notice line, blank line,
   the usage line, the positional and option descriptions, the
   verbatim flag translation table (pipe-delimited), and the
   Exit codes section listing 0 and 3.
2. **TDD RED (unit)**: write `tests/unit/test_bootstrap_help.py`
   that invokes the bootstrap parser directly (via
   `argparse.ArgumentParser` -> `parse_args(["--help"])`) and
   captures the `SystemExit` raised by argparse with exit code 0;
   captures the help text printed to stdout; asserts byte-for-byte
   equality with the fixture file. Confirm FAIL against today's
   code (today's parser has no DEPRECATED prefix, no inline
   translation table).
3. **TDD RED (integration)**: write
   `tests/integration/test_bootstrap_help.py` invoking `python -m
   mpm_cli bootstrap --help` via `subprocess.run`; asserts exit
   code 0, stdout matches fixture byte-for-byte, stderr is empty.
   Confirm FAIL.
4. **TDD GREEN**: update the argparse `subparsers.add_parser(
   "bootstrap", ...)` call in `commands/bootstrap.py`:
   - Replace today's `description=` block with a new constant
     `_BOOTSTRAP_DESCRIPTION` that starts with the verbatim
     DEPRECATED notice line.
   - Add a new constant `_FLAG_TRANSLATION_TABLE_HELP` containing
     the pipe-delimited flag translation table. Append it to the
     parser's `epilog` (kept after `RawDescriptionHelpFormatter`'s
     example list).
   - Add a new constant `_BOOTSTRAP_EXIT_CODES_HELP` listing exit
     codes 0 and 3; append to the `epilog`.
   - Keep `formatter_class=argparse.RawDescriptionHelpFormatter`
     so the table format renders verbatim.
   - The parser's positional `<package>` and option flags
     `--catalog-source`, `--output-dir` remain declared (carried
     over from T1) so they appear in the help text under the
     standard `arguments` / `options` sections.
5. **TDD REFACTOR**: confirm the constant `_FLAG_TRANSLATION_TABLE_HELP`
   is also referenced from T2's tests / docs so the table is defined
   exactly once (DRY); confirm no string duplication between
   `commands/bootstrap.py` and `docs/migration-bootstrap-to-add.md`
   beyond what's inherent to operator-facing documentation.
6. **Integration**: run the end-to-end snapshot test from step 3;
   confirm green.
7. **Docs**: update `docs/migration-bootstrap-to-add.md` (created in
   T2) to add a "What `mpm bootstrap --help` shows" section
   reproducing the verbatim fixture content so operators reading
   the migration guide see the same help text they get from the CLI.
8. **Verify**: run the full suite locally:
   `uv run pytest tests/ -v && uv run ruff check src tests && uv run
   ruff format --check src tests && uv run mypy src && uv run bandit
   -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.9 behaviour table last row (`mpm bootstrap --help` DEPRECATED prefix + retained discoverability + exit 0).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 14 (`mpm bootstrap --help` snapshot rule: DEPRECATED prefix + verbatim flag translation table inline).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E6-F1-S1-T1 | Shim core: WARN + exit 3 + zero boundary calls | in-queue |
| E6-F1-S1-T2 | Flag translation table (static argv translation) | in-queue |

## Acceptance Criteria

- [ ] AC-FUNC-001 Running `mpm bootstrap --help` exits with status 0 (help is informational per spec Section 4.9 behaviour table last row).
- [ ] AC-FUNC-002 Running `mpm bootstrap` with no positional arg AND `--help` flag prints the DEPRECATED notice line `DEPRECATED: 'mpm bootstrap' is replaced by 'mpm add' and 'mpm list'. See docs/migration-bootstrap-to-add.md.` as the FIRST non-empty line of stdout, before the `Usage:` line.
- [ ] AC-FUNC-003 The `--help` output includes the verbatim flag translation table from spec Section 4.9 (pipe-delimited; positional `<package>`, `--catalog-source`, `--output-dir` rows; `mpm add` and `mpm list` columns; Notes column).
- [ ] AC-FUNC-004 The `--help` output includes an `Exit codes` section listing exactly `0 (help output)` and `3 (deprecated invocation; any non-help call)`.
- [ ] AC-FUNC-005 The argparse parser uses `formatter_class=argparse.RawDescriptionHelpFormatter` so the pipe-delimited table renders verbatim (no line-rewrapping).
- [ ] AC-FUNC-006 Module-level constants `_FLAG_TRANSLATION_TABLE_HELP` and `_BOOTSTRAP_EXIT_CODES_HELP` are defined in `src/mpm_cli/commands/bootstrap.py`; the parser configuration references the constants; tests reference the constants (DRY -- the table text is defined exactly once).
- [ ] AC-FUNC-007 The fixture file `tests/fixtures/help/bootstrap-help.txt` is the byte-for-byte expected stdout for `mpm bootstrap --help`; both unit and integration tests compare captured stdout to this fixture.
- [ ] AC-TEST-001 New unit test file `tests/unit/test_bootstrap_help.py` invokes the bootstrap parser via `parse_args(["--help"])`, captures `SystemExit(0)` plus stdout, and asserts byte-for-byte equality with the fixture.
- [ ] AC-TEST-002 New integration test file `tests/integration/test_bootstrap_help.py` runs `python -m mpm_cli bootstrap --help` via `subprocess.run`; asserts exit 0, stdout matches fixture byte-for-byte, stderr is empty.
- [ ] AC-TEST-003 A parametrised case asserts that invoking `mpm bootstrap` with no positional arg AND no `--help` falls through to the T1 shim (exit 3, WARN to stderr), NOT to help output; this proves the `--help` path is gated explicitly on the `--help` flag rather than on missing-args.
- [ ] AC-CYCLE-001 End-to-end help cycle: run `python -m mpm_cli bootstrap --help`; capture stdout; assert the captured bytes exactly equal the bytes of `tests/fixtures/help/bootstrap-help.txt`; assert exit code 0; assert stderr empty. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/migration-bootstrap-to-add.md` is updated in the same commit to add a "What `mpm bootstrap --help` shows" section reproducing the verbatim fixture content (so the migration doc and the CLI help stay in sync).
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/commands/bootstrap.py` | Update -- replace today's `description=...` with the DEPRECATED-prefixed text; add module-level constants `_BOOTSTRAP_DESCRIPTION`, `_FLAG_TRANSLATION_TABLE_HELP`, `_BOOTSTRAP_EXIT_CODES_HELP`; wire them into argparse `description` / `epilog`; keep `RawDescriptionHelpFormatter`. |
| `tests/unit/test_bootstrap_help.py` | New -- parser-level snapshot test (parse_args, capture SystemExit(0) + stdout, byte-for-byte equality with fixture) plus the parametrised "no positional, no `--help`" case proving fall-through to T1 shim. |
| `tests/integration/test_bootstrap_help.py` | New -- subprocess-based snapshot test (`python -m mpm_cli bootstrap --help` -> exit 0, stdout matches fixture, stderr empty). |
| `tests/fixtures/help/bootstrap-help.txt` | New -- the verbatim expected `--help` output; DEPRECATED prefix + Usage + positional/options + flag translation table + Exit codes. |
| `docs/migration-bootstrap-to-add.md` | Update -- add "What `mpm bootstrap --help` shows" section reproducing the verbatim fixture content. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on `src/mpm_cli/commands/bootstrap.py` for new + modified lines.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] `tests/fixtures/help/bootstrap-help.txt` is the single source of truth for the help-text snapshot; both tests reference it.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E6-F1-S1-T3 manually blocked on E6-F1-S1-T2 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E6-F1-S1-T2
