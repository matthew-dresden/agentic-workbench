# E6-F1-S1-T1: Shim core -- WARN + exit 3 + zero boundary calls + delete `core/bootstrap.py`

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e6-f1-s1-t1`

## Description

Rewrite `src/mpm_cli/commands/bootstrap.py` so any invocation OTHER
THAN `--help` prints a WARN to stderr (verbatim text from spec
Section 4.9 behaviour table) and exits with status 3 without
performing any work. The shim does NOT call into `core/catalog.py`,
`core/bootstrap.py`, `<catalog-metadata>` parsing, or any filesystem
mutator. The legacy business module `src/mpm_cli/core/bootstrap.py`
is deleted entirely (no consumers remain after this task).

The verbatim WARN text per spec Section 4.9 behaviour table:

- `mpm bootstrap list [...flags]` -> stderr:
  ```
  WARN: 'mpm bootstrap list' is deprecated. Run instead:
      mpm list [...translated flags]
  See docs/migration-bootstrap-to-add.md.
  ```
  (The `[...translated flags]` text is the static flag-translation
  output produced by T2; this task lands the WARN-text helper that
  accepts a pre-translated argv tail and renders it into the WARN
  body. T1 supplies the empty-tail call site; T2 wires the real
  translator in.)
- `mpm bootstrap <name> [...flags]` -> stderr:
  ```
  WARN: 'mpm bootstrap <name>' is deprecated. Run instead:
      mpm add <name> [...translated flags]
  See docs/migration-bootstrap-to-add.md.
  ```

Both shims exit with status 3 (`EXIT_CODE_DEPRECATED`) via
`raise SystemExit(EXIT_CODE_DEPRECATED)` from the CLI command
handler. Exit 3 is reserved across mpm for "deprecated invocation"
(distinct from 0 success, 1 runtime/usage error, 2 argparse usage
error already used by `cli.py:117` for "no subcommand given"). The
new constant is added to `src/mpm_cli/constants.py`.

Scope boundaries -- explicitly excluded from this task:
- The static argv-to-replacement flag translation table itself
  (lands in T2). T1's WARN helper accepts a pre-translated tail
  string; T1's call site passes `""` for now.
- The `--help` DEPRECATED prefix and the inline flag translation
  table inside argparse's `description` (lands in T3).

What this task DOES include:
- Replace `_run` in `commands/bootstrap.py` with a shim function
  that prints WARN and raises `SystemExit(EXIT_CODE_DEPRECATED)`.
- Remove every `import` from `core/catalog.py`, `core/bootstrap.py`,
  and `pathlib` (the latter no longer needed without `--output-dir`
  resolution). The argparse parser still declares `<package>`,
  `--catalog-source`, and `--output-dir` so help-discoverability is
  preserved across T1/T2/T3, but `_run` no longer reads
  `args.catalog_source` or `args.output_dir`.
- Delete `src/mpm_cli/core/bootstrap.py` entirely. Verify zero
  other consumers via grep before deletion (the only existing
  caller is `commands/bootstrap.py:13`, which T1 rewrites).
- Add `EXIT_CODE_DEPRECATED = 3` to `src/mpm_cli/constants.py`
  under a new `# -- Exit codes --` section.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 4.9
behaviour table (rows 1 and 2, plus the exit-code-3 paragraph).

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e6-f1-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E6-F1-S1-T2 | Flag translation table (static argv translation) | in-queue |
| E6-F1-S1-T3 | `--help` DEPRECATED prefix and retained discoverability | in-queue |
| E6-F2-S1-T1 | Delete `src/mpm_cli/catalog/` from the wheel and add gitignore guard | in-queue |

### Approach

1. **TDD RED (unit)**: write `tests/unit/test_bootstrap_shim.py` that
   imports the new `_run` (or its rewritten replacement) and asserts:
   (a) `mpm bootstrap mpm` prints the exact `WARN: ...` text from
   the spec Section 4.9 behaviour table to stderr; (b) `_run` raises
   `SystemExit(3)`; (c) zero calls into `core.catalog.resolve_catalog_dir`
   via a `pytest` `monkeypatch` that replaces the function with a
   sentinel-raising stub; (d) zero calls to `core.bootstrap.*` (the
   module is deleted, so `import` of it would `ImportError`); (e)
   `mpm bootstrap list` prints the `'mpm bootstrap list' is
   deprecated.` variant and also raises `SystemExit(3)`. Run
   `uv run pytest tests/unit/test_bootstrap_shim.py -v` and confirm
   every new case FAILS against today's code.
2. **TDD RED (integration)**: write
   `tests/integration/test_bootstrap_shim.py` that invokes
   `python -m mpm_cli bootstrap mpm` and `python -m mpm_cli
   bootstrap list` via `subprocess.run` and asserts (a) stderr matches
   the verbatim WARN, (b) stdout is empty (no progress lines, no
   filesystem mutation), (c) exit code is 3, (d) no files were
   created in `tmp_path` (the test passes `--output-dir tmp_path` and
   verifies after the call that `tmp_path` is still empty -- proves
   the shim did NOT delegate). Run and confirm FAIL against today's
   code.
3. **TDD GREEN (constants)**: add `# -- Exit codes --` block and
   `EXIT_CODE_DEPRECATED = 3` to `src/mpm_cli/constants.py`. Run
   `uv run pytest tests/unit/test_constants.py -v` (if a constants
   test file exists) or add a one-line constants assertion to the new
   unit test file.
4. **TDD GREEN (shim)**: rewrite `src/mpm_cli/commands/bootstrap.py`:
   - Drop imports `pathlib`, `mpm_cli.core.bootstrap`,
     `mpm_cli.core.catalog`.
   - Keep argparse `<package>`, `--catalog-source`, `--output-dir`
     declarations so `--help` (T3 work) lists them.
   - Replace `_run`: when `args.package == "list"`, print the
     `'mpm bootstrap list' is deprecated.` WARN; otherwise print
     the `'mpm bootstrap <package>' is deprecated.` WARN
     (substituting the supplied package name). After printing,
     `raise SystemExit(EXIT_CODE_DEPRECATED)`.
   - Centralise the WARN-text construction in a private helper
     `_format_deprecated_warn(invocation: str, replacement_argv_tail:
     str) -> str` whose signature lets T2 supply a real translated
     tail without changing T1's call site (T1 passes the empty
     string).
5. **TDD GREEN (delete `core/bootstrap.py`)**: verify zero remaining
   consumers via `grep -rn "from mpm_cli.core.bootstrap"
   src tests` (only `commands/bootstrap.py:13` should match the
   pre-rewrite state; after the rewrite, zero matches). Then `git rm
   src/mpm_cli/core/bootstrap.py`. The existing
   `tests/unit/test_core_bootstrap.py` (if any) is replaced by the
   new `tests/unit/test_bootstrap_shim.py` from step 1 (T1 also
   deletes the old test file in the same commit -- see Changes
   Manifest).
6. **TDD REFACTOR**: confirm no dead imports remain in
   `commands/bootstrap.py`; confirm the new private helper is
   covered 100% by the new unit tests.
7. **Integration**: run the end-to-end test from step 2; confirm
   green.
8. **Docs**: add a single line to `docs/exit-codes.md` documenting
   exit 3 = deprecated invocation (mpm bootstrap). This task lands
   the file `docs/exit-codes.md` (NEW) with at minimum the four
   currently-defined codes (0, 1, 2, 3); a later task in the
   docs-update epic may expand it.
9. **Verify**: run the full suite locally:
   `uv run pytest tests/ -v && uv run ruff check src tests && uv run
   ruff format --check src tests && uv run mypy src && uv run bandit
   -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.9 (mpm bootstrap deprecation -- behaviour table rows 1+2, exit code 3 paragraph).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 0.3 (review-before-backlog item: bootstrap deprecation forces migration at the CI / script boundary).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 8 (`docs/exit-codes.md` NEW: 3 = deprecated invocation).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E1-F4-S1-T1 | Move `--catalog-source` flag definition to `core/cli_args.py` | in-queue |
| E5-F2-S1-T8 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 Invoking `mpm bootstrap mpm` (any non-`--help`, non-`list` positional) writes to stderr the verbatim text `WARN: 'mpm bootstrap mpm' is deprecated. Run instead:` followed by the `mpm add mpm` recommendation line and the `See docs/migration-bootstrap-to-add.md.` line; exits with status 3.
- [ ] AC-FUNC-002 Invoking `mpm bootstrap list` writes to stderr the verbatim text `WARN: 'mpm bootstrap list' is deprecated. Run instead:` followed by the `mpm list` recommendation line and the `See docs/migration-bootstrap-to-add.md.` line; exits with status 3.
- [ ] AC-FUNC-003 Neither invocation imports `mpm_cli.core.catalog` nor reads `MPM_CATALOG_SOURCE`; verified by monkeypatching `resolve_catalog_dir` to a sentinel-raising stub and asserting the sentinel never fires.
- [ ] AC-FUNC-004 The module `src/mpm_cli/core/bootstrap.py` is deleted; `grep -rn "from mpm_cli.core.bootstrap" src tests` returns zero matches after this task lands.
- [ ] AC-FUNC-005 No filesystem mutation occurs when the shim runs: passing `--output-dir <tmp_path>` and then asserting `list(tmp_path.iterdir()) == []` after the subprocess exit proves the shim did NOT delegate.
- [ ] AC-FUNC-006 `EXIT_CODE_DEPRECATED = 3` is defined as a module-level constant in `src/mpm_cli/constants.py` and is the integer value passed to `SystemExit` from the shim's CLI handler.
- [ ] AC-FUNC-007 The argparse parser still declares `<package>`, `--catalog-source`, and `--output-dir` so `--help` discoverability is preserved for T3 to extend; only the `_run` body changes in T1.
- [ ] AC-FUNC-008 The private helper `_format_deprecated_warn(invocation: str, replacement_argv_tail: str) -> str` exists and accepts a pre-translated argv-tail string; T1's call site passes the empty string, leaving T2 to wire the real translator.
- [ ] AC-TEST-001 New unit test file `tests/unit/test_bootstrap_shim.py` exists and contains parametrized cases covering: `mpm bootstrap list` WARN text, `mpm bootstrap <name>` WARN text, exit-code-3 assertion on both, monkeypatched `resolve_catalog_dir` not called, monkeypatched `core.bootstrap` import would fail (asserted via `import mpm_cli.core.bootstrap` raising `ImportError`).
- [ ] AC-TEST-002 New integration test file `tests/integration/test_bootstrap_shim.py` exists and invokes `python -m mpm_cli bootstrap ...` via `subprocess.run`; asserts stderr text verbatim, stdout empty, exit code 3, and `tmp_path` empty after the call.
- [ ] AC-TEST-003 The pre-existing `tests/unit/test_core_bootstrap.py` (and any pre-existing `tests/integration/test_bootstrap.py` or `tests/integration/test_commands_bootstrap.py` (ref) that exercise the old delegating behaviour) are DELETED in the same commit; `grep -rn "BootstrapOutputDirError\|bootstrap_package\|list_packages" tests` returns zero matches after this task lands.
- [ ] AC-CYCLE-001 End-to-end shim cycle: from a clean `tmp_path` working directory, run `mpm bootstrap mpm --output-dir <tmp_path>/scratch` via `subprocess.run`; assert stderr contains the verbatim WARN, stdout is empty, exit code is 3, and `<tmp_path>/scratch` does NOT exist (proves no filesystem mutation). Repeat for `mpm bootstrap list --catalog-source https://example.com/x.git@main` with a sentinel `--catalog-source` value that would raise on clone; assert the sentinel never fires (proves the shim never reached the catalog-resolve code path). Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/exit-codes.md` (NEW) is created in the same commit listing exit codes 0 (success), 1 (runtime / usage error), 2 (argparse usage error -- existing `cli.py:117`), and 3 (deprecated invocation -- this task). Linked from no other doc yet; later tasks in the docs epic add cross-references.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/commands/bootstrap.py` | Update -- rewrite `_run` as the deprecation shim: print WARN to stderr (verbatim text from spec Section 4.9 behaviour table) and `raise SystemExit(EXIT_CODE_DEPRECATED)`; drop imports of `pathlib`, `mpm_cli.core.bootstrap`, `mpm_cli.core.catalog`; add private helper `_format_deprecated_warn(invocation, replacement_argv_tail)` for T2 to extend. |
| `src/mpm_cli/core/bootstrap.py` | Delete -- legacy business module; no consumers remain after the shim rewrite. |
| `src/mpm_cli/constants.py` | Update -- add `# -- Exit codes --` section with `EXIT_CODE_DEPRECATED = 3`. |
| `tests/unit/test_bootstrap_shim.py` | New -- parametrized unit tests asserting WARN text, exit 3, and zero boundary calls (monkeypatched `resolve_catalog_dir` never fires; `import mpm_cli.core.bootstrap` raises `ImportError`). |
| `tests/integration/test_bootstrap_shim.py` | New -- subprocess-based end-to-end test: stderr text verbatim, stdout empty, exit 3, no filesystem mutation under `--output-dir <tmp_path>`. |
| `tests/unit/test_core_bootstrap.py` | Delete -- exercises the deleted `core/bootstrap.py`; replaced by the new shim unit tests. |
| `tests/integration/test_bootstrap.py` | Delete -- exercises the old delegating behaviour; replaced by the new shim integration test. |
| `docs/exit-codes.md` | New -- canonical exit-code table listing 0 (success), 1 (runtime / usage error), 2 (argparse usage error), 3 (deprecated invocation -- this task). |
| `tests/unit/test_constants.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/constants.py` per docs/source-test-atomicity.md. |
## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on `src/mpm_cli/commands/bootstrap.py` and `src/mpm_cli/constants.py` for new + modified lines.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] `grep -rn "from mpm_cli.core.bootstrap" src tests` returns zero matches.
- [ ] `grep -rn "BootstrapOutputDirError\|bootstrap_package\|list_packages" tests` returns zero matches.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E6-F1-S1-T1 manually blocked on E5-F2-S1-T8 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E5-F2-S1-T8
