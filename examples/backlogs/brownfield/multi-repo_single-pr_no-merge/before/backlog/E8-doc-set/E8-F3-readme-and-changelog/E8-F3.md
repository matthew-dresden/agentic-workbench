# E8-F3: README and CHANGELOG updates

## Status: in-queue

## Description

Top-level updates to the `matthew-dresden/mpm` repository root:

- `README.md` -- add a Quick start (mirrors spec Section 2.1 verbatim), a
  Subcommands overview table (every `mpm <subcommand>` linked to its
  doc), a Tab-completion paragraph, a Git-authentication paragraph, and
  a Migration-from-`mpm bootstrap` paragraph. The `mpm repo`
  subcommand (currently missing from the README) is added. The
  `mpm validate metadata` sub-subcommand (new) is added.
- `CHANGELOG.md` -- add Keep-a-Changelog format entries for every
  shipped feature (Added), the `mpm validate metadata` extension and
  the resolver widening (Changed), the `mpm bootstrap` deprecation
  (Deprecated), and the legacy `catalog/<name>/` + bundled
  `src/mpm_cli/catalog/` removals (Removed).

Each task in this feature has its own Story sub-directory entry under
`E8-F3-S1-top-level-updates/`.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 8 (the
`README.md` and `CHANGELOG.md` bullets) and Section 2.1 (the worked
example reproduced verbatim in the README Quick start).

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
