# E8-F3-S1: Top-level README + CHANGELOG updates

## Status: in-queue

## Description

Story scope: one task per top-level file.

Per-file task list:

- T1 `README.md` -- new Quick start (mirrors spec Section 2.1), new
  Subcommands overview table, new Tab-completion paragraph, new
  Git-authentication paragraph, new Migration-from-bootstrap paragraph.
- T2 `CHANGELOG.md` -- Keep-a-Changelog format entry set.

Both tasks depend on the upstream epics landing first so the subcommand
table is accurate (T1) and the entry set is complete (T2). Dependency
wiring is encoded at the epic level (E2-E7) so each task unblocks as
soon as the parent epic is done.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 8 (the
`README.md` and `CHANGELOG.md` bullets) and Section 2.1 (worked example).

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
