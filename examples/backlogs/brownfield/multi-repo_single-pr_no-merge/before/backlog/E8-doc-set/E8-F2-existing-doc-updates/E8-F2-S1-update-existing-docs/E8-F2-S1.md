# E8-F2-S1: Update the four existing docs under `docs/`

## Status: in-queue

## Description

Story scope: author one task per existing doc that needs an update per
spec Section 8. Three are surgical extensions; one is a full REWRITE.

Per-doc task list:

- T1 `docs/version-resolution.md` -- extend with Section 4.0 resolver
  precedence rules.
- T2 `docs/configuration.md` -- add every new env var grouped into seven
  subsections.
- T3 `docs/multi-source-guide.md` -- add a `mpm add` recommendation
  note.
- T4 `docs/creating-manifest-repos.md` -- REWRITE to remove the legacy
  `catalog/<name>/` model.

Each task is independent of the others (different file, different spec
bullet) so the autonomous loop runs them in parallel.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 8 (the
four update bullets).

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
