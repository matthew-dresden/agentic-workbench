# E8-F2: Updates to existing docs under `docs/`

## Status: in-queue

## Description

Targeted updates to four existing documents under `matthew-dresden/mpm/docs/`,
plus one full REWRITE. Spec source: Section 8 (the four update bullets).

Tasks in this feature:

- `docs/version-resolution.md` -- extend with Section 4.0 resolver
  precedence + `@` parsing rule + monorepo prefix support + widened
  bare-PEP-440 acceptance + non-PEP-440 tag note.
- `docs/configuration.md` -- add every new env var grouped into seven
  subsections (Catalog source, Resolver behavior, File paths, Lockfile,
  Concurrency, Completion cache, Retry policy).
- `docs/multi-source-guide.md` -- add a recommendation note that
  `mpm add` is the preferred way to produce `MPM_SOURCE_*` triples.
- `docs/creating-manifest-repos.md` -- **REWRITE** to remove every
  reference to the legacy `catalog/<name>/` directory model; add new
  Catalog entry contract, Tag publishing, Migrating away from
  `catalog/<name>/`, and Testing your manifest repo sections.

Each task in this feature has its own Story sub-directory entry under
`E8-F2-S1-update-existing-docs/` and is independently executable.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 8 (the
four existing-doc update bullets) and the per-doc spec sections each
task cites.

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
