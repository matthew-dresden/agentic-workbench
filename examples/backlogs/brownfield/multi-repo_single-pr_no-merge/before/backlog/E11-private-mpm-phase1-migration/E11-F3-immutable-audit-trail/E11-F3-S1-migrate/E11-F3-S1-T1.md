# E11-F3-S1-T1: Migrate the `immutable-audit-trail` entry's `<catalog-metadata>` and legacy README

## Status: in-queue

## Target Repository

- **Repo:** `example-org/private-mpm`
- **Branch:** `backlog/e11-f3-s1-t1`

## Description

Bring the `immutable-audit-trail` catalog entry into Section 3.5 soft-spot
1-5 compliance.

**Target XML (repo-relative):**
`repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml`

**Legacy README (repo-relative):** `catalog/immutable-audit-trail/README.md`

**Canonical entry-name:** `immutable-audit-trail` (XML filename and
catalog-directory name agree; no rename).

**Today's metadata state.** The `<catalog-metadata>` element has
`display-name="Immutable Audit Trail"`, `type="claude-marketplace"`,
`description="AI-powered SDLC plugin with immutable audit trail. Enforces
TDD through structural invariants with receipt-driven, immutable proof
artifacts."`, `owner-name="William Davis"`,
`owner-email="william.davis@example.com"`, `keywords="sdlc,tdd,audit,
immutable,receipts,ai,jira"`, `version="0.1.0"`. The REQUIRED `<name>`
element is MISSING and MUST be added with value `immutable-audit-trail`.

**Inherited `<project>` revision and PEP 440 status.** The single
`<project name="mpm-claude-marketplaces" remote="example-org">` pins
revision
`refs/tags/development/sdlc/general/pr-ready/immutable-audit-trail/>=0.3.0,<1.0.0`.
The remote `example-org` is inherited via `<include
name="repo-specs/git-connection/remote.xml" />`. The PEP 440
last-path-component is `>=0.3.0,<1.0.0`, which is a valid PEP 440 range
specifier. No PEP-440-non-compliance issue is required.

**README migration.** The full content of
`catalog/immutable-audit-trail/README.md` is migrated verbatim into a new
`<description>` CHILD element inside `<catalog-metadata>`, wrapped in a
`<![CDATA[...]]>` block. The existing `description="..."` attribute is
removed. The legacy `README.md` file itself is NOT deleted in this task
-- E12 handles the entire `catalog/` tree deletion.

**Acceptance command.**
`mpm catalog audit --check all repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml`
exits 0.

This is a Markdown-tier task: only `.xml` and `.md` files are touched.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/private-mpm`
- [ ] `mpm` CLI is installed and on `$PATH`
- [ ] All prerequisite tools are installed (`uv`, `git`)
- [ ] The branch `backlog/e11-f3-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E12 | private-mpm Phase 2 -- delete legacy `catalog/` tree | in-queue |

### Approach

1. **Read the legacy README:** Read
   `catalog/immutable-audit-trail/README.md` in full. Verify it contains
   no U+2014 em-dashes; if any are present, replace each with `--`
   before migration and note the substitution count in the TDD Cycle Log.
2. **Edit the XML:**
   - Add a `<name>immutable-audit-trail</name>` child element inside
     `<catalog-metadata>`.
   - Remove the `description="..."` attribute from `<catalog-metadata>`.
   - Add a `<description><![CDATA[<verbatim README content>]]></description>`
     child element inside `<catalog-metadata>`.
   - Preserve every other attribute (`display-name`, `type`, `owner-name`,
     `owner-email`, `keywords`, `version`) and the existing `<include>`
     and `<project>` elements byte-for-byte.
3. **Verify entry-name uniqueness (soft-spot 3):** Run
   `grep -RIn '<name>immutable-audit-trail</name>' repo-specs/`; assert
   exactly one match. Also run
   `mpm catalog audit --check entry-name-uniqueness .`; assert exit 0.
4. **Verify remote-url discoverability (soft-spot 4):** Run
   `mpm catalog audit --check remote-url repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml`;
   assert exit 0.
5. **Verify PEP 440 tag-format (soft-spot 5):** Run
   `mpm catalog audit --check tag-format repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml`;
   assert exit 0.
6. **Run the full audit (AC-CYCLE):** Run
   `mpm catalog audit --check all repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml`;
   assert exit 0; capture stdout + stderr in the TDD Cycle Log.
7. **Stage:** `git add` only the one XML file.

<!-- @@CODE-STANDARDS-START -->
### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.
<!-- @@CODE-STANDARDS-END -->

### Related Specifications

- **Spec:** Section 9b Phase 1 (per-entry `<catalog-metadata>` migration).
- **Spec:** Section 3.5 soft-spot rules 1-5.
- **Spec:** Section 4.8 (`mpm catalog audit` command).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E10 | private-mpm engineering + CI | in-queue |
| E5-F2 | `mpm catalog audit` catalog-author / consumer-side audit | in-queue |

## Acceptance Criteria

- [ ] AC-FUNC-001 The `<catalog-metadata>` element in `repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml` contains a `<name>immutable-audit-trail</name>` child element.
- [ ] AC-FUNC-002 The `<catalog-metadata>` element contains a `<description>` child element whose `<![CDATA[...]]>` payload is byte-for-byte identical to the content of `catalog/immutable-audit-trail/README.md (ref)` (ref) at the commit this branch is based on.
- [ ] AC-FUNC-003 The legacy `description="..."` attribute is removed from the `<catalog-metadata>` element.
- [ ] AC-FUNC-004 The `<catalog-metadata>` element preserves the existing `display-name`, `type`, `owner-name`, `owner-email`, `keywords`, and `version` attributes byte-for-byte.
- [ ] AC-FUNC-005 The `<include name="repo-specs/git-connection/remote.xml" />` element and the `<project name="mpm-claude-marketplaces" ...>` element (including its `revision` attribute `refs/tags/development/sdlc/general/pr-ready/immutable-audit-trail/>=0.3.0,<1.0.0`) are preserved byte-for-byte.
- [ ] AC-FUNC-006 No file outside the Changes Manifest is modified by this task (`git status --porcelain` lists exactly the one XML file).
- [ ] AC-FUNC-007 The legacy `catalog/immutable-audit-trail/README.md (ref)` (ref) file is NOT deleted, moved, or modified by this task.
- [ ] AC-TEST-001 `grep -c '<name>immutable-audit-trail</name>' repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml` returns `1`.
- [ ] AC-TEST-002 `grep -RIl '<name>immutable-audit-trail</name>' repo-specs/` returns exactly the one XML path above.
- [ ] AC-TEST-003 `mpm catalog audit --check metadata repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml` exits 0 with zero ERROR findings.
- [ ] AC-TEST-004 `mpm catalog audit --check source-name-derivation repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml` exits 0 with zero ERROR findings.
- [ ] AC-TEST-005 `mpm catalog audit --check entry-name-uniqueness .` (run from the repo root) exits 0 with zero ERROR findings.
- [ ] AC-TEST-006 `mpm catalog audit --check remote-url repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml` exits 0 with zero ERROR findings.
- [ ] AC-TEST-007 `mpm catalog audit --check tag-format repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml` exits 0 with zero WARN findings (the revision `refs/tags/.../>=0.3.0,<1.0.0` has a PEP 440 compliant last-path-component).
- [ ] AC-CYCLE-001 End-to-end: `mpm catalog audit --check all repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml` exits 0 (without `--strict`). Captured in `## TDD Cycle Log`.
- [ ] AC-DOC-001 No documentation updates required for this task (operator-facing-doc impact is in E10's scope).
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced in the XML edit.
- [ ] AC-SEC-002 No security bypass annotations introduced.
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-004 `mypy src` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml` | Update -- add `<name>immutable-audit-trail</name>` child; move `description` from attribute to `<description><![CDATA[...]]></description>` child carrying the verbatim content of `catalog/immutable-audit-trail/README.md`; preserve every other attribute and element byte-for-byte. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `mpm catalog audit --check all repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml` exits 0 (captured in `## TDD Cycle Log`).
- [ ] Only the file in the Changes Manifest is staged with `git add`.
- [ ] No file outside the Changes Manifest is modified.
- [ ] TDD Cycle Log captures: (a) the audit command and its exit code; (b) verification that the CDATA payload is byte-identical to the legacy README; (c) the `grep -RIl` output proving entry-name uniqueness.

## TDD Cycle Log

## Comments
