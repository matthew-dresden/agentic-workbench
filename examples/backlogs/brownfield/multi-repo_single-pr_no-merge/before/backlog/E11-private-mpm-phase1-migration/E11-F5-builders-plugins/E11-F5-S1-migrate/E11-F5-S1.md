# E11-F5-S1: Audit the `builders-plugins` entry's `<catalog-metadata>`

## Status: in-queue

## Description

Single-task story. Audit and update the `<catalog-metadata>` block in
`repo-specs/common/builders/builders-plugins/builders-plugins-marketplace.xml`.
No legacy `catalog/builders-plugins/` directory exists, so no README is
migrated; the work is to add the REQUIRED `<name>` element, replace the
em-dash in the existing `description` attribute with `--`, verify
entry-name uniqueness, verify remote-url discoverability, and document
the deliberate branch-pin (`main`) choice in the `<description>` per
soft-spot 5 reasoning. The single task `E11-F5-S1-T1` does the audit
end to end.

Spec source: spec Section 9b Phase 1; Section 3.5 soft-spot rules 1-5.

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `example-org/private-mpm`
