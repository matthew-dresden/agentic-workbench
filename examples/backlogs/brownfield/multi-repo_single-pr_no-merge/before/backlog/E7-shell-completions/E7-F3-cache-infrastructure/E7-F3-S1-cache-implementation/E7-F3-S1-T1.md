# E7-F3-S1-T1: Cache layout, file I/O, and 0700/0600 permissions

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e7-f3-s1-t1`

## Description

Implement the cache layout from Section 11.4 plus the file-I/O
helpers every subsequent task in F3 builds on.

Directory structure created by this task:

```
${MPM_CACHE_DIR}/
  catalogs/
    <sha256-of-catalog-url@ref>/
      index.txt          # one catalog entry name per line
      tags.txt           # one PEP 440-valid tag + one branch per line
      fetched_at.txt     # epoch seconds
      accessed_at.txt    # epoch seconds (updated on read, coalesced)
      origin.txt         # <url>@<ref> sidecar for __complete_cached_catalogs
  projects/
    <sha256-of-canonical-project-repo-url>/
      tags.txt
      fetched_at.txt
      accessed_at.txt
      origin.txt         # canonical <repo-url>
  completion-errors.log
```

API exposed by `src/mpm_cli/completions/cache.py`:

```python
def cache_dir() -> Path: ...
def catalog_entry_dir(catalog_url: str, ref: str) -> Path: ...
def project_entry_dir(repo_url: str) -> Path: ...
def read_entries(file_path: Path) -> list[str]: ...
def write_entries(file_path: Path, entries: Iterable[str]) -> None: ...
def read_epoch(file_path: Path) -> int | None: ...
def write_epoch(file_path: Path, epoch: int) -> None: ...
def log_completion_error(completer_name: str, exc: Exception) -> None: ...
```

Constraints:

- Every directory `mkdir`'d by `cache.py` is mode `0700`. Every
  file written is mode `0600`. The mode is enforced via `os.chmod`
  AFTER writing (the umask cannot be trusted).
- Directories and files are created lazily (`mkdir(parents=True, exist_ok=True)`).
- All paths derive from constants in `src/mpm_cli/constants.py`;
  no hard-coded path literals in `cache.py`.
- `read_entries` strips trailing newlines and skips blank lines.
  Files that do not exist return an empty list (a missing cache file
  is not an error; the caller treats it as "miss" and refetches).
- `write_entries` calls the sanitizer from E7-F3-S1-T4 BEFORE
  writing; T1 ships a stub sanitizer (`def sanitize(s): return s`)
  that T4 replaces with the real implementation.
- `log_completion_error` writes a single line per call:
  `<ISO-8601-UTC> <completer-name> <error-class>: <message>` to
  `${MPM_COMPLETION_LOG}` (default `${MPM_CACHE_DIR}/completion-errors.log`).
  Append-only; never rotates (operators run `mpm doctor --prune-cache`).

Constants added to `src/mpm_cli/constants.py`:

| Name | Default value |
|---|---|
| `MPM_CACHE_DIR_ENV` | `"MPM_CACHE_DIR"` |
| `MPM_CACHE_DIR_DEFAULT` | `"~/.cache/mpm"` (XDG fallback applied at resolve time) |
| `MPM_COMPLETION_CACHE_TTL` | `300` |
| `MPM_COMPLETION_TIMEOUT` | `2` |
| `MPM_COMPLETION_REFRESH_BG` | `1` |
| `MPM_COMPLETION_ENABLED` | `1` |
| `MPM_ACCESSED_AT_COALESCE_SEC` | `60` |
| `MPM_COMPLETION_LOG_ENV` | `"MPM_COMPLETION_LOG"` |

The resolver (`cache_dir()`) consults `${MPM_CACHE_DIR}` first,
then falls back to `${XDG_CACHE_HOME}/mpm`, then `~/.cache/mpm`
(Section 11.4 default).

Spec source: spec Section 11.4 (cache layout + cache lifecycle
header), Section 3.6 trust model ("Cache files are user-private").

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e7-f3-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E7-F2-S1-T1 | `__complete_catalog_entries` reads through the cache | in-queue |
| E7-F2-S1-T4 | `__complete_catalog_versions` reads tags.txt via cache_dir() | in-queue |
| E7-F2-S1-T5 | `__complete_project_versions` reads projects/<sha>/tags.txt | in-queue |
| E7-F2-S1-T6 | `__complete_cached_catalogs` walks catalogs/*/origin.txt | in-queue |
| E7-F3-S1-T2 | TTL math reads `fetched_at` | in-queue |
| E7-F3-S1-T3 | `accessed_at` coalescing updates `accessed_at` | in-queue |
| E7-F3-S1-T4 | Output sanitization wraps `write_entries` | in-queue |
| E7-F3-S1-T5 | Background refresh writes new `fetched_at` | in-queue |

### Approach

1. **TDD RED:** Author `tests/unit/test_cache_layout.py` with
   parametrized cases asserting: `cache_dir()` resolves to
   `${MPM_CACHE_DIR}` when set, else `${XDG_CACHE_HOME}/mpm`,
   else `~/.cache/mpm`; `catalog_entry_dir(url, ref)` returns
   a path under `cache_dir()/catalogs/<sha256-of-url@ref>/` with
   matching SHA computation; same for `project_entry_dir`;
   `read_entries`/`write_entries` round-trip; created directories
   are mode 0700 and created files are mode 0600 (verified via
   `os.stat().st_mode & 0o777`); `log_completion_error` writes a
   well-formed line; missing files return empty list (not an
   error); the SHA is deterministic across calls.
2. **TDD RED (integration):** Author
   `tests/integration/test_cache_layout.py` that sets
   `${MPM_CACHE_DIR}` to a tmp dir, invokes a real
   `mpm __complete_catalog_entries` (using a stub that writes to
   the cache), and asserts the on-disk file/dir modes match the
   contract.
3. **TDD GREEN:** Implement `src/mpm_cli/completions/cache.py`
   with the API above. Add the constants to
   `src/mpm_cli/constants.py`.
4. **TDD REFACTOR:** Factor any shared `os.chmod` boilerplate
   into a single `_chmod_secure(path: Path, mode: int)` helper.
5. **Docs:** Update `docs/shell-completion.md` with a "Cache
   layout" section. Add the env vars to `docs/configuration.md`.
6. **Verify:** Run the full suite locally:
   `uv run pytest tests/ -v && uv run ruff check src tests && uv run ruff format --check src tests && uv run mypy src && uv run bandit -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** spec Section 11.4 -- cache layout (directories, files, modes).
- **Migration spec:** spec Section 3.6 -- trust model ("Cache files are user-private").
- **Migration spec:** spec Section 11.6 -- configuration recap (env vars).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E6-F1-S1-T1 | (auto) | proposed |
| E7-F1-S1-T2 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 `cache_dir()` returns `Path("${MPM_CACHE_DIR}")` when the env var is set; falls back to `${XDG_CACHE_HOME}/mpm` when only that is set; falls back to `~/.cache/mpm` when neither is set.
- [ ] AC-FUNC-002 `catalog_entry_dir(url, ref)` returns `cache_dir() / "catalogs" / sha256(f"{url}@{ref}").hexdigest()` and is deterministic across calls.
- [ ] AC-FUNC-003 `project_entry_dir(repo_url)` returns `cache_dir() / "projects" / sha256(repo_url).hexdigest()`; the caller is expected to canonicalize `repo_url` BEFORE calling.
- [ ] AC-FUNC-004 Every directory created by `cache.py (ref)` (ref) has mode `0o700` (owner-rwx, no group, no other); enforced via `os.chmod` after `mkdir`. Verified by `os.stat().st_mode & 0o777 == 0o700`.
- [ ] AC-FUNC-005 Every file written by `cache.py (ref)` (ref) has mode `0o600`; enforced via `os.chmod` after write. Verified by `os.stat().st_mode & 0o777 == 0o600`.
- [ ] AC-FUNC-006 `read_entries(missing_file)` returns `[]` (not an error). `write_entries` creates parent directories as needed (`parents=True, exist_ok=True`).
- [ ] AC-FUNC-007 `log_completion_error(name, exc)` writes ONE line of the form `<ISO-8601-UTC> <name> <error-class-name>: <message>` to `${MPM_COMPLETION_LOG}` (resolved via the constant), append-only.
- [ ] AC-FUNC-008 Constants `MPM_CACHE_DIR_ENV`, `MPM_CACHE_DIR_DEFAULT`, `MPM_COMPLETION_CACHE_TTL`, `MPM_COMPLETION_TIMEOUT`, `MPM_COMPLETION_REFRESH_BG`, `MPM_COMPLETION_ENABLED`, `MPM_ACCESSED_AT_COALESCE_SEC`, `MPM_COMPLETION_LOG_ENV` are exported from `src/mpm_cli/constants.py` with the values specified in spec Section 11.6.
- [ ] AC-FUNC-009 The sidecar `origin.txt` is written alongside `index.txt` / `tags.txt` so E7-F2-S1-T6 can map `<sha>` back to `<url>@<ref>`.
- [ ] AC-TEST-001 `tests/unit/test_cache_layout.py` parametrized cases cover: env-var precedence for `cache_dir()`, SHA determinism, directory mode 0700, file mode 0600, round-trip read/write, missing-file -> empty list, `log_completion_error` line format.
- [ ] AC-TEST-002 `tests/integration/test_cache_layout.py` runs against a real `${MPM_CACHE_DIR}` (tmp dir) and asserts on-disk file/dir modes via `os.stat`.
- [ ] AC-CYCLE-001 End-to-end cycle: set `${MPM_CACHE_DIR}` to a tmp dir; call `write_entries(catalog_entry_dir("https://x.git", "main") / "index.txt", ["foo", "bar"])`; assert the file content equals `foo\nbar\n`, the file mode is `0o600`, and the parent directory mode is `0o700`. Then call `log_completion_error("__complete_test", ValueError("x"))`; assert the log file contains a line matching the expected format. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/shell-completion.md` updated with a "Cache layout" section describing the directory tree and file modes.
- [ ] AC-DOC-002 `docs/configuration.md` updated with rows for `MPM_CACHE_DIR`, `MPM_COMPLETION_CACHE_TTL`, `MPM_COMPLETION_TIMEOUT`, `MPM_COMPLETION_REFRESH_BG`, `MPM_ACCESSED_AT_COALESCE_SEC`, `MPM_COMPLETION_LOG`.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/completions/cache.py` | New -- cache layout helpers (`cache_dir`, `catalog_entry_dir`, `project_entry_dir`, `read_entries`, `write_entries`, `read_epoch`, `write_epoch`, `log_completion_error`) with 0700/0600 enforcement. |
| `src/mpm_cli/constants.py` | Update -- add `MPM_CACHE_DIR_ENV`, `MPM_CACHE_DIR_DEFAULT`, `MPM_COMPLETION_CACHE_TTL=300`, `MPM_COMPLETION_TIMEOUT=2`, `MPM_COMPLETION_REFRESH_BG=1`, `MPM_COMPLETION_ENABLED=1`, `MPM_ACCESSED_AT_COALESCE_SEC=60`, `MPM_COMPLETION_LOG_ENV`. |
| `tests/unit/test_cache_layout.py` | New -- parametrized cases for env-var precedence, SHA determinism, directory/file modes, round-trip, missing-file behavior, log format. |
| `tests/integration/test_cache_layout.py` | New -- subprocess-driven test asserting on-disk file/dir modes via `os.stat`. |
| `docs/shell-completion.md` | Update -- "Cache layout" section. |
| `docs/configuration.md` | Update -- env-var table rows for every cache env var. |
| `tests/unit/test_constants.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/constants.py` per docs/source-test-atomicity.md. |
## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on new lines.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] Affected documentation updated in the same commit.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E7-F3-S1-T1 manually blocked on E6-F1-S1-T1 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E6-F1-S1-T1

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E7-F3-S1-T1 manually blocked on E7-F1-S1-T2 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E7-F1-S1-T2

