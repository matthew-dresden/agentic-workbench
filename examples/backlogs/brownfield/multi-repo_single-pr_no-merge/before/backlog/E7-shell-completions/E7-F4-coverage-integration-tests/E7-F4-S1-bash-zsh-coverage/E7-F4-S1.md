# E7-F4-S1: bash + zsh coverage and snapshot tests

## Status: in-queue

## Description

Story scope: implement the integration + snapshot tests that close out
the "100% coverage" acceptance from spec Section 11.9. Three tasks:

- **T1** -- bash integration test (`tests/integration/test_completion_bash.py`)
  that sources the generated script and exercises `compgen -F` for
  every row of Section 11.2.
- **T2** -- zsh integration test (`tests/integration/test_completion_zsh.py`)
  with the same shape, using `_main_complete`.
- **T3** -- snapshot tests (`tests/functional/test_completion_snapshots.py`)
  on the `mpm completion bash` / `zsh` outputs; updates `Makefile`
  with `update-completion-snapshots`.

Each task depends on E7-F1-S1-T1, E7-F1-S1-T2, all seven E7-F2 tasks,
and all five E7-F3 tasks (the full feature surface must be in place
before the integration suite can exercise it).

Spec source: spec Section 11.8 (testing requirement) and 11.9
(acceptance for 100% coverage).

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
