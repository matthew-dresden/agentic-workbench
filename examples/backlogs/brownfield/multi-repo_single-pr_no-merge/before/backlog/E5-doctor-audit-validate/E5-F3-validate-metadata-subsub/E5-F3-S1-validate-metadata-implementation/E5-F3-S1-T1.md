# E5-F3-S1-T1: `mpm validate metadata` sub-subcommand (soft-spots 1+2+3 in-repo)

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e5-f3-s1-t1`

## Description

Add a new sub-subcommand `mpm validate metadata` to the existing
`mpm validate` command. Modifies `src/mpm_cli/commands/validate.py`
to register `metadata` alongside today's `xml` and `marketplace`
sub-subcommands. The new sub-subcommand runs the in-repo subset of
soft-spot checks from spec Section 3.5 (rules 1, 2, 3) without network
or clone:

- **Soft-spot 1 (REQUIRED + RECOMMENDED `<catalog-metadata>` fields).**
  Errors on missing REQUIRED fields, duplicate child elements within
  one `<catalog-metadata>` block, and multiple `<catalog-metadata>`
  blocks per XML file. Warns on missing RECOMMENDED fields. Same field
  lists used by `mpm catalog audit --check metadata` (E5-F2-S1-T2)
  via the shared constants `MPM_CATALOG_METADATA_REQUIRED_FIELDS`
  and `MPM_CATALOG_METADATA_RECOMMENDED_FIELDS`.
- **Soft-spot 2 (source-name derivation).** Warns when the normalised
  form differs from the entry name, AND warns when the entry name
  contains characters outside `[a-zA-Z0-9_-]`. Same rule used by
  `mpm catalog audit --check source-name-derivation` (E5-F2-S1-T3).
- **Soft-spot 3 (entry-name uniqueness).** Errors on collisions
  across XML files in the repo. Same rule used by `mpm catalog audit
  --check entry-name-uniqueness` (E5-F2-S1-T4).

Argparse surface:

- `mpm validate metadata [--repo-root <path>] [--format {text,json}]
  [--no-color]`
- `--repo-root` defaults to cwd; matches the existing `mpm validate
  xml` and `mpm validate marketplace` siblings.
- `--format` defaults to `text` (matches `marketplace`'s output style).
  `json` emits the same `{"findings": [...]}` schema as `mpm catalog
  audit`.

Exit code semantics (matches `mpm validate marketplace`):

- Exit 0 if no ERROR findings, regardless of warnings.
- Exit 1 if any ERROR finding.
- `--strict` is NOT introduced by this task. Catalog authors who want
  strict semantics use `mpm catalog audit --strict`.

What this sub-subcommand does NOT do:

- It does NOT check soft-spot 4 (`<remote>` resolvability) or soft-spot
  5 (PEP 440 tag-name compliance). Those require resolving include-chains
  through git or running `git ls-remote --tags`; they live in `mpm
  catalog audit`.
- It does NOT clone. No network access. Reads ONLY
  `repo-specs/**/*-marketplace.xml` inside `--repo-root`.

DRY: T2/T3/T4 of E5-F2 already implement the soft-spot check functions
(`_check_metadata`, `_check_source_name_derivation`,
`_check_entry_name_uniqueness`) inside `catalog.py`. This task imports
the same three functions from `catalog.py` and invokes them from
`validate.py::validate_metadata_command(args)`. If `catalog.py`'s
check functions accept a `target_path` they can be reused without
adaptation; otherwise expose them via a shared module
`src/mpm_cli/core/audit_checks.py` so both command surfaces consume
the same implementation.

Source-test atomicity: this task modifies `validate.py` (existing) and
adds new tests. AC-FINAL-014 applies to the lines this task adds.

Spec source: spec Section 3.5 (soft-spot rules) and the introductory
paragraph naming `mpm validate metadata`; Section 13 decision 10;
Section 1.1 (terminology).

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e5-f3-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|

### Approach

1. **TDD RED (unit):** Write `tests/unit/test_validate_metadata.py`
   with parametrized cases: valid manifest repo (zero findings, exit
   0); missing REQUIRED field (exit 1); missing RECOMMENDED field
   (exit 0 with warning); duplicate child (exit 1); multiple
   `<catalog-metadata>` blocks (exit 1); source-name drift (exit 0
   with warning); entry-name collision (exit 1). Each case uses a
   tmp_path manifest repo skeleton.
2. **TDD RED (integration):** Write
   `tests/integration/test_validate_metadata.py` driving the full
   `mpm validate metadata` CLI via `subprocess.run` against the
   existing E5-F2 fixtures (`broken-soft-spot-1/`, `broken-soft-spot-2/`,
   `broken-soft-spot-3/`); assert correct exit codes and findings.
   Verify the command does NOT issue any `git ls-remote` calls (no
   network).
3. **TDD GREEN:** Implement `validate_metadata_command(args)` in
   `src/mpm_cli/commands/validate.py`. Wire `metadata` into the
   existing `mpm validate` subparser. Import the three soft-spot
   check functions from E5-F2's `catalog.py` (or from
   `core/audit_checks.py` if the refactor was taken). Run them
   sequentially against `args.repo_root`, aggregate findings, format
   per `args.format`, return exit code.
4. **TDD REFACTOR:** If reuse of the check functions across `catalog.py`
   and `validate.py` creates an awkward coupling, extract them to
   `src/mpm_cli/core/audit_checks.py` and have both command files
   import. Update E5-F2's tasks' assumptions through this commit's
   amendments only if needed; otherwise just import.
5. **Integration:** End-to-end -- run `mpm validate metadata
   --repo-root tests/fixtures/catalog/broken-soft-spot-1/`; assert
   exit code 1 and stderr names the metadata errors. Run against
   `broken-soft-spot-2/`; assert exit 0 (warnings only).
6. **Docs:** Update `docs/cli/validate.md` (existing or new) adding
   the `metadata` sub-subcommand reference alongside `xml` and
   `marketplace`. Update `docs/catalog-author-guide.md` adding the
   "validate-before-push" workflow recommendation.
7. **Verify:** `uv run pytest tests/ -v && uv run ruff check src tests && uv run ruff format --check src tests && uv run mypy src && uv run bandit -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Spec:** Section 3.5 (soft-spot rules 1, 2, 3).
- **Spec:** Section 3.5 introductory paragraph naming `mpm validate metadata`.
- **Spec:** Section 13 decision 10.
- **Spec:** Section 1.1 (terminology -- entry name, source name, catalog metadata).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E2-F1-S1-T1 | `parse_catalog_metadata` helper | in-queue |
| E2-F3-S1-T1 | `derive_source_name` helper | in-queue |
| E5-F2-S1-T6 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 `mpm validate metadata --repo-root <valid-repo>` exits 0 with zero findings when the manifest repo is clean.
- [ ] AC-FUNC-002 `mpm validate metadata --repo-root <repo-with-missing-required-field>` exits 1 and stderr names the XML path + the missing REQUIRED field.
- [ ] AC-FUNC-003 `mpm validate metadata --repo-root <repo-with-missing-recommended-field>` exits 0 and stdout/stderr names the warning for the missing RECOMMENDED field.
- [ ] AC-FUNC-004 `mpm validate metadata --repo-root <repo-with-duplicate-child-element>` exits 1 and names the offending element.
- [ ] AC-FUNC-005 `mpm validate metadata --repo-root <repo-with-multiple-catalog-metadata-blocks>` exits 1 and names the XML path + block count.
- [ ] AC-FUNC-006 `mpm validate metadata --repo-root <repo-with-source-name-drift>` exits 0 with a WARN finding for the drift.
- [ ] AC-FUNC-007 `mpm validate metadata --repo-root <repo-with-entry-name-collision>` exits 1 with one finding listing all offending XML paths.
- [ ] AC-FUNC-008 `mpm validate metadata` does NOT issue any `git ls-remote` calls (no network access; verified by an env-controlled fake `git` binary in tests).
- [ ] AC-FUNC-009 `mpm validate metadata --format json` emits a parseable JSON object `{"findings": [...]}` matching the schema of `mpm catalog audit --format json`.
- [ ] AC-FUNC-010 The sub-subcommand reuses the same check functions as `mpm catalog audit --check metadata,source-name-derivation,entry-name-uniqueness` (imported from `catalog.py` (ref) or extracted to `core/audit_checks.py` (ref)).
- [ ] AC-TEST-001 Parametrized unit tests in `tests/unit/test_validate_metadata.py` cover every documented success and error path; tests verify the imported check functions are invoked the expected number of times.
- [ ] AC-TEST-002 Integration test in `tests/integration/test_validate_metadata.py` drives the full CLI against `broken-soft-spot-1/`, `broken-soft-spot-2/`, and `broken-soft-spot-3/` and against a clean valid fixture; asserts exit codes and finding content.
- [ ] AC-CYCLE-001 End-to-end cycle: run `mpm validate metadata --repo-root tests/fixtures/catalog/broken-soft-spot-1/`; assert exit 1 and the documented finding set. Run against `broken-soft-spot-2/`; assert exit 0 with warning findings. Run with a stubbed `git` binary that fails if invoked; assert command still succeeds (proves no network). Logged in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/cli/validate.md` updated with the `metadata` sub-subcommand reference alongside `xml` and `marketplace`. `docs/catalog-author-guide.md` updated with the "validate-before-push" workflow recommendation.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/commands/validate.py` | Update -- add `validate_metadata_command(args)` and register `metadata` as a sub-subcommand alongside today's `xml` and `marketplace`. Import the three soft-spot check functions from `commands/catalog.py` (or from `core/audit_checks.py` if the shared-module refactor is taken). |
| `tests/unit/test_validate_metadata.py` | New -- parametrized unit tests for every success and error path; verifies the imported check functions are invoked. |
| `tests/integration/test_validate_metadata.py` | New -- subprocess-driven end-to-end tests against `broken-soft-spot-1/`, `broken-soft-spot-2/`, `broken-soft-spot-3/`, and a clean valid fixture; verifies no `git ls-remote` calls. |
| `docs/cli/validate.md` | Update -- add the `metadata` sub-subcommand reference. |
| `docs/catalog-author-guide.md` | Update -- add the validate-before-push workflow recommendation. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on new lines of `src/mpm_cli/commands/validate.py`.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] `docs/cli/validate.md` and `docs/catalog-author-guide.md` updated in the same commit.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E5-F3-S1-T1 manually blocked on E5-F2-S1-T6 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E5-F2-S1-T6
