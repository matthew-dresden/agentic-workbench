# E5-F2-S1-T1: `mpm catalog audit` framework + --check parser

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e5-f2-s1-t1`

## Description

Create the new `mpm catalog` subcommand group and its first
sub-subcommand `mpm catalog audit`. This task lays the framework that
T2-T8 extend:

- New source file `src/mpm_cli/commands/catalog.py` containing the
  `mpm catalog audit` argparse wiring and the `audit_command(args)`
  entrypoint.
- Register `mpm catalog` and `mpm catalog audit` in
  `src/mpm_cli/cli.py` alongside the existing top-level subparser
  dispatch table.
- Argparse surface:
  - Default positional `<dir-or-source>` defaulting to `.` (cwd).
    Accepts: a directory path (must contain `repo-specs/`), OR a
    `<git_url>@<ref>` catalog source (split on the LAST `@` via the
    shared splitter from E1-F3-S1-T1).
  - `--check <subset>` accepting a comma-separated list of subset names
    OR the single value `all` (default). Valid subset names live in
    `MPM_CATALOG_AUDIT_VALID_CHECKS` in `constants.py`:
    `metadata`, `source-name-derivation`, `entry-name-uniqueness`,
    `remote-url`, `tag-format`. Empty list, unknown subset name, or
    `all` mixed with any other subset (e.g., `--check all,metadata`)
    is a hard error at argparse time with an ERROR-shape message.
  - `--format {text,json}` -- output format. Default `text`. Env
    `MPM_CATALOG_AUDIT_FORMAT`.
  - `--no-color` -- existing convention.
  - `--strict` -- placeholder boolean parsed here; promoting warnings
    to errors is implemented in T8 (T1 just parses the flag).
- `audit_command(args)` returns an exit code. T1's behaviour:
  - Resolve the audit target into a local directory path (either the
    supplied directory or a freshly-cloned cache copy of the supplied
    `<url>@<ref>` source, using the same cache layout as `mpm list` /
    `mpm add`).
  - Resolve the `--check` value into a normalized frozenset of subset
    names.
  - For T1, the actual check dispatch is a stub: T1 wires the framework
    AND adds the legacy-directory check call site (so T7 can drop in
    only the implementation). T2-T6 register their individual check
    functions into a check-name -> callable mapping.
  - Iterate every selected check, collect findings, print them in the
    requested format, return exit code 0 (T1; T2+ will return 1 when
    errors exist, T8 when `--strict` and any warnings exist).

The cloned-target cache layout: `${MPM_CACHE_DIR}/catalog-audit/<sha256
of canonicalized url@ref>/`. Cached clones are reused if present and
fresh (mtime within `MPM_CATALOG_AUDIT_CACHE_TTL_SECONDS`, default
3600; added to `constants.py`).

Output format `text`: one finding per line, prefixed with `ERROR:`,
`WARN:`, or `INFO:`. Output format `json`: a single JSON object
`{"findings": [{...}, ...]}` written to stdout.

Source-test atomicity: this task creates `catalog.py` and adds the
matching `tests/unit/test_catalog_audit_framework.py` and
`tests/unit/test_catalog_audit_check_parser.py` so AC-FINAL-014 is
satisfied at task close.

Spec source: spec Section 4.8 (`mpm catalog audit`) -- the framework
half (default arg, `<dir-or-source>` resolution, `--check` parsing,
`--strict` declaration, exit code 0).

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e5-f2-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E5-F2-S1-T2 | `--check metadata` (soft-spot 1) | in-queue |
| E5-F2-S1-T3 | `--check source-name-derivation` (soft-spot 2) | in-queue |
| E5-F2-S1-T4 | `--check entry-name-uniqueness` (soft-spot 3) | in-queue |
| E5-F2-S1-T5 | `--check remote-url` (soft-spot 4) | in-queue |
| E5-F2-S1-T6 | `--check tag-format` (soft-spot 5; PEP 440) | in-queue |
| E5-F2-S1-T7 | Legacy `catalog/<name>/` directory detection | in-queue |
| E5-F2-S1-T8 | `--strict` flag (promote warnings to errors) | in-queue |

### Approach

1. **TDD RED (parser):** Write
   `tests/unit/test_catalog_audit_check_parser.py` with parametrized
   cases: `all` (default), `metadata`, `metadata,tag-format`, `all` with
   another value (hard error), empty list (hard error), unknown subset
   name (hard error). Assert each maps to the expected normalized
   frozenset OR raises the documented argparse error.
2. **TDD RED (framework):** Write
   `tests/unit/test_catalog_audit_framework.py` with cases covering: the
   subcommand registers in `cli.py` and is discoverable in `mpm
   --help`; `audit_command` invoked with `--check all` against an empty
   manifest-repo returns exit 0 and emits no findings; output format
   `json` produces a parseable JSON object with a `findings` key.
3. **TDD RED (integration):** Write
   `tests/integration/test_catalog_audit_framework.py` driving the full
   CLI via `subprocess.run` against a tmp_path manifest-repo skeleton;
   assert exit 0 for an empty repo; assert exit code 2 (argparse usage
   error) for an invalid `--check` value.
4. **TDD GREEN:** Implement `src/mpm_cli/commands/catalog.py` with:
   - The `audit_command(args)` entrypoint.
   - A check-name -> callable registry (initially empty; T2-T7 populate
     it).
   - The `_resolve_audit_target` helper that returns a local directory
     path either from a directory arg or by cloning a `<url>@<ref>`
     source into the catalog-audit cache.
   - The `_parse_check_subset(value)` helper that produces the normalized
     frozenset.
   - The output formatter `_format_findings(findings, fmt)`.
   - The `AuditFinding` dataclass shape (mirrors `DoctorFinding` for
     consistency; kinds: "info","warn","error"; code; message; remediation).
   - Register the subcommand in `src/mpm_cli/cli.py`.
   - Add constants `MPM_CATALOG_AUDIT_VALID_CHECKS`,
     `MPM_CATALOG_AUDIT_CACHE_TTL_SECONDS`, and the cache-dir template
     to `src/mpm_cli/constants.py`.
5. **TDD REFACTOR:** If `_resolve_audit_target` overlaps with the
   manifest-repo clone logic used by `mpm list` / `mpm add` (E2
   territory), extract a shared helper in `core/catalog_clone.py`. T1
   adds the helper only if E2 has not already done so; otherwise reuse.
6. **Integration:** End-to-end -- run `mpm catalog audit .` against
   a tmp_path empty manifest repo; assert exit 0; run with `--check
   nonsense`; assert exit 2.
7. **Docs:** Create `docs/cli/catalog-audit.md` (NEW) documenting the
   subcommand surface, the five valid checks, the cache layout, and
   the JSON output schema.
8. **Verify:** `uv run pytest tests/ -v && uv run ruff check src tests && uv run ruff format --check src tests && uv run mypy src && uv run bandit -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Spec:** Section 4.8 (`mpm catalog audit`).
- **Spec:** Section 3.5 (the five soft-spot rules T2-T6 implement).
- **Spec:** Section 11 / Section 3.6 (cache layout and user-private mode).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E2-F1-S1-T1 | `parse_catalog_metadata` helper | in-queue |
| E1-F4-S1-T1 | Shared `--catalog-source` CLI arg in `core/cli_args.py` | in-queue |
| E5-F1-S1-T1 | (auto) | proposed |
| E5-F1-S1-T5 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 `mpm catalog audit --help` lists the supported `--check` values: `metadata`, `source-name-derivation`, `entry-name-uniqueness`, `remote-url`, `tag-format`, plus `all`.
- [ ] AC-FUNC-002 `mpm catalog audit --check metadata,tag-format .` parses the value into the normalized frozenset `{"metadata", "tag-format"}` and runs only those checks.
- [ ] AC-FUNC-003 `mpm catalog audit --check all .` runs every registered check.
- [ ] AC-FUNC-004 `mpm catalog audit --check '' .`, `mpm catalog audit --check unknown .`, and `mpm catalog audit --check all,metadata .` each exit with argparse usage error (exit code 2) and an ERROR-shape stderr message.
- [ ] AC-FUNC-005 `mpm catalog audit` invoked with no positional defaults to `.`; passes the resolved local path to the dispatch loop.
- [ ] AC-FUNC-006 `mpm catalog audit <git_url>@<ref>` clones into `${MPM_CACHE_DIR}/catalog-audit/<sha256>/`; subsequent invocations within `MPM_CATALOG_AUDIT_CACHE_TTL_SECONDS` reuse the cached clone (verified by mtime check).
- [ ] AC-FUNC-007 Output format `text` (default): one finding per line with `ERROR:` / `WARN:` / `INFO:` prefix. Output format `json`: a single JSON object `{"findings": [...]}` parseable by `json.loads`.
- [ ] AC-FUNC-008 `--strict` flag is parsed and stored on the args namespace; T1 stores the value but does NOT yet promote warnings (T8's responsibility).
- [ ] AC-FUNC-009 The check registry is a dict mapping check-name to a callable accepting `(target_path) -> list[AuditFinding]`; T2-T7 register their check functions into it.
- [ ] AC-FUNC-010 The valid check names AND cache TTL constants live in `src/mpm_cli/constants.py`; no inline literals in `catalog.py` (ref).
- [ ] AC-TEST-001 Parametrized unit tests in `tests/unit/test_catalog_audit_check_parser.py` cover every valid and invalid `--check` value and assert the parser produces the expected frozenset OR the expected argparse error.
- [ ] AC-TEST-002 Unit tests in `tests/unit/test_catalog_audit_framework.py` cover `audit_command` exit code, registry-iteration behaviour, output formatting in both `text` and `json` formats.
- [ ] AC-TEST-003 Integration test in `tests/integration/test_catalog_audit_framework.py` drives the full CLI against a `tmp_path` empty manifest-repo; asserts exit 0; asserts argparse usage error for invalid `--check`.
- [ ] AC-CYCLE-001 End-to-end cycle: build a `tmp_path` empty manifest-repo with `repo-specs/` but zero XML files; run `mpm catalog audit . --format json`; assert exit 0 and parseable JSON with `findings == []`. Logged in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/cli/catalog-audit.md` (NEW) documents the subcommand surface, the five valid checks, cache layout, JSON output schema, and the canonical error messages.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/commands/catalog.py` | New -- `mpm catalog audit` subcommand framework with `AuditFinding` dataclass, `audit_command(args)` entrypoint, check-name->callable registry, `_resolve_audit_target`, `_parse_check_subset`, `_format_findings`. |
| `src/mpm_cli/cli.py` | Update -- register the new `catalog` subparser group and its `audit` sub-subparser. |
| `src/mpm_cli/constants.py` | Update -- add `MPM_CATALOG_AUDIT_VALID_CHECKS`, `MPM_CATALOG_AUDIT_CACHE_TTL_SECONDS`, and the cache-dir template. |
| `tests/unit/test_catalog_audit_framework.py` | New -- unit tests for `audit_command` exit codes, registry iteration, and output formatters. |
| `tests/unit/test_catalog_audit_check_parser.py` | New -- parametrized unit tests for every valid + invalid `--check` value. |
| `tests/integration/test_catalog_audit_framework.py` | New -- subprocess-driven end-to-end tests against a tmp_path empty manifest-repo. |
| `docs/cli/catalog-audit.md` | New -- subcommand reference documenting the framework + the five valid checks. |
| `tests/unit/test_cli.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/cli.py` per docs/source-test-atomicity.md. |
| `tests/unit/test_constants.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/constants.py` per docs/source-test-atomicity.md. |
## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on `src/mpm_cli/commands/catalog.py` and the new constants.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] `docs/cli/catalog-audit.md` created in the same commit.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E5-F2-S1-T1 manually blocked on E5-F1-S1-T1 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E5-F1-S1-T1

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E5-F2-S1-T1 manually blocked on E5-F1-S1-T5 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E5-F1-S1-T5
