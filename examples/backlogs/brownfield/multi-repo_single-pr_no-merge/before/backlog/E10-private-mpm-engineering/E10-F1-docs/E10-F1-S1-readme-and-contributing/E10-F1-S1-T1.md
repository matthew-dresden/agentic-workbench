# E10-F1-S1-T1: Update README.md + create/update docs/contributing.md

## Status: in-queue

## Target Repository

- **Repo:** `example-org/private-mpm`
- **Branch:** `backlog/e10-f1-s1-t1`

## Description

Update `README.md` and create-or-update `docs/contributing.md` in the
`example-org/private-mpm` repo to codify the catalog-entry contract
from spec Section 3.5 (soft-spot 1-5 rules) and document the entry-author
workflow per spec Section 9a.

This is a Markdown-tier task: no Python source is authored; the Python
tooling AC-FINAL entries carry the canonical N/A suffix per
`docs/acceptance-criteria-canonical.md`.

### `README.md` update

Replace any existing passage that points authors at `catalog/<name>/` (or
treats that directory as the entry-author surface) with a "Catalog entry
contract" section that states, verbatim, the soft-spot 1-5 rules:

1. **`<catalog-metadata>` required fields.** Every `*-marketplace.xml`
   MUST include a `<catalog-metadata>` block containing the REQUIRED
   children `name`, `display-name`, `description`, and `version`.
   RECOMMENDED children are `type`, `owner-name`, `owner-email`,
   `keywords`. Duplicate children within the same `<catalog-metadata>`
   block are an error. Exactly one `<catalog-metadata>` block per XML
   file is permitted; multiple blocks are an error.
2. **Source-name derivation.** The source name used in
   `MPM_SOURCE_<source-name>_*` triples is derived from
   `<catalog-metadata><name>` by deterministic normalization: lowercase
   the input AND replace `-` with `_`. Catalog authors choose the entry
   name; the source name follows mechanically.
3. **Entry-name uniqueness.** `<catalog-metadata><name>` MUST be unique
   across the entire manifest repo. Collisions are an error reported by
   `mpm validate metadata`.
4. **`<remote>` discoverability.** Every `<project remote="X">` MUST
   resolve to a concrete fetch URL via the `<include>` chain rooted at
   the entry's `*-marketplace.xml`. HTTPS or SSH transports only;
   `file://` is rejected by `mpm catalog audit` unless
   `MPM_ALLOW_INSECURE_REMOTES=1` is explicitly set.
5. **PEP 440 tag-name compliance.** Tags published in target repos
   referenced by `<project>` SHOULD have a last-path-component that is
   a valid PEP 440 version (e.g., `1.0.0`, `2024.4.1`, `1.0.0a1`).
   Non-PEP 440 tags (e.g., `v1.0.0`, `release-2024`) are unaddressable
   by `mpm`'s resolver and the CI workflow warns about them.

Add a forward reference: "See `docs/contributing.md` for the full
authoring procedure."

The README must contain ZERO references to `catalog/<name>/` as the
entry-author surface. Any historical text describing
`catalog/<name>/README.md`, `catalog/<name>/mpm-readme.md`, or
`catalog/<name>/.mpm` as author-facing surfaces is removed in this
task.

### `docs/contributing.md` create-or-update

Create the file if it does not exist; otherwise replace its
catalog-author section with the canonical authoring procedure:

1. **Choose an entry name.** Pick a unique `<catalog-metadata><name>`.
   Run `mpm validate metadata .` to confirm the chosen name does not
   collide.
2. **Create the `*-marketplace.xml`.** Add one new file under
   `repo-specs/` with a complete `<catalog-metadata>` block (all
   REQUIRED fields populated; RECOMMENDED fields populated when
   available).
3. **Resolve `<remote>` references.** Verify every `<project
   remote="X">` resolves via the entry's `<include>` chain. Run `mpm
   validate marketplace .` locally before opening a PR.
4. **Publish PEP 440 tags in target repos.** When a target repo
   referenced by `<project>` cuts a release, publish the tag with a
   PEP 440-compliant last-path-component. Operators on consuming repos
   resolve via that tag.
5. **DO NOT create a `catalog/<name>/` directory.** The legacy
   `catalog/` tree is removed by E12 Phase 2; new entries MUST NOT
   reintroduce it. The CI guard (workflow `no-catalog-dir.yml`) fails
   the build on any new path matching `^catalog/[^/]+/`.
6. **Run the local checks.** Before opening the PR run, in order:
   - `mpm validate xml .`
   - `mpm validate marketplace .`
   - `mpm validate metadata .`
   - `mpm catalog audit .`
   Each MUST exit zero. The CI workflow re-runs the same commands.

Add a "Required PR checks" section that lists the workflows the PR will
encounter (audit, no-catalog-dir, pep440-tags) and what each enforces.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/private-mpm`
- [ ] The branch `backlog/e10-f1-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E10-F2-S1-T1 | CI workflow: mpm catalog audit + validate xml/marketplace/metadata + smoke mpm list | in-queue |
| E10-F2-S1-T2 | CI guard against new catalog/<name>/ directories | in-queue |
| E10-F2-S1-T3 | CI guard for PEP 440 tag-name compliance | in-queue |

### Approach

1. **Survey existing docs.** `git grep -nE 'catalog/[^/]+/' README.md docs/`
   to enumerate every reference to the legacy per-entry directory in
   author-facing text. Capture each hit so the rewrite leaves zero
   surviving references.
2. **Draft the contract block.** Author the soft-spot 1-5 summary
   block for `README.md` directly from spec Section 3.5 (the canonical
   source). Cross-reference the spec section in a footnote so future
   doc audits can trace back.
3. **Draft the authoring procedure.** Author the
   `docs/contributing.md` section using the six steps above. Verify
   step 6's command list matches the order the CI workflow runs them
   in (E10-F2-S1-T1).
4. **Rewrite the README.** Replace every surveyed legacy-directory
   reference with the new contract block + forward reference. Verify
   `git grep -nE 'catalog/[^/]+/' README.md` returns ZERO results
   after the edit (the only remaining references in the repo should
   live in spec / changelog / migration history docs).
5. **Verify on disk.** Render `README.md` and `docs/contributing.md`
   with a Markdown previewer and confirm: section headers nest
   sensibly; soft-spot 1-5 rules each carry an anchor; the
   contributing procedure's command list survives copy-paste without
   line-wrap corruption.
6. **Stage selectively.** `git add README.md docs/contributing.md`
   only.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Spec:** Section 9a (Docs subsection) -- the canonical scope for this task.
- **Spec:** Section 3.5 -- the five soft-spot rules the docs codify.
- **Spec:** Section 3.6 -- HTTPS-by-default policy referenced in rule 4.

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E5-F2-S1-T1 | `mpm catalog audit` framework + --check parser | in-queue |
| E5-F2-S1-T2 | `--check metadata` (soft-spot 1) | in-queue |
| E5-F2-S1-T3 | `--check source-name-derivation` (soft-spot 2) | in-queue |
| E5-F2-S1-T4 | `--check entry-name-uniqueness` (soft-spot 3) | in-queue |
| E5-F2-S1-T5 | `--check remote-url` (soft-spot 4) | in-queue |
| E5-F2-S1-T6 | `--check tag-format` (soft-spot 5; PEP 440) | in-queue |
| E5-F3-S1-T1 | `mpm validate metadata` sub-subcommand | in-queue |

## Acceptance Criteria

- [ ] AC-FUNC-001 `README.md` contains a "Catalog entry contract" (or equivalently-named) section that lists the soft-spot 1-5 rules verbatim per spec Section 3.5: REQUIRED `<catalog-metadata>` children (`name`, `display-name`, `description`, `version`); source-name derivation rule (lowercase + dash-to-underscore); entry-name uniqueness; `<remote>` resolvability via `<include>` chain; PEP 440 tag-name guidance.
- [ ] AC-FUNC-002 `README.md` contains a forward reference to `docs/contributing.md` for the full authoring procedure.
- [ ] AC-FUNC-003 `README.md` contains ZERO references to `catalog/<name>/` (or any equivalent legacy phrasing such as `catalog/<entry>/README.md` (ref), `catalog/<entry>/.mpm` (ref)) as the entry-author surface. The verification command is `git grep -nE 'catalog/[^/]+/' README.md`; the post-edit grep returns no hits.
- [ ] AC-FUNC-004 `docs/contributing.md` exists and contains the six-step authoring procedure: choose unique entry name; create one `*-marketplace.xml` under `repo-specs/` with complete `<catalog-metadata>`; verify `<remote>` resolvability; publish PEP 440 tags in target repos; do NOT create a `catalog/<name>/` directory; run the local `mpm validate xml` / `mpm validate marketplace` / `mpm validate metadata` / `mpm catalog audit` commands before opening a PR.
- [ ] AC-FUNC-005 `docs/contributing.md` contains a "Required PR checks" section that names each of the three workflows the PR will encounter (audit, no-catalog-dir, pep440-tags) and what each enforces.
- [ ] AC-FUNC-006 The PEP 440 tag-publishing convention from spec Section 9a is documented in both `README.md` (under the contract block) and `docs/contributing.md` (under step 4 of the authoring procedure).
- [ ] AC-FUNC-007 The HTTPS-by-default policy from spec Section 3.6 is referenced in the `<remote>` rule (rule 4) so authors understand `file://` is rejected without `MPM_ALLOW_INSECURE_REMOTES=1`.
- [ ] AC-FUNC-008 Every command shown in `docs/contributing.md` step 6 matches, in order, the commands the CI workflow `audit.yml` (ref) (authored by E10-F2-S1-T1) runs; documentation and CI agree on the canonical author-side check sequence.
- [ ] AC-CYCLE-001 End-to-end documentation review: a fresh reader who has never authored a catalog entry can (a) read `README.md`'s contract block, (b) read `docs/contributing.md`'s six-step procedure, (c) follow the steps against a tmp clone of the repo, and (d) produce a new `*-marketplace.xml` that `mpm catalog audit .` accepts. Evidence captured in `## TDD Cycle Log`: paste the produced XML, the audit command output (exit 0), and confirmation that no `catalog/<name>/` directory was created.
- [ ] AC-DOC-001 All affected documentation updated in the same commit. The Changes Manifest matches exactly the files changed by git.
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-004 `mypy src` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `private-mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `README.md` | Update -- add "Catalog entry contract" section listing soft-spot 1-5 rules; add forward reference to `docs/contributing.md`; remove every reference to `catalog/<name>/` as the entry-author surface. |
| `docs/contributing.md` | New or Update -- six-step authoring procedure (unique entry name; new `*-marketplace.xml` with complete `<catalog-metadata>`; `<remote>` resolvability; PEP 440 tag-publishing convention; do not create `catalog/<name>/`; run local validate + audit before PR); "Required PR checks" section. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `git grep -nE 'catalog/[^/]+/' README.md` returns zero hits.
- [ ] `docs/contributing.md` exists with the six-step procedure and "Required PR checks" section.
- [ ] Both files staged with explicit `git add README.md docs/contributing.md` (no `git add -A`).
- [ ] No em-dash characters introduced anywhere.

## TDD Cycle Log

## Comments
