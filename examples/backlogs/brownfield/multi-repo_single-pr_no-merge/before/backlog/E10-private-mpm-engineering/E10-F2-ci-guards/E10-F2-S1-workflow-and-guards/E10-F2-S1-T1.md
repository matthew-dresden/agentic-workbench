# E10-F2-S1-T1: CI workflow audit.yml -- catalog audit + validate + smoke list

## Status: in-queue

## Target Repository

- **Repo:** `example-org/private-mpm`
- **Branch:** `backlog/e10-f2-s1-t1`

## Description

Create the GitHub Actions workflow `.github/workflows/audit.yml` in the
`example-org/private-mpm` repo. The workflow runs on every PR
(`pull_request`) and every push to `main` (`push: branches: [main]`),
and performs three jobs that collectively cover spec Section 9a CI
bullets 1-3:

### Job A -- `mpm catalog audit`

Run `mpm catalog audit .` against the checked-out repo root. Initially
WITHOUT `--strict` because the deprecation window for legacy
`catalog/<name>/` directory warnings is still open (until E12 Phase 2
completes the migration described in spec Section 9b). After E12 Phase
2 lands, this workflow will be updated to add `--strict` (the
follow-up is tracked in spec Section 9a bullet 1 and called out by the
"future hardening" comment in the workflow file).

Exit code 0 required to pass. Any error finding from `mpm catalog
audit` fails the job.

### Job B -- `mpm validate xml` + `mpm validate marketplace` + `mpm validate metadata`

Run each of the three validators in sequence:

1. `mpm validate xml .`
2. `mpm validate marketplace .`
3. `mpm validate metadata .`

Each exits zero on success. The job runs all three even if an earlier
one fails (so authors see every distinct violation in one PR run rather
than a series of "fix one at a time" cycles).

### Job C -- `mpm list` smoke test against the repo at HEAD

Steps:

1. Install the latest `mpm` CLI (`uv tool install mpm` or the
   equivalent pinned-version install the repo already documents; the
   exact installer is the operator's choice but MUST install from the
   official package -- no source checkout).
2. Resolve the catalog-source for the smoke run. Use the workflow's
   own checkout: `mpm list --catalog-source
   file://${{ github.workspace }}@${{ github.sha }}` so the CLI sees
   the repo at exactly the SHA being tested. The workflow exports
   `MPM_ALLOW_INSECURE_REMOTES=1` only for this job because the
   `file://` source is otherwise rejected by `mpm catalog audit`'s
   HTTPS-by-default policy (spec Section 3.6).
3. Capture the entry-name set returned by `mpm list --format json`
   (parse `[].name`).
4. Capture the entry-name set implied by the repo's XML files: every
   `<catalog-metadata><name>` value in every file matched by `git
   ls-files repo-specs/**/*-marketplace.xml`. The job uses `mpm
   validate metadata` output (JSON) OR an inline `python -c "import
   xml.etree.ElementTree as ET; ..."` snippet to extract the names.
5. Assert the two sets are equal: no `mpm list`-returned entry is
   missing from the XML set (no silent skips); no XML-implied entry
   is missing from the CLI output (no orphans).

If the sets disagree, the job fails with a diagnostic listing every
asymmetric entry (CLI-only or XML-only).

### Workflow shape

- `name: audit`
- `on:`
  - `pull_request: {}`
  - `push: { branches: [main] }`
- `jobs:` three jobs running in parallel (`audit`, `validate`,
  `smoke-list`). Each job:
  - Uses `actions/checkout@<pinned-version>` with `fetch-depth: 0` so
    the smoke job can resolve the SHA.
  - Runs on `ubuntu-latest`.
  - Sets `shell: bash` on every `run:` step (per CLAUDE.md GitHub
    Workflows standards).
  - Uses `set -euo pipefail` at the top of every multi-line `run:`
    block.
- Pinned action versions (no floating tags). The exact pin is the
  operator's choice; the contract is that the workflow file MUST NOT
  reference `@main`, `@master`, or `@v*` floating tags for
  third-party actions.
- All timeouts (job-level and command-level) MUST be configurable via
  workflow `env:` block, not hard-coded in `run:` steps. The mpm
  CLI invocations inherit `MPM_GIT_RETRY_COUNT` and
  `MPM_GIT_RETRY_DELAY` from the workflow `env:` defaults so
  retries follow the same envelope as local runs.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/private-mpm`
- [ ] The branch `backlog/e10-f2-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|

### Approach

1. **Survey existing workflows.** `ls .github/workflows/` to confirm
   `audit.yml` does not already exist. If a previous CI workflow
   covers a subset of the contract, capture its name so the new
   workflow's commit message documents the relationship.
2. **Author the workflow file.** Start with the canonical `name`,
   `on`, `env`, `jobs` skeleton; add the three jobs in order. Use
   pinned action versions; use `shell: bash` and `set -euo pipefail`.
3. **Validate the workflow syntax locally.** Run `actionlint` (or
   GitHub's `act --list`) against the new file to catch typos before
   pushing.
4. **Verify against a fixture repo.** Push the workflow to a fixture
   PR (or use `act -j audit` locally) against a tmp `private-mpm`
   checkout to confirm each job runs and returns the expected exit
   code. Capture the green-run evidence in `## TDD Cycle Log`.
5. **Verify the smoke comparison fails when expected.** Introduce a
   deliberate `*-marketplace.xml` with a `<catalog-metadata><name>`
   that disagrees with what `mpm list` returns (e.g., a typo);
   confirm Job C fails with the diagnostic listing the asymmetric
   entry. Revert the deliberate breakage; confirm Job C passes again.
6. **Stage selectively.** `git add .github/workflows/audit.yml` only.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Spec:** Section 9a (CI subsection, bullets 1-3) -- the canonical scope for this task.
- **Spec:** Section 3.5 -- the soft-spot rules that `mpm catalog audit` enforces.
- **Spec:** Section 3.6 -- the HTTPS-by-default policy explaining why the smoke job exports `MPM_ALLOW_INSECURE_REMOTES=1` for the local `file://` source.

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E5-F2-S1-T1 | `mpm catalog audit` framework + --check parser | in-queue |
| E5-F2-S1-T2 | `--check metadata` (soft-spot 1) | in-queue |
| E5-F2-S1-T3 | `--check source-name-derivation` (soft-spot 2) | in-queue |
| E5-F2-S1-T4 | `--check entry-name-uniqueness` (soft-spot 3) | in-queue |
| E5-F2-S1-T5 | `--check remote-url` (soft-spot 4) | in-queue |
| E5-F2-S1-T6 | `--check tag-format` (soft-spot 5; PEP 440) | in-queue |
| E5-F3-S1-T1 | `mpm validate metadata` sub-subcommand | in-queue |

## Acceptance Criteria

- [ ] AC-FUNC-001 `.github/workflows/audit.yml` exists with `name: audit`, triggers `on: pull_request` AND `on: push: branches: [main]`, and three independent jobs (`audit`, `validate`, `smoke-list`) that run in parallel.
- [ ] AC-FUNC-002 Job `audit` runs `mpm catalog audit .` (without `--strict` initially) and fails the build on any error finding. A comment in the workflow file documents that `--strict` will be added after E12 Phase 2 completes.
- [ ] AC-FUNC-003 Job `validate` runs `mpm validate xml .`, `mpm validate marketplace .`, AND `mpm validate metadata .` in sequence; all three execute even if an earlier one fails so authors see every distinct violation in one PR run.
- [ ] AC-FUNC-004 Job `smoke-list` installs the latest `mpm` CLI from the official package (no source checkout), runs `mpm list --catalog-source file://${{ github.workspace }}@${{ github.sha }} --format json`, and exports `MPM_ALLOW_INSECURE_REMOTES=1` only for the duration of the smoke job per spec Section 3.6.
- [ ] AC-FUNC-005 Job `smoke-list` extracts the set of entry names from `git ls-files repo-specs/**/*-marketplace.xml` (one `<catalog-metadata><name>` per file) and asserts it equals the set returned by `mpm list`. Disagreement fails the job with a diagnostic naming every CLI-only and XML-only entry.
- [ ] AC-FUNC-006 Every action reference in the workflow is pinned (no `@main`, `@master`, or floating `@v*` references for third-party actions). The first-party `actions/checkout` reference is pinned to a specific tag or SHA.
- [ ] AC-FUNC-007 Every `run:` step declares `shell: bash` explicitly and every multi-line `run:` block begins with `set -euo pipefail` per CLAUDE.md GitHub Workflows standards.
- [ ] AC-FUNC-008 The workflow uses an `env:` block at the workflow or job level for every configurable timeout / retry-count value (e.g., `MPM_GIT_RETRY_COUNT`, `MPM_GIT_RETRY_DELAY`, smoke-job-only `MPM_ALLOW_INSECURE_REMOTES=1`); no inline numeric literals for timeouts inside `run:` steps.
- [ ] AC-FUNC-009 The workflow file contains no `--no-verify`, no `continue-on-error: true` on any job whose failure should block the PR, and no bypass annotations or comment-disabled checks.
- [ ] AC-CYCLE-001 End-to-end CI run: push the workflow to a fixture PR (or `act -j audit` against a tmp `private-mpm` checkout); confirm all three jobs run; confirm `audit`, `validate`, `smoke-list` each exit zero on a clean repo; confirm `smoke-list` fails with the asymmetric-entry diagnostic when a `<catalog-metadata><name>` is deliberately mis-typed. Evidence captured in `## TDD Cycle Log`: paste workflow-run URLs (or `act` output), job exit codes, and the deliberate-breakage diagnostic text.
- [ ] AC-DOC-001 All affected documentation updated in the same commit (this task ships no doc files because the workflow is self-documenting via inline comments; the E10-F1-S1-T1 docs reference the workflow by its filename, which MUST match the Manifest entry).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-004 `mypy src` exits zero -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green) -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green) -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green) -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `private-mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `.github/workflows/audit.yml` | New -- GitHub Actions workflow with three jobs (`audit`, `validate`, `smoke-list`) running on PR + push-to-main; covers spec Section 9a CI bullets 1-3 (`mpm catalog audit`, three `mpm validate` sub-subcommands, `mpm list` smoke against `git ls-files repo-specs/**/*-marketplace.xml`). |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `actionlint .github/workflows/audit.yml` (or equivalent syntax checker) exits zero.
- [ ] AC-CYCLE-001 evidence captured in `## TDD Cycle Log`.
- [ ] File staged with explicit `git add .github/workflows/audit.yml` (no `git add -A`).
- [ ] No em-dash characters introduced anywhere.

## TDD Cycle Log

## Comments
