# E10-F2-S1-T3: CI guard pep440-tags.yml -- validate tag names against PEP 440

## Status: in-queue

## Target Repository

- **Repo:** `example-org/private-mpm`
- **Branch:** `backlog/e10-f2-s1-t3`

## Description

Create the GitHub Actions workflow `.github/workflows/pep440-tags.yml`
in the `example-org/private-mpm` repo. The workflow validates that
any tag pushed to the repo, or any tag referenced by a PR, has a
last-path-component that is a valid PEP 440 version per spec Section
3.5 soft-spot 5 and Section 9a CI bullet 5.

### Behaviour envelope (deprecation window)

Per spec Section 9a bullet 5 and Section 15 (future work):

> Warning-only initially. Future release promotes to hard error after a
> deprecation window.

The initial implementation emits a WARN-shape annotation (`::warning::`)
on every non-PEP-440 tag but exits zero so the build does not fail. A
comment in the workflow file documents the planned promotion path:
flipping the `STRICT_MODE` env var (or a one-line `exit 1` toggle) to
hard-error once the deprecation window closes. The promotion is NOT
gated on E12 completion; it is gated on the spec-Section-15 follow-up.

### Implementation

1. Triggers:
   - `on: push: tags: ['**']` for direct tag pushes.
   - `on: pull_request: {}` so PRs that propose a tag (e.g., a
     release-prep PR that touches `CHANGELOG.md` and a new tag in a
     refs file) are scanned for any tag-like reference. For PRs the
     job inspects the set of tags reachable from the PR head that are
     NOT reachable from the base; this catches any tag introduced as
     part of the PR.
2. Job `pep440-tags` on `ubuntu-latest`:
   - `actions/checkout@<pinned>` with `fetch-depth: 0` and
     `fetch-tags: true` so every tag is available.
   - Install Python and the `packaging` library:
     `python -m pip install packaging==<pinned-version>` (the pinned
     version lives in the workflow `env:` block, e.g.,
     `PACKAGING_VERSION: '<pin>'`, so an operator update does not
     require editing the `run:` shell code).
   - Enumerate candidate tags:
     - For `push: tags`: `${{ github.ref_name }}` (a single tag).
     - For `pull_request`: `git tag --contains ${{
       github.event.pull_request.head.sha }} --no-contains ${{
       github.event.pull_request.base.sha }}` (the set of tags
       reachable from the PR head but not from the base).
   - For each candidate tag, take the last path component (everything
     after the final `/`) and validate it via:
     ```bash
     python -c "import sys; from packaging.version import Version; \
       Version(sys.argv[1])" "${last_component}"
     ```
     A non-zero exit from `python -c` means the component is not a
     valid PEP 440 version.
   - For every invalid component, emit `::warning file=...,title=Tag
     not PEP 440::<tag> last-component '<last>' is not a valid PEP
     440 version` to the GitHub annotations stream so the warning
     surfaces in the PR UI.
   - Exit code: zero in the initial implementation regardless of how
     many warnings were emitted. A clearly-commented `STRICT_MODE`
     env var (default `false`) gates a future `exit 1` when any
     invalid tag was seen. The promotion is the operator's
     one-character change at follow-up time.

### Workflow shape

- `name: pep440-tags`
- `on:`
  - `push: { tags: ['**'] }`
  - `pull_request: {}`
- `jobs:` one job (`pep440-tags`); `shell: bash`; `set -euo pipefail`;
  pinned action versions; `packaging` library pinned via workflow
  `env:`.
- Every configurable value (the pinned `packaging` version, the
  `STRICT_MODE` toggle, the annotation title text) lives in the
  workflow `env:` block.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/private-mpm`
- [ ] The branch `backlog/e10-f2-s1-t3` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|

### Approach

1. **Confirm the PEP 440 validation primitive matches E5-F2-S1-T6.**
   E5-F2-S1-T6 implements the catalog-side PEP 440 check using
   `packaging.version.Version`. This workflow uses the same library
   so the rule is consistent across CLI and CI. The workflow's
   `python -c "from packaging.version import Version; Version(...)"`
   call mirrors the CLI's implementation.
2. **Survey existing workflows.** `ls .github/workflows/` to confirm
   `pep440-tags.yml` does not already exist.
3. **Author the workflow file.** Use the canonical `name`, `on`,
   `env`, `jobs` skeleton; one job; pinned action versions; pinned
   `packaging` version via `env`; `shell: bash`; `set -euo pipefail`.
4. **Test the push-trigger path.** Push a tag with a valid PEP 440
   last-component (e.g., `1.0.0`); confirm the workflow runs and
   exits zero with no warnings. Push a tag with a non-PEP-440
   last-component (e.g., `v1.0.0` or `release-2024`); confirm the
   workflow runs, emits a `::warning::` annotation, and still exits
   zero (deprecation-window behaviour).
5. **Test the PR-trigger path.** Open a fixture PR whose head
   contains a new tag (`refs/tags/v2.0.0`); confirm the workflow
   detects the tag via `git tag --contains <head> --no-contains
   <base>` and emits the warning.
6. **Verify STRICT_MODE toggle.** Manually flip the `STRICT_MODE` env
   to `true` against the same fixture; confirm the workflow now
   exits non-zero on the same input. Revert.
7. **Stage selectively.** `git add .github/workflows/pep440-tags.yml`
   only.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Spec:** Section 9a (CI subsection, bullet 5) -- the canonical scope for this task.
- **Spec:** Section 3.5 soft-spot 5 -- the PEP 440 rule the workflow enforces.
- **Spec:** Section 15 -- future hard-error promotion follow-up.

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E5-F2-S1-T6 | `--check tag-format` (soft-spot 5; PEP 440) | in-queue |

## Acceptance Criteria

- [ ] AC-FUNC-001 `.github/workflows/pep440-tags.yml` exists with `name: pep440-tags`, triggers `on: push: tags: ['**']` AND `on: pull_request: {}`, and one job (`pep440-tags`) on `ubuntu-latest`.
- [ ] AC-FUNC-002 The job installs the `packaging` library at a pinned version sourced from a workflow `env:` variable (e.g., `PACKAGING_VERSION`); no inline pip-install version literal in the `run:` step.
- [ ] AC-FUNC-003 For `push: tags` events, the job validates `${{ github.ref_name }}`'s last-path-component (everything after the final `/`) by invoking `python -c "from packaging.version import Version; Version(...)"`. A non-zero exit means the component is not a valid PEP 440 version.
- [ ] AC-FUNC-004 For `pull_request` events, the job enumerates tags via `git tag --contains <head> --no-contains <base>` and validates each tag's last-path-component the same way; PRs that introduce zero new tags pass trivially.
- [ ] AC-FUNC-005 For every invalid last-component, the job emits a GitHub annotation: `::warning file=<workflow-file>,title=Tag not PEP 440::<tag> last-component '<last>' is not a valid PEP 440 version`. The annotation surfaces in the PR Checks UI.
- [ ] AC-FUNC-006 The job exits ZERO in the initial implementation regardless of warning count (warning-only mode per spec Section 9a / Section 15 deprecation window). A clearly-commented `STRICT_MODE` env var (default `false`) gates a future `exit 1` when any invalid tag was seen; flipping the env var to `true` promotes warnings to hard errors without further code changes.
- [ ] AC-FUNC-007 Every action reference in the workflow is pinned (no `@main`, `@master`, or floating `@v*` references for third-party actions).
- [ ] AC-FUNC-008 Every `run:` step declares `shell: bash` explicitly and every multi-line `run:` block begins with `set -euo pipefail`.
- [ ] AC-FUNC-009 The workflow file contains no `--no-verify`, no `continue-on-error: true`, and no bypass annotations.
- [ ] AC-CYCLE-001 End-to-end CI run: against a tmp `private-mpm` checkout, (a) push tag `1.0.0`; confirm exit zero with zero annotations. (b) push tag `v1.0.0`; confirm exit zero with one `::warning::` annotation naming `v1.0.0` and last-component `v1.0.0`. (c) push tag `release-2024`; confirm same warning shape. (d) open a fixture PR whose head contains a new tag `refs/tags/v2.0.0`; confirm the PR-triggered run emits the warning. (e) manually flip `STRICT_MODE=true` against case (b); confirm exit non-zero. Evidence captured in `## TDD Cycle Log` with workflow-run URLs (or `act` output) and annotation text for each case.
- [ ] AC-DOC-001 All affected documentation updated in the same commit. The Changes Manifest matches exactly the files changed by git. (Workflow is self-documenting via inline comments; the E10-F1-S1-T1 docs reference the workflow by its filename, which MUST match the Manifest entry.)
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-004 `mypy src` exits zero -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green) -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green) -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green) -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files -- N/A for YAML Tasks (no Python source authored)
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `private-mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `.github/workflows/pep440-tags.yml` | New -- GitHub Actions workflow with one job (`pep440-tags`) triggered on tag-push AND on PR; validates every candidate tag's last-path-component against PEP 440 via `packaging.version.Version`; emits `::warning::` annotations in deprecation-window mode; `STRICT_MODE` env-var toggle promotes warnings to hard errors per spec Section 15 follow-up. Covers spec Section 9a CI bullet 5. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `actionlint .github/workflows/pep440-tags.yml` exits zero.
- [ ] AC-CYCLE-001 evidence captured in `## TDD Cycle Log` (five fixture scenarios: PEP 440 tag, `v`-prefixed tag, `release-`-prefixed tag, PR-introduced tag, `STRICT_MODE=true` flip).
- [ ] File staged with explicit `git add .github/workflows/pep440-tags.yml` (no `git add -A`).
- [ ] No em-dash characters introduced anywhere.

## TDD Cycle Log

## Comments
