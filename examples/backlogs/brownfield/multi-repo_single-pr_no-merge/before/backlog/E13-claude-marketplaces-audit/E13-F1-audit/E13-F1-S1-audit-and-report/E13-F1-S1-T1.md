# E13-F1-S1-T1: Audit mpm-claude-marketplaces against spec standards; commit follow-up-issue report

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm-claude-marketplaces`
- **Branch:** `backlog/e13-f1-s1-t1`

## Description

Run the four mpm audit / validate commands shipped by E5 against the
checked-out `matthew-dresden/mpm-claude-marketplaces` working tree, classify
every finding by severity, and author a comprehensive Markdown audit
report committed to the target repo at
`audit-reports/2026-05-mpm-deps-spec-audit.md`.

The audit is **read-only against the target repo source tree.** The
only file this Task writes inside the target repo is the audit report
file in the Changes Manifest below. The Task does NOT modify, rename,
or delete any other repo content; it does NOT auto-fix any finding.
Remediation lives in the follow-up issues the operator files after
this Task lands.

### What "the four commands" means

Per the spec, the audit runs all four of the following against the
target repo's working tree (each command's stdout / stderr is captured
verbatim and embedded in the report):

1. `mpm catalog audit <repo-path> --check all --format json`
2. `mpm validate xml <repo-path>`
3. `mpm validate marketplace <repo-path>`
4. `mpm validate metadata <repo-path>`

Per soft-spot rule 4 (spec Section 3.5), `mpm catalog audit` also
re-runs the `<remote>`-resolution walk that `mpm validate
marketplace` performs, so the four commands collectively cover every
soft-spot 1-5 rule plus the trust-model rules from spec Section 3.6.

### Target-repo shape (what to expect)

The target repo's layout differs from `private-mpm`:

- No `repo-specs/` directory.
- No `*-marketplace.xml` files anywhere in the tree.
- Plugin content lives under `common/` with each plugin's
  `.claude-plugin/marketplace.json` describing it.
- A top-level `repo-config.json` declares the monorepo's
  `marketplace_root`, `reserved_directories`, and
  `flattening_strip_prefixes`.

Because the repo has no XML manifests, several mpm commands have
nothing to validate. The audit MUST report this state as an INFO
finding rather than treating "zero inputs found" as a silent pass; the
report records the audit-coverage gap so the follow-up issues
explicitly acknowledge whether mpm's existing standards are
applicable to this repo's `.claude-plugin/marketplace.json` shape OR
whether new mpm validators are needed (the latter is itself a
follow-up issue).

### Report contents

The audit report's body covers:

1. **Provenance header.** First line of the report body reads:
   `Generated by E13-F1-S1-T1 against mpm-claude-marketplaces commit <SHA>`
   where `<SHA>` is the output of `git rev-parse HEAD` run inside the
   target repo at audit time. Includes the audit run timestamp in ISO
   8601 UTC and the mpm CLI version reported by `mpm --version`.
2. **Back-link** to spec Section 9c (`spec/mpm-list-add-lock-features-spec.md`
   (ref)) and to spec Section 3.5 and Section 3.6.
3. **Command outputs.** Verbatim stdout + stderr + exit code from each
   of the four commands listed above. Each block is preceded by the
   exact command line used so the audit is reproducible.
4. **Findings by soft-spot rule.** A section per soft-spot rule from
   spec Section 3.5 (rules 1 through 5) plus a section for spec
   Section 3.6 trust-model violations. Under each rule, every distinct
   finding is listed once with: finding code (the code emitted by the
   mpm CLI, e.g. `MISSING_REQUIRED_FIELD`), severity
   (`ERROR` / `WARN` / `INFO`), the offending path(s) inside the
   target repo, and the mpm-CLI-supplied remediation text.
5. **Recommended follow-up issues.** For each `ERROR` or `WARN`
   finding, a ready-to-paste GitHub-issue body containing: title,
   labels (`mpm-deps-spec`, severity label), problem summary,
   affected paths, remediation steps copied from the mpm CLI
   output, and a reference back to the spec rule the violation
   pertains to. `INFO` findings are documented but do not get
   issue-body drafts (per AC-FUNC-003 below).
6. **Operator filing instructions.** A short section telling the
   operator how to file each issue via the mpm-claude-marketplaces
   GitHub issue tracker (UI link, copy-paste flow). The instructions
   explicitly note that the audit task does NOT file issues itself
   (provider-agnostic constraint per spec Section 3.6).
7. **Summary table.** Count of findings by severity and by soft-spot
   rule, plus an overall pass / fail indication (FAIL when any ERROR
   finding exists, WARN when only WARN/INFO findings exist, PASS when
   only INFO findings exist).

### Definition of Ready

- [ ] All dependency work units are `done` (E5-F2-S1-T1 .. T8 and E5-F3-S1-T1
      ship the four audit / validate commands).
- [ ] Target repository is cloned and accessible at
      `${JUDGE_WORKSPACE_ROOT}/mpm-claude-marketplaces`.
- [ ] The mpm CLI (built from the `matthew-dresden/mpm` working
      tree at HEAD of the merged backlog) is installed and on `PATH`.
- [ ] The branch `backlog/e13-f1-s1-t1` does not already exist (or is
      from a prior attempt).
- [ ] The spec and acceptance criteria are unambiguous; no open questions.

### Depends On This

| ID | Title | Status |
|----|-------|--------|

### Approach

1. **Establish provenance.** `cd` into the target repo working tree;
   capture `git rev-parse HEAD`, current ISO 8601 UTC timestamp, and
   `mpm --version`. Stash these for the report header.
2. **Run the four commands.** For each of `mpm catalog audit
   <repo-path> --check all --format json`, `mpm validate xml
   <repo-path>`, `mpm validate marketplace <repo-path>`, and `mpm
   validate metadata <repo-path>`: capture stdout, stderr, and the
   exit code. Do NOT abort on a non-zero exit; the report must include
   every command's output even when one fails (a non-zero exit is
   itself a finding the report documents).
3. **Parse the findings.** For `mpm catalog audit --format json`
   parse the structured `findings` array directly. For the three
   `mpm validate *` commands parse stderr line-by-line against the
   `ERROR:` / `WARN:` / `INFO:` prefixed lines defined by the spec
   error-message shape (spec Section 4).
4. **Map findings to soft-spot rules.** Each finding-code emitted by
   `mpm catalog audit` maps directly to one of soft-spot rules 1-5.
   `mpm validate xml` findings map to schema correctness (no soft-spot
   bucket; surfaced as a separate section in the report). `mpm
   validate marketplace` findings map to soft-spot 4. `mpm validate
   metadata` findings map to soft-spot 1, 2, or 3 depending on the
   finding code. Findings whose mapping is ambiguous are placed in a
   dedicated "uncategorized" section in the report with a TODO note
   asking the operator to clarify; the report does NOT silently drop
   any finding.
5. **Draft follow-up issue bodies.** For each ERROR or WARN finding,
   compose an issue-body block per the report-contents spec above.
   Group issues by soft-spot rule so the operator can file a batch per
   rule if preferred.
6. **Write the report file.** Author the Markdown report under the
   target repo at the path in the Changes Manifest. The file is the
   only target-repo change this Task makes.
7. **Verify reproducibility.** Re-run the audit a second time against
   the same target-repo HEAD; confirm the second run's findings list
   matches the first run's exactly (every command must be
   deterministic against a fixed input tree). The TDD Cycle Log
   captures both runs' exit codes.
8. **Stage only the Manifest file.** `git add` the single report file
   listed in the Changes Manifest; nothing else.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only. This Task inherits the constraint: it does NOT file GitHub issues itself; the report instead documents the issue bodies for the operator to file manually.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Spec:** `spec/mpm-list-add-lock-features-spec.md` (ref) Section 9c (sibling-repo audit scope).
- **Spec:** Section 3.5 (soft-spot rules 1-5).
- **Spec:** Section 3.6 (trust model; provider-agnostic constraint).
- **Spec:** Section 4.8 (`mpm catalog audit` command surface).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E5-F2-S1-T1 | `mpm catalog audit` framework + --check parser | in-queue |
| E5-F2-S1-T2 | `mpm catalog audit --check metadata` (soft-spot 1) | in-queue |
| E5-F2-S1-T3 | `mpm catalog audit --check source-name-derivation` (soft-spot 2) | in-queue |
| E5-F2-S1-T4 | `mpm catalog audit --check entry-name-uniqueness` (soft-spot 3) | in-queue |
| E5-F2-S1-T5 | `mpm catalog audit --check remote-url` (soft-spot 4) | in-queue |
| E5-F2-S1-T6 | `mpm catalog audit --check tag-format` (soft-spot 5; PEP 440) | in-queue |
| E5-F2-S1-T7 | `mpm catalog audit` legacy `catalog/<name>/` directory detection | in-queue |
| E5-F2-S1-T8 | `mpm catalog audit --strict` (promote warnings to errors) | in-queue |
| E5-F3-S1-T1 | `mpm validate metadata` sub-subcommand | in-queue |

## Acceptance Criteria

- [ ] AC-FUNC-001 The audit report names every distinct finding emitted by the four audit / validate commands, grouped by the soft-spot rule (spec Section 3.5 rules 1-5) or trust-model section (spec Section 3.6) the finding pertains to. No finding from any command's stdout/stderr is silently dropped.
- [ ] AC-FUNC-002 Each finding in the audit report carries an explicit severity classification: one of `ERROR`, `WARN`, or `INFO`. The classification matches the prefix the mpm CLI itself emitted; reclassification (promotion or demotion) is not permitted.
- [ ] AC-FUNC-003 For every `ERROR` and `WARN` finding, the audit report includes a ready-to-paste GitHub-issue body containing: a title, applicable labels, a problem summary, the affected target-repo paths, remediation steps copied from the mpm CLI output, and a reference back to the spec rule the violation pertains to. `INFO` findings are documented in the report but do not get issue-body drafts.
- [ ] AC-FUNC-004 The audit report includes a Summary table counting findings by severity AND by soft-spot rule, plus an overall pass / fail indication (`FAIL` when any `ERROR` exists; `WARN` when only `WARN` / `INFO` exist; `PASS` when only `INFO` exists).
- [ ] AC-FUNC-005 The audit report embeds the verbatim stdout, stderr, and exit code of each of the four commands listed in the Description, each preceded by the exact command line used. A non-zero exit code from any one of the four commands does not abort the audit; the audit captures all four regardless.
- [ ] AC-FUNC-006 The audit report includes an "Operator filing instructions" section explicitly stating that the audit task does NOT file GitHub issues itself (provider-agnostic constraint per spec Section 3.6) and describing the copy-paste flow for filing each recommended issue via the mpm-claude-marketplaces issue tracker.
- [ ] AC-FUNC-007 When `mpm validate xml` / `mpm validate marketplace` / `mpm validate metadata` finds no `*-marketplace.xml` inputs to validate against the target repo's working tree, the audit report records that state as an `INFO` finding with the literal phrasing "no XML manifests present; this mpm validator has nothing to audit against the current repo shape" rather than treating zero-inputs as a silent pass.
- [ ] AC-CYCLE-001 End-to-end audit cycle: run all four commands listed in the Description against the checked-out target-repo working tree, capture every command's exit code and combined output, and assert the file listed in the Changes Manifest exists, is non-empty, and contains the provenance header, the four command-output blocks, the per-soft-spot findings sections, the issue-body drafts, and the summary table required by AC-FUNC-001 through AC-FUNC-006. Logged in `## TDD Cycle Log` with each command's exit code captured.
- [ ] AC-CYCLE-002 Reproducibility check: re-run the same four commands a second time against the same target-repo HEAD and confirm the second run produces the same set of findings (same codes, same severities, same paths) as the first run. The TDD Cycle Log records both runs' exit codes side-by-side. Any divergence between the two runs is itself a finding the audit report documents under a "non-deterministic audit output" subsection.
- [ ] AC-DOC-001 The audit report links back to spec Section 9c (sibling-repo audit scope) AND to spec Section 3.5 (soft-spot rules) AND to spec Section 3.6 (trust model) so a reader can trace each finding back to the rule it violates.
- [ ] AC-DOC-002 The audit report's first body line reads `Generated by E13-F1-S1-T1 against mpm-claude-marketplaces commit <SHA>` where `<SHA>` is the verbatim output of `git rev-parse HEAD` run inside the target repo at audit time. The report also records the audit run timestamp in ISO 8601 UTC and the mpm CLI version reported by `mpm --version`.
- [ ] AC-SEC-001 The audit task does NOT call any provider HTTP API (e.g. `api.github.com`) and does NOT shell out to any provider CLI (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). The report's operator-instructions section states this constraint explicitly.
- [ ] AC-SEC-002 The audit task does NOT modify, rename, or delete any file in the target repo's working tree other than the single report file listed in the Changes Manifest. No security bypass annotations introduced anywhere.
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-004 `mypy src` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm-claude-marketplaces/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `audit-reports/2026-05-mpm-deps-spec-audit.md` | New -- comprehensive audit report against spec Section 3.5 soft-spot rules 1-5 and spec Section 3.6 trust model; embeds verbatim output of `mpm catalog audit`, `mpm validate xml`, `mpm validate marketplace`, and `mpm validate metadata`; classifies every finding by severity (ERROR / WARN / INFO); drafts a ready-to-paste GitHub-issue body per ERROR and WARN finding; includes operator filing instructions, summary table, and provenance header (target-repo commit SHA, audit timestamp, mpm CLI version). |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] The file in the Changes Manifest exists, is non-empty, and is committed on the work-unit branch.
- [ ] The four audit / validate commands listed in the Description have each been run against the target-repo working tree; every command's verbatim output is embedded in the report file from the Changes Manifest.
- [ ] Every finding emitted by any of the four commands appears in the report's findings sections, classified by severity and grouped by soft-spot rule.
- [ ] Each ERROR / WARN finding has a corresponding ready-to-paste GitHub-issue body in the report.
- [ ] The report's first body line carries the provenance string required by AC-DOC-002.
- [ ] Only the file in the Changes Manifest is staged with `git add`; no other target-repo content is modified.
- [ ] TDD Cycle Log captures the two audit runs required by AC-CYCLE-002 with their exit codes side-by-side.

## TDD Cycle Log

## Comments
