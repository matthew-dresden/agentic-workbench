# E3-F3-S1-T8: HTTPS enforcement for `<remote>` URLs in resolved manifests

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e3-f3-s1-t8`

## Description

Implement the spec Section 4.7 "`<remote>` non-HTTPS URL" rule together
with the Section 3.6 trust-model requirement: when a resolved manifest
XML contains a `<remote>` element whose `fetch` URL is not HTTPS or SSH,
`mpm install` raises a hard error unless the
`MPM_ALLOW_INSECURE_REMOTES=1` env var is set.

The accept set:

- HTTPS (`https://...`): allowed unconditionally.
- SSH (`git@host:org/repo.git` OR `ssh://...`): allowed unconditionally.
- HTTP (`http://...`): rejected by default; allowed only with
  `MPM_ALLOW_INSECURE_REMOTES=1`.
- `file://`: rejected by default; allowed only with
  `MPM_ALLOW_INSECURE_REMOTES=1`.
- Any other scheme (`git://`, `ftp://`, custom schemes): rejected by
  default; allowed only with `MPM_ALLOW_INSECURE_REMOTES=1`.

The check runs after manifest XML parsing and before any clone or
`git ls-remote` attempt for any `<project>` whose `<remote>` resolves
to a non-HTTPS/SSH URL. Failure raises `InsecureRemoteUrlError` listing
the source path, the offending `<remote>` name, the URL, and the
remediation: "Use an HTTPS or SSH `<remote>` URL, or set
`MPM_ALLOW_INSECURE_REMOTES=1` if this is intentional (the override
disables the security check)."

Implementation surface:

- Add `MPM_ALLOW_INSECURE_REMOTES = "MPM_ALLOW_INSECURE_REMOTES"` to
  `src/mpm_cli/constants.py` (the env-var name).
- Add `_classify_remote_url_scheme(url: str) -> RemoteUrlScheme` helper
  in a new module `src/mpm_cli/core/remote_url.py`. Enum values:
  `HTTPS`, `SSH_GIT_AT`, `SSH_PROTOCOL`, `HTTP`, `FILE`, `OTHER`.
- Add `_enforce_remote_url_policy(url: str, allow_insecure: bool) ->
  None` helper that raises `InsecureRemoteUrlError` for HTTP / FILE /
  OTHER schemes when `allow_insecure=False`. Returns `None` on accept.
- Add `InsecureRemoteUrlError(InstallError)` exception class.
- Wire the check into the resolver path in `core/install.py`: for each
  resolved `<remote>` URL encountered during transitive include walks,
  call `_enforce_remote_url_policy(url, allow_insecure=os.environ.get(MPM_ALLOW_INSECURE_REMOTES) == "1")`.

The check is enforced on every resolution path (absent-lockfile fresh
resolve, refresh-lock, refresh-lock-source). On the lockfile-consistent
replay path, the check is ALSO enforced against the locked URLs because a
malicious lockfile could record an HTTP `<remote>` URL that the operator
should not silently clone.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 4.7
"`<remote>` non-HTTPS URL" row; Section 3.6 (Trust model) HTTPS
requirement.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e3-f3-s1-t8` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E5-F1-S1-T1 | `mpm doctor` reuses the same `_enforce_remote_url_policy` for workspace checks | in-queue |
| E6-F1-S1-T1 | `mpm catalog audit --check remote-url` reuses the same scheme classifier | in-queue |

### Approach

1. **TDD RED:** Author `tests/unit/test_install_https_enforcement.py`
   parametrising every scheme + env-var combination: HTTPS allowed
   regardless; `git@`-style SSH allowed regardless; `ssh://` allowed
   regardless; HTTP rejected by default, allowed with override; `file://`
   rejected by default, allowed with override; unknown scheme rejected by
   default, allowed with override; empty string rejected (treats as
   OTHER).
2. **TDD RED (integration):** Author
   `tests/integration/test_install_remote_url_policy.py` exercising the
   end-to-end behaviour against a fixture manifest repo with an HTTP
   `<remote>`; assert `mpm install` exits non-zero by default and
   exits zero when `MPM_ALLOW_INSECURE_REMOTES=1` is set.
3. **TDD GREEN:** Implement `RemoteUrlScheme` enum, the classifier, the
   policy enforcer, and `InsecureRemoteUrlError`. Wire into the
   resolver's remote-encounter callsite. Add the env-var constant to
   `constants.py`.
4. **TDD GREEN (replay-path check):** Add the enforcement call to the
   lockfile-consistent replay branch so locked malicious URLs are
   rejected.
5. **TDD REFACTOR:** None expected; the policy is a pure function plus a
   single callsite.
6. **Docs:** Update `docs/configuration.md` with the new env var and the
   security rationale; update `docs/troubleshooting.md` with the recovery
   path; update `docs/lockfile.md` to note that replay also enforces the
   policy.
7. **Verify:** Full suite.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.7 "`<remote>` non-HTTPS URL" row.
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 3.6 (Trust model) HTTPS requirement.

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E3-F3-S1-T1 | Lockfile state-matrix branching in `mpm install` | in-queue |
| E3-F1-S1-T1 | (auto) | proposed |
| E3-F3-S1-T5 | (auto) | proposed |
| E3-F3-S1-T7 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 `_classify_remote_url_scheme("https://github.com/foo/bar.git")` returns `RemoteUrlScheme.HTTPS`; `_enforce_remote_url_policy` accepts it regardless of the `allow_insecure` flag.
- [ ] AC-FUNC-002 `_classify_remote_url_scheme("git@github.com:foo/bar.git")` returns `RemoteUrlScheme.SSH_GIT_AT`; `_enforce_remote_url_policy` accepts it regardless.
- [ ] AC-FUNC-003 `_classify_remote_url_scheme("ssh://git@host/foo/bar.git")` returns `RemoteUrlScheme.SSH_PROTOCOL`; `_enforce_remote_url_policy` accepts it regardless.
- [ ] AC-FUNC-004 `_classify_remote_url_scheme("http://example.com/foo.git")` returns `RemoteUrlScheme.HTTP`; `_enforce_remote_url_policy(url, allow_insecure=False)` raises `InsecureRemoteUrlError`; `_enforce_remote_url_policy(url, allow_insecure=True)` returns `None`.
- [ ] AC-FUNC-005 `_classify_remote_url_scheme("file:///path/to/repo.git")` returns `RemoteUrlScheme.FILE`; same accept-when-allowed rule as HTTP.
- [ ] AC-FUNC-006 `_classify_remote_url_scheme("git://example.com/foo.git")` returns `RemoteUrlScheme.OTHER`; same accept-when-allowed rule.
- [ ] AC-FUNC-007 `_classify_remote_url_scheme("")` returns `RemoteUrlScheme.OTHER`; the policy enforcer rejects it by default.
- [ ] AC-FUNC-008 `mpm install` against a fixture manifest with an HTTP `<remote>` exits non-zero with `InsecureRemoteUrlError` naming the source path, the `<remote>` name, the URL, and the override env var.
- [ ] AC-FUNC-009 `MPM_ALLOW_INSECURE_REMOTES=1 mpm install` against the same fixture exits zero.
- [ ] AC-FUNC-010 The enforcement also runs on the lockfile-consistent replay path; an HTTP URL baked into a lockfile is rejected (defends against malicious lockfile content).
- [ ] AC-TEST-001 Unit tests in `tests/unit/test_install_https_enforcement.py` parametrise every scheme + override combination.
- [ ] AC-TEST-002 Integration test in `tests/integration/test_install_remote_url_policy.py` builds a fixture manifest with an HTTP `<remote>`, asserts the default-reject behaviour AND the override-accept behaviour via a subprocess invocation that sets the env var.
- [ ] AC-CYCLE-001 End-to-end cycle: fixture manifest with HTTP `<remote>`; `mpm install` exits 1; rerun with `MPM_ALLOW_INSECURE_REMOTES=1` exits 0; modify the manifest to use HTTPS; rerun without the override exits 0. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/configuration.md` documents `MPM_ALLOW_INSECURE_REMOTES` with the security rationale; `docs/troubleshooting.md` documents the recovery path; `docs/lockfile.md` notes that replay also enforces the policy.
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/core/remote_url.py` | New -- `RemoteUrlScheme` enum, `_classify_remote_url_scheme`, `_enforce_remote_url_policy`, `InsecureRemoteUrlError`. |
| `src/mpm_cli/core/install.py` | Update -- call the policy enforcer on every encountered `<remote>` URL during fresh resolution AND lockfile replay. |
| `src/mpm_cli/constants.py` | Update -- add `MPM_ALLOW_INSECURE_REMOTES = "MPM_ALLOW_INSECURE_REMOTES"` env-var name. |
| `tests/unit/test_remote_url.py` | New -- parametrised scheme classification + policy tests. |
| `tests/unit/test_install_https_enforcement.py` | New -- wiring test asserting `install` calls the policy enforcer with the correct args from env. |
| `tests/integration/test_install_remote_url_policy.py` | New -- end-to-end HTTP `<remote>` fixture exercising reject-and-override paths. |
| `docs/configuration.md` | Update -- `MPM_ALLOW_INSECURE_REMOTES` env-var row plus security rationale. |
| `docs/troubleshooting.md` | Update -- recovery path for the insecure-remote rejection error. |
| `docs/lockfile.md` | Update -- note that replay also enforces the policy. |
| `tests/unit/test_constants.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/constants.py` per docs/source-test-atomicity.md. |
## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes.
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on new and modified code.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] Docs updated in the same commit.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E3-F3-S1-T8 manually blocked on E3-F1-S1-T1 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E3-F1-S1-T1

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E3-F3-S1-T8 manually blocked on E3-F3-S1-T5 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E3-F3-S1-T5

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E3-F3-S1-T8 manually blocked on E3-F3-S1-T7 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E3-F3-S1-T7
