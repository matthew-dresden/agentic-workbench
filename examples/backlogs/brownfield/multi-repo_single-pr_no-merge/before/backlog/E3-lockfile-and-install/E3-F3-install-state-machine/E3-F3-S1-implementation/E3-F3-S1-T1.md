# E3-F3-S1-T1: Lockfile state-matrix branching in `mpm install`

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e3-f3-s1-t1`

## Description

Modify `src/mpm_cli/core/install.py::install` to read the lockfile (when
present), compare `mpm_hash(.mpm)` to the lockfile's `mpm_hash` field,
compare the lockfile's `[catalog].source` to the CLI/env catalog source, and
branch on the five-row state matrix from spec Section 4.7:

| State | Behaviour |
|---|---|
| `.mpm` exists, `.mpm.lock` absent | Resolve every transitive version fresh; install; write `.mpm.lock` capturing every resolved SHA + the catalog source + the `mpm_hash`; emit info-line `"lockfile rebuilt from .mpm (N sources, M projects)"`. |
| `.mpm` exists, `.mpm.lock` exists, both consistent (matching `mpm_hash`) | Install EXACTLY the SHAs in the lockfile; do NOT re-resolve; ignore newer tags; emit info-line `"installing from lockfile (N sources, M projects)"`. Catalog source MAY be read from the lockfile's `[catalog].source` when no CLI/env source is set. |
| `.mpm` modified (hash mismatch) | Hard error. Remediation: `mpm install --refresh-lock` or `--refresh-lock-source <name>`. |
| `.mpm.lock` references a SHA no longer reachable on remote | Hard error. Remediation: `mpm install --refresh-lock-source <name>` for the affected source. |
| `.mpm.lock` records a different `[catalog].source` than the CLI/env source (when CLI/env is set) | Hard error. The lockfile is authoritative; remediation is `--refresh-lock` (operator changed catalogs intentionally). |

Implementation surface:

- Add `read_lockfile_if_present(path: Path) -> Lockfile | None` helper in
  `core/install.py` (or extract to `core/lockfile.py` if it fits the SRP
  there; T1's lockfile module is the natural home). Returns `None` for an
  absent path; raises through for any other error.
- Compute `mpm_hash(.mpm)` once per install invocation; pass the value
  through to the lockfile-write path so the new lockfile records the
  hash that produced it.
- Add the catalog-source resolution rule: precedence is CLI flag, then
  `MPM_CATALOG_SOURCE` env var, then `lockfile.[catalog].source` (only on
  the consistent state; never on refresh paths). If none of these is set,
  raise `MissingCatalogSourceError` with the spec's standard message.
- Define the new state-branch info-line emit function
  `_emit_install_state(state: InstallState, sources: int, projects: int)`
  that prints the spec's exact info-line text for the matching state.
- Define the new exception classes in `core/install.py`:
  `MpmHashMismatchError`, `LockfileUnreachableShaError`,
  `CatalogSourceMismatchError`. Each carries the standard error-shape
  payload (one-line summary, context, remediation line).

This task does NOT implement the four state-rows that are owned by sibling
tasks: branch-drift, transitive-conflict (T5), orphan-lock-entry (T4),
include-cycle/diamond (T6), HTTPS-enforcement (T8). T1 ships the
foundation; the sibling tasks layer their behaviour on top of the
branching framework T1 introduces.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 4.7 (the
state matrix), Section 4.7.1 (engine contract), and Section 4 header
(catalog-source resolution precedence with lockfile fallback).

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e3-f3-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E3-F3-S1-T2 | `--refresh-lock` flag | in-queue |
| E3-F3-S1-T3 | `--refresh-lock-source` flag | in-queue |
| E3-F3-S1-T4 | `--strict-lock` + `--strict-drift` flags | in-queue |
| E3-F3-S1-T5 | Canonical-URL conflict detection | in-queue |
| E3-F3-S1-T6 | `<include>` cycle and diamond handling | in-queue |
| E3-F3-S1-T7 | Default `--lock-file` derivation from `--mpm-file` | in-queue |
| E3-F3-S1-T8 | HTTPS enforcement for `<remote>` URLs | in-queue |
| E3-F3-S1-T9 | Concurrency lock extension to `mpm add` / `mpm remove` | in-queue |
| E4-F1-S1-T1 | `mpm outdated` reads lockfile | in-queue |
| E4-F2-S1-T1 | `mpm why` reads lockfile | in-queue |
| E5-F1-S1-T1 | `mpm doctor` lockfile parity checks | in-queue |

### Approach

1. **TDD RED (unit):** Author `tests/unit/test_install_state_matrix.py`
   parametrising the five state rows. Each test mocks out the resolver and
   the symlink aggregator (existing helpers in `core/install.py` already
   support mocking via dependency injection), constructs a synthetic
   `.mpm` plus optional `.mpm.lock` on `tmp_path`, and asserts the
   correct branch is taken: which info-line is emitted, whether the
   lockfile is rewritten, whether a hard error is raised with the expected
   exception class and message.
2. **TDD RED (integration):** Author
   `tests/integration/test_install_lockfile_replay.py` exercising the
   `lockfile-consistent` row end-to-end: build a fixture git repo with a
   tagged release, run `mpm install` to produce a baseline lockfile,
   modify the remote (add a newer tag), run `mpm install` again, assert
   the lockfile is unchanged AND the installed SHA matches the original
   tag (the newer tag is ignored).
3. **TDD GREEN:** Add the lockfile-reading branch to
   `core/install.py::install`. The function's signature extends with a
   new keyword argument `lock_file_path: Path | None = None` (the resolved
   default is provided by T7; T1 accepts whatever is passed).
4. **TDD GREEN (catalog-source resolution):** Extract a helper
   `_resolve_catalog_source(cli_arg, env_value, lockfile_catalog_source,
   install_state)` that implements the precedence rule and the
   state-dependent lockfile fallback. The helper returns the canonical
   `<url>@<ref>` string or raises `MissingCatalogSourceError`.
5. **TDD GREEN (error classes):** Define `MpmHashMismatchError`,
   `LockfileUnreachableShaError`, and `CatalogSourceMismatchError` as
   subclasses of a new base `InstallError(Exception)`. Each error class
   accepts a structured payload (summary, context, remediation) and
   renders via `str(err)` to the spec's standard three-line shape.
6. **TDD GREEN (info-line emit):** Implement `_emit_install_state` to
   print the spec's verbatim info-lines to stdout (info-level, not
   stderr). The info-line text is parametrised on `(state, sources_count,
   projects_count)`.
7. **TDD REFACTOR:** If existing helpers in `core/install.py` overlap with
   the new branching code, extract a private `_classify_install_state(.mpm_path,
   lockfile_path)` function that returns an `InstallState` enum, so the
   downstream branch is a tidy `match state:` block.
8. **Docs:** Update `docs/architecture.md` (NEW per spec Section 4.7.1)
   with the install state-matrix diagram and the new error-class table.
   Update `docs/lockfile.md` with the cross-link from each lockfile field
   to the state branch that reads it.
9. **Verify:** Full suite.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.7 (state matrix and info-line text), Section 4.7.1 (install engine contract).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4 header (catalog-source precedence with lockfile fallback as the documented exception to NO-FALLBACK).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 5.1 (`mpm_hash` consumed by the hash-comparison branch).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E3-F1-S1-T1 | TOML lockfile parser + writer (schema v1) | in-queue |
| E3-F2-S1-T1 | `mpm_hash()` deterministic SHA-256 | in-queue |
| E1-F4-S1-T1 | Shared `--catalog-source` CLI args module | in-queue |

## Acceptance Criteria

- [ ] AC-FUNC-001 State `lockfile-absent`: `mpm install` resolves every transitive version fresh, installs, writes `.mpm.lock` with the matching `mpm_hash` and `[catalog].source`, and emits the info-line `"lockfile rebuilt from .mpm (N sources, M projects)"` with the correct counts.
- [ ] AC-FUNC-002 State `lockfile-consistent`: `mpm install` installs exactly the SHAs in `.mpm.lock` without re-resolving (the resolver helper is NOT called); the info-line `"installing from lockfile (N sources, M projects)"` is emitted with the correct counts; the lockfile on disk is not modified.
- [ ] AC-FUNC-003 State `lockfile-hash-mismatch`: `mpm install` raises `MpmHashMismatchError` with a message naming both the lockfile's `mpm_hash` and the freshly-computed `mpm_hash(.mpm)`; the remediation line names `mpm install --refresh-lock` and `--refresh-lock-source <name>`.
- [ ] AC-FUNC-004 State `lockfile-unreachable-sha`: when the resolver discovers a lockfile SHA is no longer reachable on remote, `mpm install` raises `LockfileUnreachableShaError` naming the source, the SHA, and the remote URL; remediation is `--refresh-lock-source <name>`.
- [ ] AC-FUNC-005 State `lockfile-catalog-source-mismatch`: when the lockfile's `[catalog].source` differs from the CLI/env catalog source (and CLI/env is set), `mpm install` raises `CatalogSourceMismatchError` naming both values; remediation is `--refresh-lock`.
- [ ] AC-FUNC-006 Catalog-source precedence (consistent state): CLI flag wins over `MPM_CATALOG_SOURCE` env var, which wins over `lockfile.[catalog].source`; the lockfile fallback applies ONLY in the consistent state and ONLY when both CLI flag and env var are unset.
- [ ] AC-FUNC-007 Catalog-source missing (all three sources unset, lockfile absent or not consulted): `mpm install` raises `MissingCatalogSourceError` with the spec's standard error text linking to `docs/catalogs-explained.md` (ref).
- [ ] AC-FUNC-008 The `_classify_install_state(mpm_path, lockfile_path)` helper returns the correct `InstallState` enum for each of the five spec rows; parametrised over fixture combinations of present/absent files and matching/mismatching hashes.
- [ ] AC-TEST-001 Unit tests in `tests/unit/test_install_state_matrix.py` parametrise every row of the spec's state matrix, mocking the resolver to return predetermined SHAs and asserting the correct branch is taken.
- [ ] AC-TEST-002 Integration test in `tests/integration/test_install_lockfile_replay.py` builds a real fixture git repo with a tagged release, runs `mpm install` end-to-end via the CLI entry point, modifies the remote to add a newer tag, runs `mpm install` again, asserts the second run installs the ORIGINAL SHA (lockfile replay ignores the newer tag) and the lockfile is unchanged on disk.
- [ ] AC-CYCLE-001 End-to-end cycle: fixture git repo at tag `1.0.0`; first install writes lockfile; modify `.mpm` REVISION to `1.1.0`; second install hard-errors with `MpmHashMismatchError` naming both `mpm_hash` values; recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/architecture.md` (NEW or extended) documents the install state matrix as a table with each state's behaviour, error class, and remediation; `docs/lockfile.md` cross-links each lockfile field to the state branch that reads it.
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/core/install.py` | Update -- add `_classify_install_state`, `_resolve_catalog_source`, `_emit_install_state`, the five-row state-matrix branch inside `install`, and the new exception classes (`InstallError`, `MpmHashMismatchError`, `LockfileUnreachableShaError`, `CatalogSourceMismatchError`). |
| `tests/unit/test_install_state_matrix.py` | New -- parametrised unit tests for all five state rows. |
| `tests/integration/test_install_lockfile_replay.py` | New -- end-to-end lockfile-replay integration test against a fixture git repo. |
| `docs/architecture.md` | New -- install engine internals, state-matrix table, error-class table. |
| `docs/lockfile.md` | Update -- cross-link each lockfile field to its state branch. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes.
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on the new and modified code in `src/mpm_cli/core/install.py`.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] `docs/architecture.md` and `docs/lockfile.md` updated in the same commit.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E3-F3-S1-T1 manually blocked on E3-F2-S1-T1 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E3-F2-S1-T1
