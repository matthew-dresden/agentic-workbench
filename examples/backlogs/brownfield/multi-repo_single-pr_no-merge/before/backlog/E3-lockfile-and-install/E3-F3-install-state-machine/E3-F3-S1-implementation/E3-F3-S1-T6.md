# E3-F3-S1-T6: `<include>` cycle and diamond handling

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e3-f3-s1-t6`

## Description

Implement transitive `<include>` resolution with cycle detection AND
diamond support during `mpm install`. Today's resolver walks
`<include>` chains, but the cycle and diamond cases are not explicitly
defined. Spec Section 4.7 mandates:

- **Cycle detection**: walking `<include>` chains depth-first, the
  resolver tracks visited XML paths (canonicalised). When the walk
  re-visits a node that is already on the active path, the resolver
  raises `IncludeCycleError` with a message rendering the cycle as
  `A.xml -> B.xml -> A.xml` (the closing edge that detected the cycle
  is rendered explicitly so the operator sees where the loop closes).
- **Diamond handling**: when the walk re-visits a node that has already
  been fully processed (i.e., it is in the "done" set, not the "active
  path" set), the resolver deduplicates and continues; the second visit
  is a no-op for `<include>` traversal but the project rows below the
  shared include are STILL processed under both paths' contexts (which
  is fine because diamonds resolve to the same SHAs).

The canonical path used as the dedup key is the resolved absolute path
inside the manifest repo (e.g., `repo-specs/git-connection/remote.xml`),
not the relative `<include name=...>` attribute value (which can differ
between callers). Canonicalisation strips `./` segments, resolves `..`
segments, and normalises path separators per stdlib `os.path.normpath`.

Implementation surface:

- Add `_walk_includes(start_xml_path: Path, manifest_repo: Path) ->
  IncludeTree` helper in `core/install.py` (or in a new helper module
  `core/include_walker.py` if it keeps `install.py` from sprawling).
  The walker uses two sets: `active_path: list[Path]` (stack of
  ancestors on the current DFS branch) and `done: set[Path]` (paths
  whose subtree has been fully processed). Pre-visit: if the next path
  is in `active_path`, raise `IncludeCycleError` with the rendered
  cycle. If it is in `done`, skip (diamond dedupe). Otherwise push to
  `active_path`, recurse, pop, add to `done`.
- Add `IncludeCycleError(InstallError)` exception class.
- Add `IncludeTree` dataclass: `path: Path`, `includes: list[IncludeTree]`,
  `projects: list[ResolvedProject]`. The walker returns the root of this
  tree; downstream lockfile-writing code serialises it into the nested
  `[[sources.includes]]` TOML structure.
- The walker's output drives lockfile serialisation: nested
  `[[sources.includes]]` entries match the depth and order of the DFS
  walk; diamond-deduped duplicates appear only at their first
  encountered position (a comment in the lockfile-write helper notes
  this).

The error message format for cycles renders every node in order:
`"include cycle: <p0> -> <p1> -> ... -> <pn> -> <p0>"` where `p0` is the
node that closed the cycle. The path list uses repo-relative paths (not
absolute filesystem paths) so the message is portable across operator
machines.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 4.7
"`<include>` cycle detected" and "`<include>` diamond" rows; Section 5
(lockfile format) for the nested `[[sources.includes]]` structure.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e3-f3-s1-t6` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E3-F3-S1-T3 | `--refresh-lock-source` reuses `_walk_includes` when re-resolving a single source's chain | in-queue |
| E4-F2-S1-T1 | `mpm why` reuses the include tree to render provenance | in-queue |

### Approach

1. **TDD RED (unit):** Author `tests/unit/test_include_walker.py`
   with parametrised cases: (a) flat single XML (zero includes) -> tree
   has root only; (b) two-level chain A -> B -> C -> tree depth 2; (c)
   cycle A -> B -> A -> `IncludeCycleError` rendered correctly; (d)
   diamond A includes B and C, both B and C include D -> tree has D
   appearing only once at its first-walked position; (e) self-include A
   -> A is a one-cycle, rendered as `A.xml -> A.xml`.
2. **TDD RED (integration):** Author
   `tests/integration/test_install_include_cycle.py` and
   `tests/integration/test_install_include_diamond.py` exercising the
   walker against real manifest-repo fixtures placed in `tmp_path`. The
   cycle fixture creates three XML files forming a triangle; the diamond
   fixture creates four files forming a diamond.
3. **TDD GREEN:** Implement the walker in
   `src/mpm_cli/core/include_walker.py` (new module to keep
   `install.py` from sprawling). Implement `IncludeCycleError` and
   `IncludeTree` dataclasses. Re-export from `core/install.py` if
   call-sites prefer that import path.
4. **TDD GREEN (lockfile wiring):** Update `install.py`'s
   lockfile-serialisation step to consume the `IncludeTree` and produce
   the nested `[[sources.includes]]` TOML structure that
   E3-F1-S1-T1's writer expects.
5. **TDD REFACTOR:** Extract the `os.path.normpath`-based
   canonicalisation as a `_canonicalize_include_path(path: Path) -> Path`
   helper for DRY (it is reused on every walk step).
6. **Docs:** Update `docs/lockfile.md` with the include-tree
   serialisation rule and the diamond-dedupe note; update
   `docs/architecture.md` with the cycle-detection algorithm.
7. **Verify:** Full suite.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.7 "`<include>` cycle detected" and "`<include>` diamond" rows.
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 5 (lockfile format) -- nested `[[sources.includes]]` structure that the walker output must produce.

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E3-F3-S1-T1 | Lockfile state-matrix branching in `mpm install` | in-queue |
| E3-F3-S1-T4 | (auto) | proposed |
| E3-F3-S1-T5 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 `_walk_includes(start, repo)` returns an `IncludeTree` rooted at `start` with no `includes` for an XML with zero `<include>` elements.
- [ ] AC-FUNC-002 `_walk_includes` builds a depth-N tree for an N-deep `<include>` chain; each node's `path` is the canonical repo-relative path.
- [ ] AC-FUNC-003 `_walk_includes` raises `IncludeCycleError` for any cycle including the self-cycle A -> A; the error's message renders the cycle as `"include cycle: <p0> -> ... -> <p0>"` using repo-relative paths.
- [ ] AC-FUNC-004 `_walk_includes` produces a single tree node for a diamond-deduped XML (re-visit while not on the active path); the deduped node appears at its first-walked position in the tree, not at both diamond legs.
- [ ] AC-FUNC-005 `<include>` paths are canonicalised via `os.path.normpath` before comparison; equivalent `./foo.xml (ref)` (ref) and `foo.xml` (ref) references are treated as the same node.
- [ ] AC-FUNC-006 The walker's output drives lockfile serialisation: the nested `[[sources.includes]]` TOML structure produced by the writer matches the depth and order of the DFS walk.
- [ ] AC-FUNC-007 `IncludeCycleError` is a subclass of the `InstallError` base T1 introduced.
- [ ] AC-TEST-001 Unit tests in `tests/unit/test_include_walker.py` parametrise the five cases in AC-FUNC-001 through AC-FUNC-005 and the diamond ordering rule in AC-FUNC-006.
- [ ] AC-TEST-002 Integration test `tests/integration/test_install_include_cycle.py` builds a fixture manifest repo with three XML files (A includes B, B includes C, C includes A) and asserts `mpm install` exits non-zero with `IncludeCycleError` and the rendered cycle.
- [ ] AC-TEST-003 Integration test `tests/integration/test_install_include_diamond.py` builds a fixture manifest repo with a four-XML diamond (A includes B and C; B and C both include D); `mpm install` succeeds; the resulting lockfile has D appearing exactly once in the include tree.
- [ ] AC-CYCLE-001 End-to-end cycle: (a) build a fixture manifest repo with a triangle cycle; `mpm install` exits 1 with the rendered cycle. (b) Replace the triangle with a four-node diamond; `mpm install` exits 0 and the lockfile's include tree has the shared XML appearing once. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/lockfile.md` documents the include-tree serialisation order and the diamond-dedupe rule; `docs/architecture.md` documents the cycle-detection algorithm.
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/core/include_walker.py` | New -- `_walk_includes`, `IncludeCycleError`, `IncludeTree`, `_canonicalize_include_path`. |
| `src/mpm_cli/core/install.py` | Update -- import the walker; wire its output into the resolver and the lockfile-serialisation step. |
| `tests/unit/test_include_walker.py` | New -- parametrised tests for tree depth, cycle, diamond, normalisation. |
| `tests/integration/test_install_include_cycle.py` | New -- end-to-end triangle-cycle fixture. |
| `tests/integration/test_install_include_diamond.py` | New -- end-to-end four-node diamond fixture. |
| `docs/lockfile.md` | Update -- include-tree serialisation and diamond-dedupe rules. |
| `docs/architecture.md` | Update -- cycle-detection algorithm description. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes.
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on new and modified code.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] Docs updated in the same commit.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E3-F3-S1-T6 manually blocked on E3-F3-S1-T4 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E3-F3-S1-T4

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E3-F3-S1-T6 manually blocked on E3-F3-S1-T5 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E3-F3-S1-T5
