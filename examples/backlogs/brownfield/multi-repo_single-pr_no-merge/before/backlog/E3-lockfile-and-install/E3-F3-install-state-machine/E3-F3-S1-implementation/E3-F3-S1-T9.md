# E3-F3-S1-T9: Concurrency lock extension to `mpm add` and `mpm remove`

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e3-f3-s1-t9`

## Description

Today's `mpm install` acquires an exclusive `fcntl.flock(LOCK_EX)` on
`.mpm-data/.mpm-install.lock` (`INSTALL_LOCK_FILENAME` in
`constants.py`) so concurrent install invocations serialise. Spec
Section 7.5 extends this lock to also cover:

- `mpm add` (E2-F4): mutates `.mpm`; the lock prevents a concurrent
  install from reading a half-written `.mpm` (atomic temp+rename
  prevents truncation but does not prevent racing readers from missing a
  source).
- `mpm remove` (E2-F5): also mutates `.mpm`; same rationale.
- `mpm doctor --refresh-completion-cache`: mutates completion-cache
  files; the lock prevents two concurrent refreshes from clobbering each
  other.

Same lock file (`.mpm-data/.mpm-install.lock`), same `fcntl.flock`
semantics: blocking exclusive, kernel-released on process exit (so
crashes do not leave a stuck lock).

If `.mpm-data/` does not yet exist (first invocation in a fresh
workspace), mpm creates it eagerly with `mkdir(parents=True,
exist_ok=True)` before acquiring the lock so the lock file has a place
to live.

Implementation surface:

- Extract today's install-only lock-acquire code into a reusable context
  manager at `src/mpm_cli/utils/concurrency.py` (new module). Public
  API: `mpm_workspace_lock(workspace_root: Path)` returns a context
  manager that creates `.mpm-data/` if absent, opens the lock file,
  acquires `LOCK_EX`, yields, and releases (and closes the FD) on
  context exit. The context manager is reentrant only across processes,
  not within a single process (the standard `fcntl.flock` semantics on
  Linux do not promote LOCK_EX to LOCK_SH if the same process reacquires;
  the wrapper documents this).
- Update `src/mpm_cli/core/install.py` to import and use
  `mpm_workspace_lock` instead of the inline `fcntl.flock` block; the
  behaviour is byte-equivalent.
- Update `src/mpm_cli/commands/add.py` (created by E2-F4-S1-T1) to wrap
  its `.mpm` mutation inside `mpm_workspace_lock`.
- Update `src/mpm_cli/commands/remove.py` (created by E2-F5-S1-T1) to
  wrap its `.mpm` mutation inside `mpm_workspace_lock`.
- Update the completion-cache refresh path in `src/mpm_cli/commands/doctor.py`
  (existing) to wrap its mutation inside `mpm_workspace_lock`.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 7.5
(Concurrency and atomicity) including the extension of the lock to
`mpm add` / `mpm remove` / `mpm doctor --refresh-completion-cache`
and the eager-create rule for `.mpm-data/`.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e3-f3-s1-t9` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E5-F1-S1-T1 | `mpm doctor --refresh-completion-cache` wraps its mutation in this context manager | in-queue |

### Approach

1. **TDD RED:** Author `tests/unit/test_concurrency.py` with
   parametrised cases: (a) `mpm_workspace_lock` creates `.mpm-data/`
   if absent; (b) lock acquire is exclusive (blocking) across processes;
   (c) lock release happens on normal context exit; (d) lock release
   happens on exception inside the context (try/finally semantics).
   Inter-process tests use `multiprocessing.Process` with a barrier so
   one process holds the lock while the other attempts acquisition with
   `fcntl.LOCK_NB` and verifies it fails until the first releases.
2. **TDD RED (integration):** Author
   `tests/integration/test_concurrency_serialization.py` spawning two
   concurrent `mpm install` subprocesses against the same workspace
   (using `subprocess.Popen`) and asserting they serialise via
   timestamps on the resulting lockfile (the second install starts
   AFTER the first finishes; the lockfile mtime sequence proves it).
3. **TDD GREEN:** Implement `mpm_workspace_lock` as a context manager
   using `contextlib.contextmanager` plus `fcntl.flock`. Move the
   existing install-time `flock` code into this helper. Update the four
   call sites (`install`, `add`, `remove`, `doctor`).
4. **TDD GREEN (eager-create):** The context manager runs
   `workspace_root.joinpath(".mpm-data").mkdir(parents=True, exist_ok=True)`
   before opening the lock file. The lock file path is built from
   `INSTALL_LOCK_FILENAME` in `constants.py`.
5. **TDD REFACTOR:** None expected; this is a relocate-and-reuse change.
6. **Docs:** Update `docs/architecture.md` with the concurrency model;
   update `docs/troubleshooting.md` with the "another mpm process is
   running" wait scenario.
7. **Verify:** Full suite.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 7.5 (Concurrency and atomicity) -- lock extension to `mpm add` / `mpm remove` / `mpm doctor --refresh-completion-cache` plus eager-create rule for `.mpm-data/`.

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E3-F3-S1-T1 | Lockfile state-matrix branching in `mpm install` | in-queue |
| E2-F4-S1-T1 | `mpm add` command surface | in-queue |
| E2-F5-S1-T1 | `mpm remove` command surface | in-queue |
| E2-F4-S1-T3 | (auto) | proposed |
| E2-F5-S1-T2 | (auto) | proposed |
| E3-F3-S1-T6 | (auto) | proposed |
| E3-F3-S1-T8 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 `mpm_workspace_lock(workspace_root)` creates `.mpm-data/` (with `parents=True, exist_ok=True`) before opening the lock file, so a first-invocation operator with no `.mpm-data/` directory does not hit `FileNotFoundError`.
- [ ] AC-FUNC-002 `mpm_workspace_lock(workspace_root)` acquires `fcntl.LOCK_EX` on `.mpm-data/.mpm-install.lock` and releases on normal context exit; the FD is closed on exit so the kernel cleans up.
- [ ] AC-FUNC-003 An exception raised inside the context still releases the lock (try/finally semantics); the test asserts a subsequent `LOCK_NB` acquisition succeeds in the same process after the exception.
- [ ] AC-FUNC-004 Two concurrent processes calling `mpm_workspace_lock` against the same workspace serialise: the second process blocks until the first releases; the test uses `multiprocessing.Barrier` and `LOCK_NB` to assert the contention.
- [ ] AC-FUNC-005 `mpm install`, `mpm add`, `mpm remove`, and `mpm doctor --refresh-completion-cache` all wrap their mutations inside `mpm_workspace_lock`; the test uses subprocess invocations to confirm pairwise serialisation between every cross-command pair.
- [ ] AC-FUNC-006 The lock file path is built from `INSTALL_LOCK_FILENAME` in `constants.py` (ref); no inline path literals exist in the four command callers or in the context manager.
- [ ] AC-TEST-001 Unit tests in `tests/unit/test_concurrency.py` parametrise the eager-create case, the normal-exit release case, the exception-exit release case, and the cross-process contention case.
- [ ] AC-TEST-002 Integration test in `tests/integration/test_concurrency_serialization.py` spawns two `mpm install` subprocesses concurrently against the same workspace and asserts they serialise via lockfile mtime ordering; a second variant spawns `mpm install` + `mpm add` concurrently and asserts the same.
- [ ] AC-CYCLE-001 End-to-end cycle: start two concurrent `mpm install` subprocesses against a shared workspace; one blocks while the other holds the lock; the blocked one finishes after the holder releases; the final lockfile content is consistent with the holder's resolved SHAs. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/architecture.md` documents the concurrency model; `docs/troubleshooting.md` documents the "another mpm process is running" wait scenario including how to identify the holder via `lsof`.
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/utils/concurrency.py` | New -- `mpm_workspace_lock(workspace_root)` context manager with eager-create + LOCK_EX + release on exit. |
| `src/mpm_cli/core/install.py` | Update -- replace inline `fcntl.flock` block with `mpm_workspace_lock` usage. |
| `src/mpm_cli/commands/add.py` | Update -- wrap `.mpm` mutation inside `mpm_workspace_lock`. |
| `src/mpm_cli/commands/remove.py` | Update -- wrap `.mpm` mutation inside `mpm_workspace_lock`. |
| `src/mpm_cli/commands/doctor.py` | Update -- wrap `--refresh-completion-cache` mutation inside `mpm_workspace_lock`. |
| `tests/unit/test_concurrency.py` | New -- parametrised tests for eager-create, normal-exit release, exception-exit release, cross-process contention (paired with `src/mpm_cli/utils/concurrency.py` per source/test atomicity). |
| `tests/integration/test_concurrency_serialization.py` | New -- end-to-end concurrent subprocess invocation tests across all four commands. |
| `docs/architecture.md` | Update -- concurrency model section. |
| `docs/troubleshooting.md` | Update -- "another mpm process is running" wait scenario plus `lsof`-based diagnosis. |
| `tests/unit/test_install.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/core/install.py` per docs/source-test-atomicity.md. |
| `tests/unit/test_add.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/commands/add.py` per docs/source-test-atomicity.md. |
| `tests/unit/test_remove.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/commands/remove.py` per docs/source-test-atomicity.md. |
| `tests/unit/test_doctor.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/commands/doctor.py` per docs/source-test-atomicity.md. |
## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes.
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on new and modified code.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] Docs updated in the same commit.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E3-F3-S1-T9 manually blocked on E2-F4-S1-T3 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E2-F4-S1-T3

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E3-F3-S1-T9 manually blocked on E2-F5-S1-T2 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E2-F5-S1-T2

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E3-F3-S1-T9 manually blocked on E3-F3-S1-T6 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E3-F3-S1-T6

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E3-F3-S1-T9 manually blocked on E3-F3-S1-T8 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E3-F3-S1-T8
