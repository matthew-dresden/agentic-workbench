# E3-F2-S1: Implement `mpm_hash()`

## Status: in-queue

## Description

Single-task story authoring `src/mpm_cli/core/mpm_hash.py` with the
`mpm_hash(mpm_path: Path) -> str` function plus its paired unit-test
file. The story is one task because the hash function is a small, focused
utility with no internal phases that warrant splitting across multiple
tasks; the spec section it implements (5.1) is a single normalisation
algorithm.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 5.1.

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
