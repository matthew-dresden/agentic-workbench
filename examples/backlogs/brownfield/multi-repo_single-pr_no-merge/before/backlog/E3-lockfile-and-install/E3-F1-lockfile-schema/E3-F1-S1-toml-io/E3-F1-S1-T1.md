# E3-F1-S1-T1: TOML lockfile parser and atomic writer (schema v1)

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e3-f1-s1-t1`

## Description

Author `src/mpm_cli/core/lockfile.py` containing the schema v1 dataclass
tree plus the two public entry points:

- `read_lockfile(path: Path) -> Lockfile`: parse a TOML file from disk into
  the dataclass tree, applying every validation rule in spec Section 5
  (resolved-SHA hex-length check; revision-spec PEP 440 / `refs/...` /
  branch-charset regex; `canonical_url` equals `canonicalize_repo_url(url)`;
  `path` and `path_in_repo` reject embedded NUL / newline / tab characters).
  Validation failures raise specific exceptions (`LockfileSchemaError`,
  `LockfileValidationError`) with messages naming the offending field, the
  unparseable value, and the operator's likely remediation step.
- `write_lockfile(lockfile: Lockfile, path: Path) -> None`: serialise the
  dataclass tree back to TOML and atomically replace the target path using
  a write-temp-then-rename pattern in the same directory (so the rename is
  on the same filesystem). The temp file uses a `.tmp.<pid>.<rand>` suffix
  so two concurrent writers do not collide on the temp path.

The dataclass tree mirrors the TOML schema exactly:

- `Lockfile`: `schema_version: int`, `generated_at: str` (ISO-8601 UTC),
  `generator: str` (e.g., `"mpm-cli/1.4.0"`), `mpm_hash: str`,
  `catalog: CatalogBlock`, `sources: list[SourceEntry]`.
- `CatalogBlock`: `source: str` (the `<url>@<ref>` form),
  `url: str`, `revision_spec: str`, `resolved_ref: str`, `resolved_sha: str`.
- `SourceEntry`: `name: str`, `url: str`, `revision_spec: str`,
  `resolved_ref: str`, `resolved_sha: str`, `path: str`,
  `includes: list[IncludeEntry]`, `projects: list[ProjectEntry]`.
- `IncludeEntry`: `name: str`, `path_in_repo: str`, `url: str`,
  `resolved_sha: str`, `includes: list[IncludeEntry]` (recursive, unbounded
  depth).
- `ProjectEntry`: `name: str`, `url: str`, `canonical_url: str`,
  `revision_spec: str`, `resolved_ref: str`, `resolved_sha: str`.

This task also adds `MPM_LOCK_FILE = "MPM_LOCK_FILE"` to
`src/mpm_cli/constants.py` (the env-var name; the value is the operator's
override path). The constant lives next to the existing
`MPM_MPM_FILE` constant so the env-var surface is grouped.

Validation specifics:

- `resolved_sha` must match `^[a-f0-9]{40}$` OR `^[a-f0-9]{64}$` (SHA-1 OR
  SHA-256 git object IDs). Uppercase hex is rejected; mixed-case is
  rejected; non-hex is rejected.
- `revision_spec` is accepted if it satisfies any one of: parses as
  `packaging.requirements.SpecifierSet` (e.g., `==1.0.0`, `~=2.0.0`,
  `>=1.0,<2.0`); starts with `refs/` (literal git ref); matches the
  branch-name regex `^[a-zA-Z0-9_./+-]+$`. PEP 440 specs MAY carry a
  monorepo path prefix (`subpackage/==1.0.0`); the prefix is stripped
  before SpecifierSet parsing.
- `canonical_url` on every `ProjectEntry` is compared via string equality to
  `canonicalize_repo_url(entry.url)`. Mismatch is
  `LockfileValidationError`.
- `path` and `path_in_repo` strings are checked for `\x00`, `\n`, `\t`; any
  occurrence raises `LockfileValidationError`.

Schema migration policy is in scope for T2; this task only handles
`schema_version == 1` and raises `LockfileSchemaError` for any other value
with the message "lockfile schema v<N> not supported by this mpm
version".

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 5
(Lockfile format and validation rules) and Section 4.7.1 (atomicity
contract for the lockfile writer).

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e3-f1-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E3-F1-S1-T2 | Schema migration policy | in-queue |
| E3-F3-S1-T1 | Lockfile state-matrix branching in `mpm install` | in-queue |
| E3-F3-S1-T2 | `--refresh-lock` flag | in-queue |
| E3-F3-S1-T3 | `--refresh-lock-source` flag | in-queue |
| E3-F3-S1-T4 | `--strict-lock` + `--strict-drift` flags | in-queue |
| E3-F3-S1-T5 | Conflict detection via `canonicalize_repo_url` | in-queue |
| E3-F3-S1-T6 | `<include>` cycle and diamond handling | in-queue |
| E3-F3-S1-T7 | Default `--lock-file` derivation from `--mpm-file` | in-queue |
| E3-F3-S1-T8 | HTTPS enforcement for `<remote>` URLs | in-queue |
| E3-F3-S1-T9 | Concurrency lock extension to `mpm add` / `mpm remove` | in-queue |
| E4-F1-S1-T1 | `mpm outdated` reads lockfile | in-queue |
| E4-F2-S1-T1 | `mpm why` reads lockfile | in-queue |
| E5-F1-S1-T1 | `mpm doctor` lockfile parity checks | in-queue |

### Approach

1. **TDD RED:** Author `tests/unit/test_lockfile.py` with parametrised
   cases for every validation rule listed in the Description. RED phase
   confirms every test fails against an absent `src/mpm_cli/core/lockfile.py`
   (`ImportError`).
2. **TDD RED (integration):** Author
   `tests/integration/test_lockfile_roundtrip.py` with a property-style
   case that constructs a `Lockfile` dataclass in memory, writes it to a
   `tmp_path`-scoped file, reads it back, and asserts deep equality. The
   roundtrip test exercises the atomic write + rename codepath against the
   real filesystem.
3. **TDD GREEN (dataclasses):** Author the `Lockfile`, `CatalogBlock`,
   `SourceEntry`, `IncludeEntry`, `ProjectEntry` dataclasses with the field
   types listed in the Description. Re-run the unit tests; the dataclass
   construction tests pass.
4. **TDD GREEN (reader):** Implement `read_lockfile(path)` using the
   stdlib `tomllib` module (Python 3.11+). Each validation rule is a small
   helper function under the module's private namespace
   (`_validate_resolved_sha`, `_validate_revision_spec`,
   `_validate_canonical_url`, `_validate_path_chars`). Each helper raises
   `LockfileValidationError` with the standard error shape (Section 4).
5. **TDD GREEN (writer):** Implement `write_lockfile(lockfile, path)`
   using `tomli_w` (already in pyproject.toml dependencies if present,
   otherwise added). Atomic write via `tempfile.NamedTemporaryFile(dir=path.parent, delete=False)`
   followed by `os.replace(tmp_path, path)`. The temp file is closed
   before rename; the temp file's permissions are set to match the
   destination's expected mode before rename.
6. **TDD GREEN (constants):** Add `MPM_LOCK_FILE = "MPM_LOCK_FILE"`
   to `src/mpm_cli/constants.py` adjacent to the existing
   `MPM_MPM_FILE`.
7. **TDD REFACTOR:** Extract any shared validation helpers (e.g., the
   resolved-SHA regex check) into the module's private namespace if used
   by more than one validator.
8. **Docs:** Update `docs/lockfile.md` (NEW per spec Section 8) with the
   v1 schema definition, the validation rules, and a worked example
   matching the spec Section 5 listing.
9. **Verify:** `uv run pytest tests/unit/test_lockfile.py tests/integration/test_lockfile_roundtrip.py -v`,
   then the full suite: `uv run pytest tests/ -v && uv run ruff check src tests && uv run ruff format --check src tests && uv run mypy src && uv run bandit -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 5 (Lockfile format and validation rules).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.7.1 (Install engine details -- atomic lockfile write contract).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 3 (Existing primitives table -- `canonicalize_repo_url` consumed by the project-row validator).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E1-F2-S1-T1 | Add `core/url.py::canonicalize_repo_url` | in-queue |
| E2-F4-S1-T1 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 `read_lockfile(path)` parses a valid schema-v1 TOML file into the `Lockfile` dataclass tree with every field populated, including nested `[[sources.includes]]` chains at unbounded depth.
- [ ] AC-FUNC-002 `read_lockfile` raises `LockfileValidationError` when any `resolved_sha` is not exactly 40 OR 64 lowercase hex characters; the error message names the offending field path (e.g., `sources[0].projects[2].resolved_sha`) and the bad value.
- [ ] AC-FUNC-003 `read_lockfile` raises `LockfileValidationError` when any `revision_spec` fails all three accept rules (PEP 440 SpecifierSet parse, literal `refs/...` prefix, branch-charset regex `^[a-zA-Z0-9_./+-]+$`); the monorepo path prefix (`subpackage/==1.0.0`) is stripped before SpecifierSet parsing and is accepted when the stripped suffix is a valid SpecifierSet.
- [ ] AC-FUNC-004 `read_lockfile` raises `LockfileValidationError` when any `ProjectEntry.canonical_url` does not equal `canonicalize_repo_url(entry.url)`; the error includes both the recorded `canonical_url` and the computed value so the operator can see the mismatch.
- [ ] AC-FUNC-005 `read_lockfile` raises `LockfileValidationError` when any `path` or `path_in_repo` field contains `\x00`, `\n`, or `\t`; the error names the bad character (by codepoint) and the field path.
- [ ] AC-FUNC-006 `write_lockfile(lockfile, path)` is atomic: the writer creates a temp file in `path.parent`, writes the serialised TOML, fsyncs, then `os.replace`s into the destination. A reader observing the destination either sees the prior file's full content or the new file's full content, never a truncated file.
- [ ] AC-FUNC-007 `write_lockfile` followed by `read_lockfile` round-trips deep-equal for an arbitrary `Lockfile` dataclass instance constructed in the test (the roundtrip is asserted via dataclass equality, not field-by-field manual comparison).
- [ ] AC-FUNC-008 The constant `MPM_LOCK_FILE = "MPM_LOCK_FILE"` exists in `src/mpm_cli/constants.py` and is exported (importable as `from mpm_cli.constants import MPM_LOCK_FILE`).
- [ ] AC-FUNC-009 Unknown `schema_version` values (not 1) raise `LockfileSchemaError` with message `"lockfile schema v<N> not supported by this mpm version"`; the error is distinct from `LockfileValidationError` so T2's migration policy can dispatch on it.
- [ ] AC-TEST-001 Unit tests in `tests/unit/test_lockfile.py` parametrise every validation rule: `resolved_sha` shape (40 hex, 64 hex, uppercase rejected, mixed-case rejected, non-hex rejected); `revision_spec` accept set (each of the three accept branches plus the monorepo prefix); `canonical_url` mismatch; embedded NUL / newline / tab in `path` and `path_in_repo`; unknown `schema_version`.
- [ ] AC-TEST-002 Integration test in `tests/integration/test_lockfile_roundtrip.py` builds a `Lockfile` with at least two top-level sources, each with nested `[[sources.includes]]` chains 3 levels deep and at least 2 `[[sources.projects]]` rows, then asserts write-then-read deep equality on `tmp_path`.
- [ ] AC-CYCLE-001 End-to-end cycle: build the in-memory `Lockfile` instance from AC-TEST-002, call `write_lockfile`; assert the file on disk matches a known-good TOML fixture (literal-string comparison after stripping `generated_at`); call `read_lockfile` on the same file; assert the parsed object deep-equals the source object. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/lockfile.md` (NEW per spec Section 8) documents the v1 schema, every validation rule, and a worked example matching spec Section 5 listing.
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/core/lockfile.py` | New -- schema v1 dataclasses (`Lockfile`, `CatalogBlock`, `SourceEntry`, `IncludeEntry`, `ProjectEntry`), `read_lockfile`, `write_lockfile` (atomic), and the `LockfileSchemaError` + `LockfileValidationError` exception classes. |
| `src/mpm_cli/constants.py` | Update -- add `MPM_LOCK_FILE = "MPM_LOCK_FILE"` env-var constant adjacent to the existing `MPM_MPM_FILE`. |
| `tests/unit/test_lockfile.py` | New -- parametrised unit tests for every validation rule plus dataclass construction. |
| `tests/integration/test_lockfile_roundtrip.py` | New -- end-to-end write-then-read roundtrip on `tmp_path` with nested-includes fixture. |
| `docs/lockfile.md` | New -- v1 schema reference, validation rules, worked example. |
| `tests/unit/test_constants.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/constants.py` per docs/source-test-atomicity.md. |
## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on `src/mpm_cli/core/lockfile.py`.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] `docs/lockfile.md` updated in the same commit; no doc-drift between schema and reference.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E3-F1-S1-T1 manually blocked on E2-F4-S1-T1 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E2-F4-S1-T1
