# E9-F1-S1-T09: `mpm catalog --help` + `mpm catalog audit --help` snapshots

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e9-f1-s1-t09`

## Description

Add the `mpm catalog --help` (subcommand-group head) and the
`mpm catalog audit --help` (subcommand-group child) snapshots to the
snapshot harness landed by E9-F1-S1-T01. This is the only task in the
chain that contributes TWO fixture files and TWO `_HELP_CASES` rows
because the catalog command exposes a nested subparser-of-subparsers
shape from spec Section 14 (the `Manifest repo (catalog author)`
group lists `catalog audit` as the operator-facing entry; the bare
`catalog` form prints the group's subcommand listing).

New fixture file `tests/fixtures/help/mpm-catalog.txt`:

- Verbatim `mpm catalog --help` output. Argparse renders the
  subcommand-group listing (with `audit` as the sole entry today; the
  spec leaves room for future `catalog <verb>` subcommands per Section
  4.8's Catalog audit framework). Exit code 0; stderr empty.
- LF line endings; no trailing whitespace; preserve argparse's trailing
  newline; zero em-dash characters.

New fixture file `tests/fixtures/help/mpm-catalog-audit.txt`:

- Verbatim `mpm catalog audit --help` output. Covers the
  `--check <name>` filter flag (spec Section 4.8 lists the
  audit-check names: `metadata`, `tag-format`, `legacy-catalog-dir`,
  `duplicate-entry-names`, `pep440-warn`), the `--format text|json`
  option, and the standard Catalog source group with its
  no-lockfile-fallback note (audit reads the manifest repo, not a
  consumer's lockfile). Exit code 0; stderr empty.
- LF line endings; no trailing whitespace; preserve argparse's trailing
  newline; zero em-dash characters.

Update `tests/functional/test_help_snapshots.py`:

- Append TWO rows to `_HELP_CASES`, in this order, after the row added
  by E9-F1-S1-T08:
  1. `("mpm-catalog", ("catalog",), "mpm-catalog.txt")`
  2. `("mpm-catalog-audit", ("catalog", "audit"), "mpm-catalog-audit.txt")`
- The parent-then-child order mirrors how argparse renders the help
  hierarchy and makes the test-id ordering match the spec Section 14
  reading order.
- No other changes to the harness. The existing `_clean_env()`,
  `_FIXTURES_DIR`, and `test_help_snapshot` function (from T01) cover
  both cases unchanged.

This task UPDATES `tests/functional/test_help_snapshots.py` and the
prior task (E9-F1-S1-T08) also UPDATES the same file. Manifest
Conflict on this path is resolved by the dependency chain
(T01 <- T02 <- ... <- T11) per `workbench/docs/backlog-contract.md`
Manifest Conflict Rule plus the issue #145 N-claimant ordered-chain
DAG. The two fixture-file paths are unique to this task.

Spec source:
- `spec/mpm-list-add-lock-features-spec.md` Section 14 -- CLI
  `--help` reference, snapshot-tested, listing `mpm catalog --help`
  and `mpm catalog audit --help` per the per-command snapshot rule.
- Section 4.8 -- `mpm catalog audit` framework + `--check` names.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e9-f1-s1-t09` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E9-F1-S1-T10 | `mpm completion --help` snapshot | in-queue |

### Approach

1. **TDD RED (fixtures)**: Author BOTH fixture files
   (`tests/fixtures/help/mpm-catalog.txt` and
   `tests/fixtures/help/mpm-catalog-audit.txt`) with verbatim
   expected stdout. Use LF line endings; no trailing whitespace;
   preserve the trailing newline argparse emits.
2. **TDD RED (harness update)**: Append the two parametrise rows to
   `_HELP_CASES` (in parent-then-child order). Run
   `uv run pytest tests/functional/test_help_snapshots.py -v -k "mpm-catalog or mpm-catalog-audit"`
   and confirm both cases are collected. They should fail on
   byte-mismatch if either fixture has any drift from what argparse
   emits, or pass if both fixtures are exact.
3. **TDD GREEN**: Iterate on each fixture until both captured stdouts
   match byte-for-byte. No production-code edits in this task; the
   `catalog` subcommand-group head is the one registered by
   E5-F2-S1-T1 (the `catalog audit` framework lands the parent
   `catalog` subparser at registration time).
4. **TDD REFACTOR**: Confirm the two new rows are the ONLY changes to
   `_HELP_CASES` (no reordering of pre-existing rows). Confirm both
   fixture files are valid UTF-8 with LF line endings via `file
   --mime-encoding`.
5. **Integration**: Run the full functional suite:
   `uv run pytest tests/functional/test_help_snapshots.py -v`; confirm
   every previously-passing case still passes plus the two new ones.
6. **Docs**: Update `tests/fixtures/help/README.md` (created in T01) to
   list both new fixtures under the fixture-naming-convention table,
   including the explicit note that `mpm-catalog-audit.txt` covers
   the nested-subparser child and uses a hyphenated filename to mirror
   the argv path.
7. **Verify**: `uv run pytest tests/ -v && uv run ruff check src tests
   && uv run ruff format --check src tests && uv run mypy src && uv run
   bandit -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 14 (`mpm catalog --help` and `mpm catalog audit --help` per-command snapshot rule).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.8 (`mpm catalog audit` framework + `--check` names: `metadata`, `tag-format`, `legacy-catalog-dir`, `duplicate-entry-names`, `pep440-warn`).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E9-F1-S1-T08 | (prior snapshot task in chain -- harness ordering) | in-queue |
| E5-F2-S1-T1 | `mpm catalog audit` framework + --check parser | in-queue |

## Acceptance Criteria

- [ ] AC-FUNC-001 New fixture `tests/fixtures/help/mpm-catalog.txt` exists and contains the verbatim `mpm catalog --help` output as rendered by the catalog subcommand-group head (registered alongside the `catalog audit` framework in E5-F2-S1-T1).
- [ ] AC-FUNC-002 New fixture `tests/fixtures/help/mpm-catalog-audit.txt` exists and contains the verbatim `mpm catalog audit --help` output, including the `--check <name>` flag listing every name (`metadata`, `tag-format`, `legacy-catalog-dir`, `duplicate-entry-names`, `pep440-warn`), the `--format text|json` option, and the standard Catalog source group.
- [ ] AC-FUNC-003 Both fixtures use LF line endings, have no trailing whitespace on non-empty lines, preserve the trailing newline argparse emits, and contain zero em-dash characters (U+2014).
- [ ] AC-FUNC-004 `tests/functional/test_help_snapshots.py`'s `_HELP_CASES` list contains exactly two new rows added by this task, appended after the row added by E9-F1-S1-T08, in parent-then-child order: `("mpm-catalog", ("catalog",), "mpm-catalog.txt")` followed by `("mpm-catalog-audit", ("catalog", "audit"), "mpm-catalog-audit.txt")`.
- [ ] AC-FUNC-005 No pre-existing `_HELP_CASES` rows are reordered, modified, or removed by this task; the only diff in the harness file is the two appended rows.
- [ ] AC-FUNC-006 The existing `_clean_env()`, `_FIXTURES_DIR`, and `test_help_snapshot` function (from T01) cover both cases unchanged; this task does NOT introduce a second test function or a duplicate environment helper.
- [ ] AC-TEST-001 The functional snapshot test passes for the `mpm-catalog` case: `uv run pytest tests/functional/test_help_snapshots.py -v -k mpm-catalog` exits zero.
- [ ] AC-TEST-002 The functional snapshot test passes for the `mpm-catalog-audit` case: `uv run pytest tests/functional/test_help_snapshots.py -v -k mpm-catalog-audit` exits zero.
- [ ] AC-TEST-003 The full snapshot suite still passes: `uv run pytest tests/functional/test_help_snapshots.py -v` exits zero and reports every previously-added case PLUS both new ones as passing.
- [ ] AC-CYCLE-001 End-to-end cycle (parent): run `subprocess.run([sys.executable, "-m", "mpm_cli", "catalog", "--help"], capture_output=True, env=_clean_env())`; assert captured stdout bytes exactly equal the bytes of `tests/fixtures/help/mpm-catalog.txt` read via `read_bytes()`; assert exit code 0; assert stderr empty. Recorded in `## TDD Cycle Log` with the exact captured stdout length and the first 200 bytes for audit.
- [ ] AC-CYCLE-002 End-to-end cycle (child): run `subprocess.run([sys.executable, "-m", "mpm_cli", "catalog", "audit", "--help"], capture_output=True, env=_clean_env())`; assert captured stdout bytes exactly equal the bytes of `tests/fixtures/help/mpm-catalog-audit.txt` read via `read_bytes()`; assert exit code 0; assert stderr empty. Recorded in `## TDD Cycle Log` with the exact captured stdout length and the first 200 bytes for audit.
- [ ] AC-DOC-001 `tests/fixtures/help/README.md` is updated in the same commit to list both `mpm-catalog.txt` and `mpm-catalog-audit.txt` in the fixture-naming-convention table, with the explicit note that the hyphenated filename mirrors the argv path for nested subparsers.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `tests/functional/test_help_snapshots.py` | Update -- append two rows to `_HELP_CASES`: `("mpm-catalog", ("catalog",), "mpm-catalog.txt")` and `("mpm-catalog-audit", ("catalog", "audit"), "mpm-catalog-audit.txt")`. No other edits. |
| `tests/fixtures/help/mpm-catalog.txt` | New -- verbatim `mpm catalog --help` output (subcommand-group head). |
| `tests/fixtures/help/mpm-catalog-audit.txt` | New -- verbatim `mpm catalog audit --help` output (subcommand-group child; covers `--check`, `--format`, Catalog source group). |
| `tests/fixtures/help/README.md` | Update -- add both new fixtures to the fixture-naming-convention table with the nested-subparser note. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] Only files in the Changes Manifest are staged with `git add`.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments
