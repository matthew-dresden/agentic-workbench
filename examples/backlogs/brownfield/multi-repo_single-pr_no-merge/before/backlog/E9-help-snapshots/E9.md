# E9: `--help` snapshot tests (per command)

## Status: in-queue

## Description

Lock the verbatim `--help` output of every `mpm` subcommand behind
byte-for-byte snapshot tests. Spec Section 14 declares the help text as
contract: each command's `--help` lives in `tests/fixtures/help/` and is
asserted byte-for-byte against the captured stdout of the actual
`python -m mpm_cli <command> --help` invocation.

This epic ships:

- A single parametrised snapshot harness at
  `tests/functional/test_help_snapshots.py` that drives every command's
  `--help` through `subprocess.run` and diffs the captured stdout to the
  matching fixture file.
- One fixture file per command under `tests/fixtures/help/`:
  - `mpm-toplevel.txt` -- `mpm --help`
  - `mpm-list.txt` -- `mpm list --help`
  - `mpm-add.txt` -- `mpm add --help`
  - `mpm-remove.txt` -- `mpm remove --help`
  - `mpm-outdated.txt` -- `mpm outdated --help`
  - `mpm-why.txt` -- `mpm why --help`
  - `mpm-install.txt` -- `mpm install --help`
  - `mpm-doctor.txt` -- `mpm doctor --help`
  - `mpm-catalog.txt` -- `mpm catalog --help`
  - `mpm-catalog-audit.txt` -- `mpm catalog audit --help`
  - `mpm-completion.txt` -- `mpm completion --help`
  - `mpm-bootstrap.txt` -- `mpm bootstrap --help` (DEPRECATED prefix)

Snapshot tests are byte-compared. Any drift in argparse-rendered help
text (added flag, reworded description, reordered option group) fails
the test, so the contract is preserved deliberately rather than by
accident.

E9 is thin: per-command snapshot fixtures, no production code. Each
upstream command epic (E2-E7) lands the implementation; E9 freezes the
resulting help text against the spec's Section 14 reference.

Spec source:
- `spec/mpm-list-add-lock-features-spec.md` Section 14 (CLI `--help`
  reference, snapshot-tested) -- the full normative help-text contract.
- Section 14 also calls out the `mpm bootstrap --help` DEPRECATED
  prefix requirement, mirrored in `E6-F1-S1-T3`.

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E2-F2-S1-T1 | `mpm list` default output + entry-name index | in-queue |
| E2-F4-S1-T1 | `mpm add` core -- triple writing from `<catalog-metadata>` | in-queue |
| E2-F5-S1-T1 | `mpm remove` core (accepts source-name OR entry-name) | in-queue |
| E3-F3-S1-T1 | Lockfile state-matrix branching in `mpm install` | in-queue |
| E4-F1-S1-T1 | `mpm outdated` core (tag-based comparison logic) | in-queue |
| E4-F2-S1-T1 | `mpm why` core chain-walker (text format, URL-match) | in-queue |
| E5-F1-S1-T1 | `mpm doctor` consistency checks 1-5 | in-queue |
| E5-F2-S1-T1 | `mpm catalog audit` framework + --check parser | in-queue |
| E6-F1-S1-T1 | Shim core: WARN + exit 3 + zero boundary calls | in-queue |
| E6-F1-S1-T3 | `--help` DEPRECATED prefix and retained discoverability | in-queue |
| E7-F1-S1-T1 | `mpm completion` subcommand wiring (shtab integration) | in-queue |

## Target Repository

- **Repo:** `matthew-dresden/mpm`
