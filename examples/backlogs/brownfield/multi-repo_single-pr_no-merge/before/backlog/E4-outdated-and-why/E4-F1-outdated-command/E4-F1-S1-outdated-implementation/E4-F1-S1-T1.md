# E4-F1-S1-T1: `mpm outdated` core (tag-based comparison logic)

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e4-f1-s1-t1`

## Description

Implement the core `mpm outdated` command surface as a new module
`src/mpm_cli/commands/outdated.py`. The command:

1. **Resolves the catalog** (required; no lockfile fallback for `outdated` per spec Section 4.4
   behaviour 1). Catalog source precedence: `--catalog-source` CLI flag, then
   `MPM_CATALOG_SOURCE` env var. If neither is set, hard error with the standard
   `ERROR: <one-line summary>` + remediation shape (operator must pass `--catalog-source` or
   export `MPM_CATALOG_SOURCE`).
2. **Reads the `.mpm` file** at `--mpm-file` (default from `MPM_MPM_FILE`,
   constant default `./.mpm`).
3. **Reads `.mpm.lock`** at `--lock-file` if present (default derived from `--mpm-file`). The
   lockfile is OPTIONAL for `outdated`: when present, it provides the `current` resolved SHA; when
   absent, the command live-resolves against the catalog.
4. **For each `MPM_SOURCE_<name>_*` block** (parsed via the existing
   `core/mpmenv.py::parse_mpmenv`), computes three columns:
   - **current** -- from the lockfile when present (the locked SHA), else live-resolved via the
     resolver (calls `version.py::resolve_version` with the source's URL + REVISION).
   - **latest-matching-spec** -- the latest ref under the source's prefix that satisfies the
     source's REVISION spec, via `version.py::_resolve_constraint_from_tags`.
   - **latest-available** -- the latest ref under the source's prefix ignoring the spec
     (equivalent to a `*` constraint applied to the same prefix).
5. **Emits one row per source** in `name | current | latest-matching-spec | latest-available |
   upgrade-type` format. Default format `table` (controlled by `MPM_OUTDATED_FORMAT`); JSON
   output is added in E4-F1-S1-T4. The `upgrade-type` column reads `none`, `patch`, `minor`,
   `major`, or `prerelease`, derived by comparing `current` to `latest-matching-spec` via
   `packaging.version.Version`. Branch-pinned sources are added in E4-F1-S1-T2 (this task keeps
   them resolving as if tag-pinned; the dedicated branch logic lands in T2). For this task, every
   row in the test fixtures uses tag-pinned sources only.

This task scopes ONLY to tag-pinned sources. Branch-pinned columns (T2), `--fail-on-upgrade` (T3),
and `--format json` (T4) are scoped to follow-up tasks within the same story. T1 lands:

- The `outdated.py` module with a `run(args)`-shaped function and a registration entry point.
- The subcommand wiring in `src/mpm_cli/cli.py` (under the existing argparse subparsers registry,
  reusing the shared `--catalog-source` / `--mpm-file` / `--lock-file` flag definitions from
  `core/cli_args.py`).
- The `MPM_OUTDATED_FORMAT` constant in `src/mpm_cli/constants.py` (default value `"table"`)
  and the `--format` flag wiring with `choices=("table",)` for this task (extended to
  `("table", "json")` in T4).
- Exit code 0 always for this task (T3 introduces `--fail-on-upgrade`).

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 4.4 (behaviour 1-3) and Section 7
(`MPM_OUTDATED_FORMAT`, `--catalog-source`, `--mpm-file`, `--lock-file`, `--format`).

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e4-f1-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E4-F1-S1-T2 | Branch-pinned source columns: drift detection and 12-char SHA truncation | in-queue |
| E4-F1-S1-T3 | `mpm outdated --fail-on-upgrade` flag | in-queue |
| E4-F1-S1-T4 | `mpm outdated --format json` output | in-queue |

### Approach

1. **TDD RED (unit):** Author `tests/unit/test_outdated.py` with parametrized cases for:
   (a) a single tag-pinned source whose lockfile SHA equals `latest-matching-spec` (`upgrade-type`
   = `none`); (b) a source where `latest-matching-spec > current` by a patch bump; (c) a source
   where `latest-available > latest-matching-spec` (operator could upgrade if the spec relaxed);
   (d) `.mpm` missing -> hard error; (e) catalog source absent (no flag, no env) -> hard error;
   (f) zero PEP 440-parseable tags under prefix -> loud error inherited from E1-F1-S1-T2. Run
   `uv run pytest tests/unit/test_outdated.py -v`; confirm RED.
2. **TDD GREEN (unit):** Implement `src/mpm_cli/commands/outdated.py` with a `run(args)` entry
   point that reads `.mpm`, optionally reads `.mpm.lock`, iterates sources, computes the
   three columns + upgrade-type for tag-pinned sources, and writes rows via a deterministic table
   formatter to stdout. Add `MPM_OUTDATED_FORMAT = "table"` to `src/mpm_cli/constants.py`. Wire
   the subcommand in `src/mpm_cli/cli.py` (`outdated` subparser, reusing shared flag defs from
   `core/cli_args.py`). Re-run unit tests; confirm GREEN.
3. **TDD RED (integration):** Author `tests/integration/test_outdated.py` with an end-to-end
   fixture: a local fixture git server (file:// remote) hosting a manifest repo with one
   `*-marketplace.xml` (one `<catalog-metadata>` + one `<project>`), a fixture project repo with
   tags `1.0.0`, `1.0.1`, `1.1.0`, and a workspace with a `.mpm` referencing the source at spec
   `>=1.0.0,<1.1`. Assert `mpm outdated --catalog-source file://<manifest>@HEAD` prints exactly
   one row with `current=1.0.0` (or the resolved tag), `latest-matching-spec=1.0.1`,
   `latest-available=1.1.0`, `upgrade-type=patch`. Run integration tests; confirm RED.
4. **TDD GREEN (integration):** Verify the unit-tier implementation suffices; otherwise extend
   `outdated.py` until the integration test passes. Re-run integration tests; confirm GREEN.
5. **TDD REFACTOR:** Factor the row-construction logic into a private helper
   `_build_row(source, lock_entry, catalog)` returning a typed dataclass; the table formatter
   reads the dataclass. Keep the function signature minimal so T2/T3/T4 can extend it without
   re-shaping. No public-API changes.
6. **Docs:** Update `docs/cli-reference.md` (or the mpm equivalent at `docs/commands.md` if the
   repo uses that filename -- the executor must select the existing per-command reference doc and
   add an `outdated` section mirroring the spec's Section 4.4 behaviour list). Document
   `MPM_OUTDATED_FORMAT` in `docs/configuration.md` alongside the other `MPM_*_FORMAT` env
   vars.
7. **Verify:** Run the full suite locally:
   `uv run pytest tests/ -v && uv run ruff check src tests && uv run ruff format --check src tests && uv run mypy src && uv run bandit -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.4 behaviour 1-3 (catalog required, `.mpm` read, three-column row).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 0.2 (`outdated` exit-code policy -- default 0; this task implements the default, T3 adds `--fail-on-upgrade`).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 7 (`MPM_OUTDATED_FORMAT` env var; shared `--catalog-source` / `--mpm-file` / `--lock-file` / `--format` flags).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E1-F1-S1-T2 | Loud-error in `_resolve_constraint_from_tags` when zero PEP 440 tags remain | in-queue |
| E1-F4-S1-T1 | Move `--catalog-source` flag definition to `core/cli_args.py` | in-queue |
| E2-F1-S1-T1 | `_parse_catalog_metadata` helper | in-queue |
| E2-F3-S1-T1 | `derive_source_name` helper | in-queue |
| E3-F1-S1-T1 | Lockfile reader | in-queue |
| E3-F2-S1-T1 | `mpm_hash` lockfile-consistency hash | in-queue |
| E2-F5-S1-T1 | (auto) | proposed |
| E3-F3-S1-T8 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 `mpm outdated --catalog-source <url>@<ref> --mpm-file <path>` reads the `.mpm` file at the given path, resolves the catalog at the given source, and emits one row per `MPM_SOURCE_<name>_*` block in the file.
- [ ] AC-FUNC-002 Each row contains exactly five columns in this order: `name | current | latest-matching-spec | latest-available | upgrade-type`.
- [ ] AC-FUNC-003 The `current` column reads the locked SHA from `.mpm.lock` when present at `--lock-file` (default derived from `--mpm-file`); when the lockfile is absent, the command live-resolves the current version against the catalog and reports the resolved ref.
- [ ] AC-FUNC-004 The `latest-matching-spec` column reads the latest ref under the source's prefix that satisfies the source REVISION spec (via `version.py::_resolve_constraint_from_tags`).
- [ ] AC-FUNC-005 The `latest-available` column reads the latest ref under the source's prefix ignoring the spec (equivalent to evaluating the prefix against constraint `*`).
- [ ] AC-FUNC-006 The `upgrade-type` column reads `none` when `current == latest-matching-spec`; otherwise one of `patch`, `minor`, `major`, or `prerelease` derived by comparing `current` vs `latest-matching-spec` via `packaging.version.Version` component diffs.
- [ ] AC-FUNC-007 Default `--format` is `table`, sourced from `MPM_OUTDATED_FORMAT` env var (constant default `"table"` in `src/mpm_cli/constants.py`).
- [ ] AC-FUNC-008 Exit code is 0 always for this task (regardless of upgrade availability) per spec Section 0.2; `--fail-on-upgrade` is OUT OF SCOPE for T1 and added in T3.
- [ ] AC-FUNC-009 Absent catalog source (neither `--catalog-source` nor `MPM_CATALOG_SOURCE` set) raises a hard error `ERROR: no catalog source configured` with remediation pointing to the two ways to provide one. Exit code non-zero.
- [ ] AC-FUNC-010 Absent `.mpm` at `--mpm-file` raises a hard error naming the missing path with the standard `ERROR: <summary>` shape. Exit code non-zero.
- [ ] AC-FUNC-011 Zero PEP 440-parseable tags under a source's prefix propagates the loud error from E1-F1-S1-T2 verbatim (listing the non-PEP-440 tag names skipped and the `mpm catalog audit --check tag-format` remediation pointer).
- [ ] AC-TEST-001 Unit tests in `tests/unit/test_outdated.py` cover at minimum: row construction for `upgrade-type = none`, `patch`, `minor`, `major`, `prerelease`; missing-catalog-source error; missing-`.mpm` error; zero-parseable-tags loud error; locked-SHA-from-lockfile path; live-resolve-when-no-lockfile path. Tests are authored BEFORE the implementation (TDD RED then GREEN).
- [ ] AC-TEST-002 Integration tests in `tests/integration/test_outdated.py` build a real fixture git server (file:// remote) with at least one manifest repo + one project repo with tags `1.0.0`, `1.0.1`, `1.1.0`; assert the full `name | current | ... | upgrade-type` row content for the source.
- [ ] AC-TEST-003 The integration test exercises the command via its public CLI entry point (subprocess invocation of `python -m mpm_cli outdated ...` or the installed `mpm outdated` console script), not via direct function call.
- [ ] AC-CYCLE-001 End-to-end cycle: build a fixture catalog at `file://<tmp>/catalog@HEAD` with one source `foo` whose `<project>` has tags `1.0.0`, `1.0.1`, `1.1.0`; write a `.mpm` referencing `MPM_SOURCE_FOO_REVISION=>=1.0.0,<1.1`; write a `.mpm.lock` pinning `foo` to the SHA of tag `1.0.0`; invoke `mpm outdated --catalog-source file://<tmp>/catalog@HEAD --mpm-file <tmp>/.mpm --lock-file <tmp>/.mpm.lock`; assert stdout contains exactly one row with `current=1.0.0`, `latest-matching-spec=1.0.1`, `latest-available=1.1.0`, `upgrade-type=patch`; assert exit code 0. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 The mpm CLI reference doc (`docs/cli-reference.md` or the existing per-command reference file the repo uses) is updated in the same commit to add an `outdated` section mirroring spec Section 4.4 behaviour list.
- [ ] AC-DOC-002 `docs/configuration.md` (or the existing env-var reference doc) documents `MPM_OUTDATED_FORMAT` with constant default `"table"`.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/commands/outdated.py` | New -- core `mpm outdated` command surface: catalog read, `.mpm` parse, per-source row construction for tag-pinned sources, default `table` formatter, registration entry point. |
| `src/mpm_cli/cli.py` | Update -- register the `outdated` subparser under the existing subcommand registry, reusing shared `--catalog-source` / `--mpm-file` / `--lock-file` / `--format` flag definitions from `core/cli_args.py`. |
| `src/mpm_cli/constants.py` | Update -- add `MPM_OUTDATED_FORMAT` env var name + constant default `"table"`. |
| `tests/unit/test_outdated.py` | New -- parametrized unit tests for row construction (every `upgrade-type` value), missing-catalog-source error, missing-`.mpm` error, zero-parseable-tags loud error propagation, locked-SHA-from-lockfile path, live-resolve-when-no-lockfile path. |
| `tests/integration/test_outdated.py` | New -- end-to-end integration test that builds a real file:// fixture catalog + project repo and invokes the `mpm outdated` CLI via subprocess to assert row content and exit code. |
| `docs/cli-reference.md` | Update -- add `outdated` section mirroring spec Section 4.4 behaviour list. (Executor selects the existing per-command reference doc; if the repo uses a different filename, update that file and document the choice in the TDD Cycle Log.) |
| `docs/configuration.md` | Update -- document `MPM_OUTDATED_FORMAT` env var alongside the other `MPM_*_FORMAT` entries. |
| `tests/unit/test_cli.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/cli.py` per docs/source-test-atomicity.md. |
| `tests/unit/test_constants.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/constants.py` per docs/source-test-atomicity.md. |
## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on `src/mpm_cli/commands/outdated.py` for new + modified lines.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] Documentation updated in the same commit; no doc-drift between code and docs.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E4-F1-S1-T1 manually blocked on E1-F4-S1-T1 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E1-F4-S1-T1

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E4-F1-S1-T1 manually blocked on E2-F5-S1-T1 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E2-F5-S1-T1

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E4-F1-S1-T1 manually blocked on E3-F3-S1-T8 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E3-F3-S1-T8
