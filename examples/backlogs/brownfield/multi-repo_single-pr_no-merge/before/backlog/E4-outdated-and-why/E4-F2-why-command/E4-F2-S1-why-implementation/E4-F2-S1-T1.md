# E4-F2-S1-T1: `mpm why` core chain-walker (text format, URL-match)

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e4-f2-s1-t1`

## Description

Implement the core `mpm why` command as a new module `src/mpm_cli/commands/why.py`. The command:

1. **Reads `.mpm`** at `--mpm-file` (default `MPM_MPM_FILE` = `./.mpm`).
2. **Resolves the full dependency tree**:
   - If `.mpm.lock` exists at `--lock-file` (default derived from `--mpm-file`), the tree is
     read directly from the lockfile -- every node already has its resolved SHA. This is the
     reproducible path; no network calls needed.
   - Otherwise, live-resolve the tree against the catalog (which requires a catalog source via
     `--catalog-source` or `MPM_CATALOG_SOURCE`). Live-resolution uses the existing tree-walker
     from `mpm install`'s resolution stage (every `MPM_SOURCE_*` block -> `<catalog-metadata>`
     entry -> `<include>` / `<project>` graph). Each node carries `{kind, name, ref, sha, url}`.
3. **Locates the requested node**: T1 implements ONLY the URL-match category (spec Section 4.5
   step 2 item (a)): canonicalize the argument via `core/url.py::canonicalize_repo_url` and look
   for a `<project>` node whose canonicalized URL equals the canonicalized argument. (XML-path
   matching and source-name matching arrive in E4-F2-S1-T2 alongside ambiguity detection;
   closest-match suggestions arrive in T3; JSON output in T4.)
4. **Walks every chain ending at the requested node** and prints one line per chain to stdout:

   ```
   <top-level-source> -> <xml-manifest-path>@<resolved-sha> -> ... -> <project>@<resolved-sha>
   ```

   The walker is DFS-based: for each top-level `MPM_SOURCE_*` source, perform a depth-first
   traversal of the resolved tree, collecting ancestor paths that reach the requested project URL.
   Each ancestor path becomes one printed chain.
5. **Default format** is `text` (controlled by `MPM_WHY_FORMAT`). The `--format` argparse flag
   has `choices=("text",)` for this task; T4 extends to `("text", "json")`.

This task scopes ONLY to:

- URL-match-by-canonicalized-equality (the most common and least ambiguous case);
- chain-walking and text printing;
- subcommand registration in `cli.py`;
- the `MPM_WHY_FORMAT = "text"` constant.

Not-found in T1 is a hard error with the standard `ERROR: <name-or-url> not found in resolved
tree` message. The closest-match Levenshtein suggestion lands in T3. Ambiguity errors land in T2.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 4.5 behaviour steps 1, 2 (item a),
4 (text format). Section 7 for `MPM_WHY_FORMAT` env-var.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e4-f2-s1-t1` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E4-F2-S1-T2 | Ambiguity detection across name/URL/XML-path categories | in-queue |
| E4-F2-S1-T3 | Levenshtein closest-match suggestion on not-found | in-queue |
| E4-F2-S1-T4 | `mpm why --format json` output | in-queue |

### Approach

1. **TDD RED (unit):** Author `tests/unit/test_why.py` with parametrized cases for: (a) single-
   chain tree (one top-level source -> one `<project>` matching the argument URL) prints exactly
   one line; (b) multi-chain tree (one project reachable via two different `<include>` paths from
   the same top-level source) prints two lines, one per chain; (c) two top-level sources reaching
   the same project print two lines; (d) requested URL not in tree -> hard error with the standard
   "not found" message and non-zero exit code; (e) `.mpm` missing -> hard error; (f) live-
   resolve path requires catalog source, absent catalog -> hard error; (g) lockfile-present path
   reads SHAs directly from the lockfile and does NOT live-resolve; (h) URL canonicalization
   equivalence: `git@github.com:org/repo.git` and `https://github.com/org/repo` match the same
   `<project>`. Run; confirm RED.
2. **TDD GREEN (unit):** Implement `src/mpm_cli/commands/why.py` with a `run(args)` entry point.
   Add a tree-resolution helper that delegates to the lockfile reader when `.mpm.lock` is
   present, else to the live tree-walker. Add `_walk_chains(tree, target_url)` that performs DFS
   collection of every root-to-target path. Add `_render_text(chains)` that prints one chain per
   line in the spec format. Add `MPM_WHY_FORMAT = "text"` to `src/mpm_cli/constants.py`. Wire
   the subcommand in `src/mpm_cli/cli.py` (reusing shared flag defs from `core/cli_args.py`).
   Re-run unit tests; confirm GREEN.
3. **TDD RED (integration):** Author `tests/integration/test_why_chain_walker.py` with an end-to-
   end fixture: a real file:// fixture catalog with one manifest XML that `<include>`s a child
   XML which declares a `<project>`; a `.mpm` referencing the top-level source; a `.mpm.lock`
   pinning every resolved SHA. Invoke `mpm why <project-url>` via subprocess; assert stdout
   contains exactly one chain with the canonical arrow-separated format and that the SHAs match
   the lockfile values. Run; confirm RED.
4. **TDD GREEN (integration):** Verify the unit-tier implementation suffices; otherwise extend
   `why.py` until integration passes. Re-run.
5. **TDD REFACTOR:** Factor any URL-canonicalization equality check into a single helper so T2's
   added matching categories can reuse it cleanly. No public-API changes.
6. **Docs:** Update `docs/cli-reference.md` to add a `why` section mirroring spec Section 4.5
   behaviour steps 1, 2 (item a), 4. Document `MPM_WHY_FORMAT` in `docs/configuration.md`
   alongside the other `MPM_*_FORMAT` env vars.
7. **Verify:** `uv run pytest tests/ -v && uv run ruff check src tests && uv run ruff format --check src tests && uv run mypy src && uv run bandit -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.5 behaviour steps 1 (tree resolution), 2 item (a) (URL match via canonicalization), 4 (text chain format).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 7 (`MPM_WHY_FORMAT` env var, `--catalog-source` / `--mpm-file` / `--lock-file` / `--format` flags).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E1-F2-S1-T1 | `core/url.py::canonicalize_repo_url` helper | in-queue |
| E1-F4-S1-T1 | Move `--catalog-source` flag definition to `core/cli_args.py` | in-queue |
| E2-F1-S1-T1 | `_parse_catalog_metadata` helper | in-queue |
| E2-F3-S1-T1 | `derive_source_name` helper | in-queue |
| E3-F1-S1-T1 | Lockfile reader | in-queue |
| E4-F1-S1-T1 | (auto) | proposed |
| E4-F1-S1-T4 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 `mpm why <project-url> --mpm-file <path>` reads the `.mpm` at `<path>`, resolves the dependency tree (from `.mpm.lock` when present, else live-resolves against the catalog), and prints every chain reaching a `<project>` whose canonicalized URL equals the canonicalized `<project-url>` argument.
- [ ] AC-FUNC-002 Each printed chain follows exactly the format `<top-source> -> <xml-path>@<sha> -> ... -> <project>@<sha>`, one chain per stdout line.
- [ ] AC-FUNC-003 URL matching is canonicalization-equality based: `git@github.com:org/repo.git` and `https://github.com/org/repo.git/` both match a `<project>` whose `remote.fetch` + `name` canonicalizes to the same value, via `core/url.py::canonicalize_repo_url`.
- [ ] AC-FUNC-004 The lockfile path is preferred over live-resolve: when `.mpm.lock` exists, the tree is built from lockfile entries with no `git ls-remote` calls.
- [ ] AC-FUNC-005 The live-resolve path is used only when `.mpm.lock` is absent; it requires a catalog source (CLI flag or env var). Absent catalog source -> hard error.
- [ ] AC-FUNC-006 Default `--format` is `text`, sourced from `MPM_WHY_FORMAT` env var (constant default `"text"` in `src/mpm_cli/constants.py`). T1 restricts choices to `("text",)`; T4 extends to `("text", "json")`.
- [ ] AC-FUNC-007 Argument not found in the resolved tree -> hard error `ERROR: <arg> not found in resolved tree` with non-zero exit code. Closest-match suggestion is OUT OF SCOPE for T1 and added in T3.
- [ ] AC-FUNC-008 `.mpm` missing at `--mpm-file` -> hard error naming the missing path. Non-zero exit code.
- [ ] AC-TEST-001 Unit tests in `tests/unit/test_why.py` cover at minimum: single-chain, multi-chain-same-source, multi-chain-different-sources, not-found, missing-`.mpm`, missing-catalog-source-when-no-lockfile, lockfile-present-skips-live-resolve, URL-canonicalization-equality. Tests authored BEFORE the implementation (TDD RED then GREEN).
- [ ] AC-TEST-002 Integration test in `tests/integration/test_why_chain_walker.py` builds a real file:// fixture catalog with at least one `<include>` chain reaching a `<project>` and asserts the printed chains match expected SHAs from the fixture lockfile.
- [ ] AC-TEST-003 The integration test exercises the CLI via subprocess (`python -m mpm_cli why ...` or the installed console script), not via direct function call.
- [ ] AC-CYCLE-001 End-to-end cycle: build a fixture catalog where source `foo` includes `bar.xml` (ref) which declares `<project name="baz" remote="..." revision="...">`; write a `.mpm` referencing `foo`; write a `.mpm.lock` pinning every node; invoke `mpm why <baz-url>`; assert stdout contains exactly one line of the form `foo -> bar.xml (ref)@<sha> -> baz@<sha>` and that exit code is 0. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 The mpm CLI reference doc (`docs/cli-reference.md`) is updated in the same commit to add a `why` section mirroring spec Section 4.5 behaviour steps 1, 2 (item a), 4.
- [ ] AC-DOC-002 `docs/configuration.md` documents `MPM_WHY_FORMAT` with constant default `"text"`.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/commands/why.py` | New -- core `mpm why` command: `.mpm` read, lockfile-or-live tree resolution, URL-match-by-canonicalization, DFS chain enumeration, text formatter, registration entry point. |
| `src/mpm_cli/cli.py` | Update -- register the `why` subparser under the existing subcommand registry, reusing shared `--catalog-source` / `--mpm-file` / `--lock-file` / `--format` flag definitions from `core/cli_args.py`. |
| `src/mpm_cli/constants.py` | Update -- add `MPM_WHY_FORMAT` env var name + constant default `"text"`. |
| `tests/unit/test_why.py` | New -- parametrized unit tests for single-chain, multi-chain, not-found, missing-`.mpm`, missing-catalog-source, lockfile-present-skips-live, URL canonicalization equivalence. |
| `tests/integration/test_why_chain_walker.py` | New -- end-to-end integration test that builds a real file:// fixture catalog with an `<include>` chain and asserts chain output via subprocess. |
| `docs/cli-reference.md` | Update -- add `why` section mirroring spec Section 4.5 behaviour steps 1, 2 (item a), 4. |
| `docs/configuration.md` | Update -- document `MPM_WHY_FORMAT` env var alongside the other `MPM_*_FORMAT` entries. |
| `tests/unit/test_cli.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/cli.py` per docs/source-test-atomicity.md. |
| `tests/unit/test_constants.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/constants.py` per docs/source-test-atomicity.md. |
## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on `src/mpm_cli/commands/why.py` for new + modified lines.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] Documentation updated in the same commit; no doc-drift between code and docs.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E4-F2-S1-T1 manually blocked on E4-F1-S1-T1 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E4-F1-S1-T1

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E4-F2-S1-T1 manually blocked on E4-F1-S1-T4 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E4-F1-S1-T4
