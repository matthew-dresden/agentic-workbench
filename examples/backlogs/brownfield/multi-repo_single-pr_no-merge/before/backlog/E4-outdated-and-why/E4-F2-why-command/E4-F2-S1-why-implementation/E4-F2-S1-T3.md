# E4-F2-S1-T3: `mpm why` Levenshtein closest-match suggestion on not-found

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e4-f2-s1-t3`

## Description

Extend the not-found path of `mpm why` (introduced in E4-F2-S1-T1 and elaborated in T2) with a
Levenshtein-distance-bounded closest-match suggestion. Per spec Section 4.5 step 5:

- When the requested node is not in the tree across any of the three matching categories (URL,
  XML path, source name), the command emits a hard error to stderr that includes the top 3
  closest matches drawn from the union of source names + project URLs + XML paths in the resolved
  tree.
- Closest-match is computed via Levenshtein edit distance against the argument string. Only
  candidates with **edit distance <= 3** are eligible. The suggestion list is sorted ascending by
  distance and truncated to at most 3 entries.
- When zero candidates are within edit distance 3, the error message reports the not-found error
  with no suggestion list (the message states explicitly that no close matches were found,
  consistent with the standard `ERROR: <summary>` shape).

To keep the helper reusable and unit-testable in isolation, the Levenshtein implementation lands
in a dedicated module `src/mpm_cli/utils/levenshtein.py` with its matching
`tests/unit/test_levenshtein.py`. The helper exposes a single public function
`levenshtein_distance(a: str, b: str) -> int` returning the Damerau-free edit distance (insertion,
deletion, substitution; no transpositions, matching the spec's "Levenshtein" wording).

The closest-match suggester in `why.py` (call it `_suggest_closest_matches(argument, universe,
max_distance, top_n)`) accepts the universe of strings to compare against (the union of source
names, XML paths, and project URLs from the resolved tree), filters to `distance <= max_distance`,
sorts ascending by `(distance, value)` for deterministic output, and returns the top `top_n`.

The constants `MPM_WHY_SUGGEST_MAX_DISTANCE` (default 3) and `MPM_WHY_SUGGEST_TOP_N` (default
3) live in `src/mpm_cli/constants.py` so the thresholds are not hard-coded in source.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 4.5 step 5.

### Definition of Ready

- [ ] All dependency work units are `done`
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`)
- [ ] The branch `backlog/e4-f2-s1-t3` does not already exist (or is from a prior attempt)
- [ ] The spec and acceptance criteria are unambiguous -- no open questions

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E4-F2-S1-T4 | `mpm why --format json` output | in-queue |

### Approach

1. **TDD RED (unit, helper):** Author `tests/unit/test_levenshtein.py` with parametrized cases
   covering: identical strings -> 0; single insertion -> 1; single deletion -> 1; single
   substitution -> 1; empty-vs-string -> `len(string)`; mixed-case characters preserved (no
   normalization); unicode characters counted as single characters; longer-string examples with
   known distances ("kitten" vs "sitting" -> 3). Run; confirm RED.
2. **TDD GREEN (unit, helper):** Implement `src/mpm_cli/utils/levenshtein.py` with the standard
   dynamic-programming edit-distance algorithm. Function signature
   `levenshtein_distance(a: str, b: str) -> int`. No external dependencies. Re-run; confirm GREEN.
3. **TDD RED (unit, suggester):** Author `tests/unit/test_why_suggestions.py` with parametrized
   cases: (a) typo in source name (one substitution) -> exactly that source suggested; (b) typo
   in URL within distance threshold -> exactly that URL suggested; (c) typo in XML path within
   threshold -> exactly that path suggested; (d) multiple typos beyond threshold -> empty
   suggestion list -> error message reports no close matches; (e) more than 3 candidates within
   threshold -> exactly 3 suggested, sorted ascending by distance; (f) ties broken by lexicographic
   sort of the candidate value (deterministic). Run; confirm RED.
4. **TDD GREEN (unit, suggester):** Add `_suggest_closest_matches(argument, universe,
   max_distance, top_n)` to `why.py`. Wire it into the not-found path: when zero matches across
   all three categories, build the universe = source-names U project-URLs U XML-paths from the
   resolved tree, call the suggester, and append the suggestion list to the not-found error
   message (or note "no close matches found" when empty). Add `MPM_WHY_SUGGEST_MAX_DISTANCE`
   (default 3) and `MPM_WHY_SUGGEST_TOP_N` (default 3) to `src/mpm_cli/constants.py`. Re-run;
   confirm GREEN.
5. **TDD RED (integration):** Author `tests/integration/test_why_not_found_suggestion.py` with an
   end-to-end fixture: a `.mpm` with source `foo` and a manifest XML at `repo-specs/foo/foo.xml`
   plus a `<project>` at `https://github.com/org/foo.git`; invoke `mpm why fooo` (one-char
   typo) via subprocess; assert stderr contains the not-found error AND the three closest-match
   suggestions including `foo` (the source name). Invoke `mpm why xyzzy` (no close matches);
   assert the message reports zero close matches. Run; confirm RED.
6. **TDD GREEN (integration):** Verify the unit-tier implementation suffices; re-run.
7. **TDD REFACTOR:** If the universe-construction logic is duplicated between the matcher (T2) and
   the suggester (T3), extract a `_build_match_universe(tree, mpmenv)` helper returning a list
   of `(category, value, node_ref)` tuples that both code paths consume. No public-API changes.
8. **Docs:** Update the `why` section of `docs/cli-reference.md` to document the closest-match
   suggestion behaviour, including the bounded distance and top-3 cap. Document the two new
   `MPM_WHY_SUGGEST_*` env vars in `docs/configuration.md`.
9. **Verify:** `uv run pytest tests/ -v && uv run ruff check src tests && uv run ruff format --check src tests && uv run mypy src && uv run bandit -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.5 step 5 (Levenshtein closest-match suggestion on not-found; distance <= 3; top 3).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E4-F2-S1-T1 | `mpm why` core chain-walker (text format, URL-match) | in-queue |
| E4-F2-S1-T2 | (auto) | proposed |

## Acceptance Criteria

- [ ] AC-FUNC-001 `levenshtein_distance(a, b)` in `src/mpm_cli/utils/levenshtein.py` returns the standard Levenshtein edit distance (insertion, deletion, substitution; no transpositions); identical strings return 0; empty-string-vs-other returns the other string's length; the function is symmetric (`distance(a, b) == distance(b, a)`).
- [ ] AC-FUNC-002 When `mpm why <arg>` finds zero matches across all three categories AND at least one candidate in the universe has edit distance `<= MPM_WHY_SUGGEST_MAX_DISTANCE` (default 3) to the argument, the not-found stderr message includes a suggestion list of up to `MPM_WHY_SUGGEST_TOP_N` candidates (default 3), sorted ascending by distance and tie-broken by lexicographic order.
- [ ] AC-FUNC-003 When no candidate is within the distance threshold, the not-found message states explicitly that no close matches were found and does NOT list any suggestions.
- [ ] AC-FUNC-004 The candidate universe is the union of: every `MPM_SOURCE_<name>_*` key (in `derive_source_name`-normalized form), every `<include>` XML manifest path in the resolved tree, and every `<project>` URL in the resolved tree (in canonicalized form).
- [ ] AC-FUNC-005 The distance threshold and top-N count are sourced from `MPM_WHY_SUGGEST_MAX_DISTANCE` and `MPM_WHY_SUGGEST_TOP_N` env vars (constants in `src/mpm_cli/constants.py` with defaults 3 and 3); no inline literals.
- [ ] AC-FUNC-006 Successful matches (single or ambiguous) from T1/T2 are unaffected; the suggester runs only in the zero-match path.
- [ ] AC-TEST-001 Unit tests in `tests/unit/test_levenshtein.py` cover at minimum: identical strings -> 0, single insertion -> 1, single deletion -> 1, single substitution -> 1, empty vs non-empty -> len, mixed-case preserved (no normalization), `levenshtein("kitten", "sitting") == 3`, symmetry. Tests authored BEFORE the implementation (TDD RED then GREEN).
- [ ] AC-TEST-002 Unit tests in `tests/unit/test_why_suggestions.py` cover at minimum: typo within threshold matches source name, URL, or XML path; no candidate within threshold -> empty suggestion list; more than 3 candidates within threshold -> exactly 3 returned, sorted by (distance, value); env-var thresholds respected. Tests authored BEFORE the implementation (TDD RED then GREEN).
- [ ] AC-TEST-003 Integration test in `tests/integration/test_why_not_found_suggestion.py` exercises `mpm why <typo>` via subprocess and asserts the stderr contains both the not-found error and the expected suggestion list (or "no close matches" when applicable).
- [ ] AC-CYCLE-001 End-to-end cycle: build a fixture with source `foo`, XML path `repo-specs/bar/bar.xml` (ref), and project URL `https://github.com/org/baz.git`; invoke `mpm why fooo`; assert stderr includes a not-found message with `foo` among the top-3 suggestions; invoke `mpm why xyzzy`; assert stderr includes a not-found message reporting no close matches. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 The `why` section of `docs/cli-reference.md` is updated to document the closest-match suggestion behaviour, the bounded distance, and the top-3 cap.
- [ ] AC-DOC-002 `docs/configuration.md` documents `MPM_WHY_SUGGEST_MAX_DISTANCE` (default 3) and `MPM_WHY_SUGGEST_TOP_N` (default 3) env vars.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/utils/levenshtein.py` | New -- standalone `levenshtein_distance(a, b)` helper using the standard DP edit-distance algorithm. |
| `src/mpm_cli/commands/why.py` | Update -- add `_suggest_closest_matches(...)` and wire it into the zero-match path; append top-3 (or "no close matches") to the not-found error message. |
| `src/mpm_cli/constants.py` | Update -- add `MPM_WHY_SUGGEST_MAX_DISTANCE` (default 3) and `MPM_WHY_SUGGEST_TOP_N` (default 3). |
| `tests/unit/test_levenshtein.py` | New -- parametrized unit tests for the Levenshtein helper (identical, insertion, deletion, substitution, empty, mixed-case, `kitten`/`sitting`, symmetry). |
| `tests/unit/test_why_suggestions.py` | New -- parametrized unit tests for the suggester: typo-matches-source, typo-matches-URL, typo-matches-XML-path, no-candidates-within-threshold, more-than-3-candidates -> top-3 sorted, env-var thresholds. |
| `tests/integration/test_why_not_found_suggestion.py` | New -- end-to-end integration test that builds a real fixture catalog, invokes `mpm why <typo>` via subprocess, and asserts stderr contains both the not-found error and the expected suggestion list. |
| `docs/cli-reference.md` | Update -- document the closest-match suggestion behaviour in the `why` section. |
| `docs/configuration.md` | Update -- document `MPM_WHY_SUGGEST_MAX_DISTANCE` and `MPM_WHY_SUGGEST_TOP_N` env vars. |
| `tests/unit/test_constants.py` | New -- TDD-paired unit test covering the production change in `src/mpm_cli/constants.py` per docs/source-test-atomicity.md. |
## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes (vendored carve-out unchanged).
- [ ] `uv run bandit -r src -ll` passes (vendored carve-out unchanged).
- [ ] 100% coverage on `src/mpm_cli/utils/levenshtein.py` and `src/mpm_cli/commands/why.py` for new + modified lines.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] Documentation updated in the same commit; no doc-drift between code and docs.
- [ ] TDD Cycle Log captures the RED-GREEN-REFACTOR phases with exit codes for each `pytest` run.

## TDD Cycle Log

## Comments

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E4-F2-S1-T3 manually blocked on E4-F2-S1-T1 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E4-F2-S1-T1

[2026-05-11 17:21 UTC] [agent/operator] [WU_WIRED] E4-F2-S1-T3 manually blocked on E4-F2-S1-T2 via `workbench add-dep`. [BLOCKED_PENDING_PROPOSAL] E4-F2-S1-T2
