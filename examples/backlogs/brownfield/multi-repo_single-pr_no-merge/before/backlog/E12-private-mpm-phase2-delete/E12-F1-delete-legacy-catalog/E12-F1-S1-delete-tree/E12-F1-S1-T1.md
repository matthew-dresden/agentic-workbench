# E12-F1-S1-T1: Delete `catalog/` directory tree and remove internal references

## Status: in-queue

## Target Repository

- **Repo:** `example-org/private-mpm`
- **Branch:** `backlog/e12-f1-s1-t1`

## Description

Execute the Phase-2 operational migration for private-mpm
(spec Section 9b Phase 2): delete the legacy `catalog/` directory tree
in its entirety, and remove every internal-tooling reference to
`catalog/<name>/` paths in the same coordinated change.

Concretely:

1. **Enumerate the deletion set.** Run `git ls-files catalog/` to list
   every tracked file under the legacy tree. As of the spec's authoring,
   the enumeration returns these 10 paths (the executor re-runs the
   command at task time to capture any drift):

   - `catalog/workbench-backlog-builder/.mpm`
   - `catalog/workbench-backlog-builder/README.md`
   - `catalog/history/.mpm`
   - `catalog/history/README.md`
   - `catalog/immutable-audit-trail/.mpm`
   - `catalog/immutable-audit-trail/README.md`
   - `catalog/mpm/.mpm`
   - `catalog/mpm/mpm-readme.md`
   - `catalog/security-code-review/.mpm`
   - `catalog/security-code-review/mpm-readme.md`

2. **Enumerate the reference-scrub set.** Run `git grep -F 'catalog/'`
   from inside `private-mpm/` (equivalently, `git grep -F
   'catalog/' private-mpm/` from the workspace root). Filter
   matches:

   - Inside the deletion target itself: drop (these go away with the
     deletion).
   - Inside `CHANGELOG.md` history entries that describe a PRIOR
     deletion: drop (a forward history pointer is allowed).
   - Inside `.github/workflows/` Phase-2 CI guards that intentionally
     test for `catalog/` presence (the E10 guard): drop (the guard
     wants to see the path string to enforce absence).
   - Everything else: this is the scrub set. Each match is either
     deleted (if the surrounding doc was wholly about the legacy
     directory shape) or rewritten to point at the new XML source of
     truth (`repo-specs/**/*-marketplace.xml`).

3. **Delete.** `git rm -r catalog/` removes every tracked file under
   `catalog/`. The deletion is forward-only; the files remain in git
   history and can be retrieved via `git show HEAD~1:catalog/...` if
   ever needed.

4. **Scrub references.** For each path in the scrub set, edit the file
   to remove the reference. Stage every edited file with `git add
   <path>`.

5. **CHANGELOG entry.** If `CHANGELOG.md` exists in the repo root,
   append an entry under the appropriate version heading describing
   the Phase-2 deletion: enumerate the 5 deleted subdirectories,
   reference spec Section 9b Phase 2, and link to this work unit. If
   no `CHANGELOG.md` exists, the deletion is documented via the
   work-unit branch's commit message instead (the orchestrator's
   git-ops layer records the work-unit ID in the commit message
   automatically).

6. **Verify zero residue.** After staging, re-run:
   - `git ls-files catalog/` -- must return zero lines.
   - `git grep -F 'catalog/' -- ':!CHANGELOG.md' ':!.github/workflows/'` --
     must return zero lines (the path filters exclude allowed
     history pointers and the E10 CI guard).

7. **Verify mpm list still works.** Run `mpm list --catalog-source
   <repo-path-at-HEAD>` against the staged tree (where `<repo-path-at-
   HEAD>` is the local checkout's path with the deletion applied).
   Assert the command exits 0 and lists every expected entry. The
   expected set is 5 entries: `workbench-backlog-builder`, `history`,
   `immutable-audit-trail`, `security-code-review`, `builders-plugins`.
   Record the full stdout in `## TDD Cycle Log` as the AC-CYCLE-001
   evidence.

This task does NOT modify:

- `repo-specs/**/*-marketplace.xml` (E11's output; the new source of
  truth is read-only here).
- `.github/workflows/**` Phase-2 CI guards added by E10 (they already
  exist; this task makes their post-Phase-2 mode reachable but does
  not alter the workflow YAML).
- The mpm CLI source in `matthew-dresden/mpm` (cross-repo; E6
  scope).

The executor agent enumerates both sets (deletion and scrub) at task
time and acts on the live enumeration, not the snapshot above. If the
deletion enumeration drifts (e.g., a file was added to `catalog/`
between spec authoring and task execution), the executor extends the
deletion to include it. If the scrub enumeration is empty (every
internal reference was already removed during E10's doc-update
sweep), the scrub step is a no-op and AC-DOC-002 is satisfied by
inspection.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 9b
Phase 2 ("Delete `private-mpm/catalog/` in its entirety.
Remove any internal tooling references to `catalog/<name>/` paths.
Acceptance: `git ls-files catalog/` returns zero tracked files; CI
green; `mpm list --catalog-source <this-repo-at-HEAD>` still
returns every expected entry.").

### Definition of Ready

- [ ] All dependency work units are `done` (every E11 per-entry task plus the E10 CI / doc updates that own the `<catalog-metadata>` documentation surface).
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/private-mpm`.
- [ ] The `mpm` CLI is installed in the executor environment (the AC-CYCLE-001 evidence command requires it) at the version produced by the upstream mpm backlog (E1-E9 done).
- [ ] The branch `backlog/e12-f1-s1-t1` does not already exist on the remote (or is from a prior attempt that has been cleaned up).
- [ ] The spec and acceptance criteria are unambiguous -- no open questions.

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| (none) | This is the terminal task in the E12 chain; nothing depends on it. | -- |

### Approach

1. **Audit (deletion set).** From inside `private-mpm/`, run `git ls-files catalog/`. Capture the output to a local scratch list. Confirm the count matches what the spec snapshot recorded (10 files across 5 subdirectories) or, if it has drifted, document the drift in `## TDD Cycle Log` along with the live enumeration.
2. **Audit (scrub set).** From inside `private-mpm/`, run `git grep -F 'catalog/' -- ':!catalog/' ':!CHANGELOG.md' ':!.github/workflows/'`. Capture the output. If non-empty, each line names a file + line + match snippet; classify each as "delete the line", "delete the whole block", or "rewrite to point at `repo-specs/**/*-marketplace.xml`" and record the decision in `## TDD Cycle Log`.
3. **Delete.** Run `git rm -r catalog/`. Confirm `git status --short` shows every file as deleted (`D` flag).
4. **Scrub.** For each path in the scrub set, edit the file per the step-2 classification. Stage with explicit `git add <path>` per file.
5. **CHANGELOG.** If `CHANGELOG.md` exists at repo root, append an entry describing the Phase-2 deletion. Stage it with `git add CHANGELOG.md`. If no CHANGELOG exists, skip (the work-unit branch's commit message carries the audit trail).
6. **Verify zero residue.** Re-run both audit commands; assert both return zero lines. Capture exit codes in `## TDD Cycle Log`.
7. **Verify mpm list.** Run `mpm list --catalog-source <absolute-path-to-staged-repo>` (the staged tree is on disk under the executor's working directory; the `mpm list` command reads `repo-specs/**/*-marketplace.xml` from the checkout). Assert exit 0 and assert the output contains all 5 expected entry names. Capture the full stdout in `## TDD Cycle Log`.
8. **Final stage check.** Run `git status --short` and confirm the only staged paths are the deletions enumerated in step 1, the scrub edits from step 4, and (optionally) `CHANGELOG.md`. Nothing else.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 9b Phase 2 (single-task deletion gated on every Phase-1 task being done; acceptance is `git ls-files catalog/` returning zero plus `mpm list --catalog-source <this-repo-at-HEAD>` returning every expected entry).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 9a CI bullet 4 (the post-Phase-2 CI guard that fails the build if a new `catalog/<name>/` directory is added; lands in E10 and becomes hard-enforcing after this task).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 9b "Work-unit traceability" paragraph (one Phase-2 task at the end, gated on every Phase-1 task being done; task shape is homogeneous for the autonomous loop).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E11-F1-S1-T1 | Migrate `<catalog-metadata>` for workbench-backlog-builder | in-queue |
| E11-F2-S1-T1 | Migrate `<catalog-metadata>` for history | in-queue |
| E11-F3-S1-T1 | Migrate `<catalog-metadata>` for immutable-audit-trail | in-queue |
| E11-F4-S1-T1 | Migrate `<catalog-metadata>` for security-code-review | in-queue |
| E11-F5-S1-T1 | Migrate `<catalog-metadata>` for builders-plugins | in-queue |

## Acceptance Criteria

- [ ] AC-FUNC-001 Every file enumerated by `git ls-files catalog/` at task start has been removed via `git rm`; the deletion is forward-only (files remain in git history and can be retrieved via `git show <prior-commit>:catalog/<path>`).
- [ ] AC-FUNC-002 After staging, `git ls-files catalog/` returns zero lines (the tree is gone from the index).
- [ ] AC-FUNC-003 Every file enumerated by `git grep -F 'catalog/' -- ':!catalog/' ':!CHANGELOG.md' ':!.github/workflows/'` at task start has been edited to remove or rewrite the `catalog/<name>/ (ref)` (ref) reference; after staging, the same command returns zero lines.
- [ ] AC-FUNC-004 No file outside the enumerated deletion + scrub + (optional) CHANGELOG set is staged; verified by `git status --short` reporting only paths from the Changes Manifest.
- [ ] AC-FUNC-005 The 5 expected entries (`workbench-backlog-builder`, `history`, `immutable-audit-trail`, `security-code-review`, `builders-plugins`) are discoverable post-deletion via the entries' `*-marketplace.xml` files under `repo-specs/`; verified by AC-CYCLE-001's `mpm list` invocation.
- [ ] AC-FUNC-006 The legacy `catalog/mpm/` (ref) subdirectory is included in the deletion (it is a `catalog/<name>/ (ref)` (ref) directory like the others; the mpm self-bootstrap entry's bundled-catalog mirror is handled separately by `E6-F2-S1-T1` in the mpm repo, NOT here).
- [ ] AC-TEST-001 The pre-deletion audit commands (`git ls-files catalog/` and the filtered `git grep`) are recorded in `## TDD Cycle Log` with their exit codes and full output -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-TEST-002 The post-staging audit commands (same two) are recorded in `## TDD Cycle Log` with their exit codes and full output (both must return zero lines) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-CYCLE-001 End-to-end verification recorded in `## TDD Cycle Log`: (a) `git ls-files catalog/` returns zero lines (exit 0, empty stdout); (b) `mpm list --catalog-source <absolute-path-to-staged-repo>` exits 0 and stdout contains every expected entry name (`workbench-backlog-builder`, `history`, `immutable-audit-trail`, `security-code-review`, `builders-plugins`); both commands' exit codes and full stdout are captured.
- [ ] AC-DOC-001 The deletion is documented in `CHANGELOG.md` (if the repo has one) with an entry referencing this work unit ID and spec Section 9b Phase 2; if no `CHANGELOG.md` exists, this AC is satisfied by the work-unit branch's commit message, recorded by inspection in `## TDD Cycle Log`.
- [ ] AC-DOC-002 No internal-tooling reference to `catalog/<name>/ (ref)` (ref) paths remains in the repo; verified by `git grep -F 'catalog/' -- ':!CHANGELOG.md' ':!.github/workflows/'` returning zero lines (the path filters exclude the allowed CHANGELOG history pointer and the E10 CI guard that intentionally names the path string for absence-enforcement).
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced (the change is delete-only on docs / templates).
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-004 `mypy src` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green) -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files -- N/A for Markdown Tasks (no Python source authored)
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `private-mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `catalog/workbench-backlog-builder/.mpm` | Delete -- legacy per-entry `.mpm` template; superseded by `repo-specs/common/software-development/requirements/workbench-backlog-builder/workbench-backlog-builder-marketplace.xml`. |
| `catalog/workbench-backlog-builder/README.md` | Delete -- legacy author-facing README; content migrated to the entry's `<catalog-metadata><description>` by E11-F1-S1-T1. |
| `catalog/history/.mpm` | Delete -- legacy per-entry `.mpm` template; superseded by `repo-specs/common/claude-tools/history/claude-history-marketplace.xml`. |
| `catalog/history/README.md` | Delete -- legacy author-facing README; content migrated to the entry's `<catalog-metadata><description>` by E11-F2-S1-T1. |
| `catalog/immutable-audit-trail/.mpm` | Delete -- legacy per-entry `.mpm` template; superseded by `repo-specs/common/sdlc/general/pr-ready/immutable-audit-trail/immutable-audit-trail-marketplace.xml`. |
| `catalog/immutable-audit-trail/README.md` | Delete -- legacy author-facing README; content migrated to the entry's `<catalog-metadata><description>` by E11-F3-S1-T1. |
| `catalog/mpm/.mpm` | Delete -- legacy self-bootstrap template for the mpm entry; the bundled-catalog mirror in the mpm wheel is handled separately by `E6-F2-S1-T1` in the `matthew-dresden/mpm` repo. |
| `catalog/mpm/mpm-readme.md` | Delete -- legacy author-facing README for the mpm entry. |
| `catalog/security-code-review/.mpm` | Delete -- legacy per-entry `.mpm` template; superseded by `repo-specs/common/security-and-compliance/code-review/full-review-marketplace.xml`. |
| `catalog/security-code-review/mpm-readme.md` | Delete -- legacy author-facing README; content migrated to the entry's `<catalog-metadata><description>` by E11-F4-S1-T1. |
| `CHANGELOG.md` | Update -- append an entry under the next-release heading describing the Phase-2 deletion, referencing this work-unit ID and spec Section 9b Phase 2. Conditional: only if the file exists at repo root; otherwise the entry is omitted and the work-unit commit message carries the audit trail (recorded in `## TDD Cycle Log`). |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `git ls-files catalog/` returns zero lines (captured in `## TDD Cycle Log`).
- [ ] `git grep -F 'catalog/' -- ':!CHANGELOG.md' ':!.github/workflows/'` returns zero lines (captured in `## TDD Cycle Log`).
- [ ] `mpm list --catalog-source <absolute-path-to-staged-repo>` exits 0 and lists all 5 expected entries (`workbench-backlog-builder`, `history`, `immutable-audit-trail`, `security-code-review`, `builders-plugins`); full stdout captured in `## TDD Cycle Log`.
- [ ] Only files in the Changes Manifest are staged with `git add` (`git status --short` confirmation captured in `## TDD Cycle Log`).
- [ ] `## TDD Cycle Log` captures the pre-deletion audit, the deletion + scrub operations, and the post-staging verification, each with exit codes and command output.

## TDD Cycle Log

## Comments
