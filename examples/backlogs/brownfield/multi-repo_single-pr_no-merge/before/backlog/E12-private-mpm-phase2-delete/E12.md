# E12: Private-mpm Phase 2 -- delete the legacy `catalog/` directory tree

## Status: in-queue

## Target Repository

- **Repo:** `example-org/private-mpm`

## Description

Phase 2 of the private-mpm operational migration (spec Section 9b
Phase 2). Once every entry's author-facing metadata has been migrated into
its `*-marketplace.xml` `<catalog-metadata>` block by Phase 1 (E11), the
legacy `catalog/<name>/` per-entry directory tree is no longer the
source of truth and must be removed in its entirety.

Phase 2 is a single, mechanical, repo-wide deletion:

- `git rm -r catalog/` removes every tracked file under `catalog/` (today
  10 tracked files across 5 subdirectories: `workbench-backlog-builder/`,
  `history/`, `immutable-audit-trail/`, `mpm/`, `security-code-review/`,
  each containing a `.mpm` file plus a `README.md` or `mpm-readme.md`).
- Any internal tooling reference to `catalog/<name>/` paths anywhere in
  the repo (README docs, CI scripts, contributing guide, helper scripts)
  is removed in the same change so no orphan reference survives the delete.

Acceptance is binary and observable end-to-end:

1. `git ls-files catalog/` returns zero tracked files (the tree is gone
   from version control).
2. `mpm list --catalog-source <this-repo-at-HEAD>` still returns every
   expected entry (5 entries: `workbench-backlog-builder`, `history`,
   `immutable-audit-trail`, `security-code-review`, `builders-plugins`)
   after the delete -- proving the `mpm` CLI no longer needs the
   legacy directory tree to discover entries.
3. CI is green on the delete PR.

The mpm entry (`catalog/mpm/`) is in scope for this deletion: it is
a legacy `catalog/<name>/` directory like the others. It does NOT have
a Phase 1 sibling in E11 because the mpm entry was bundled-only (no
XML to migrate to); the bundled-catalog mirror inside the mpm wheel
is handled separately by E6-F2-S1-T1 (`matthew-dresden/mpm` repo).
After both E6-F2-S1-T1 and E12 land, no copy of `catalog/mpm/` exists
anywhere in the ecosystem.

E12 depends on every E11 task being `done`. The deletion cannot run
until every entry's metadata has been migrated into XML, because any
operator currently reading author-facing metadata from a `catalog/<name>/`
README must, post-deletion, find that content inside the entry's
`<catalog-metadata><description>` block.

Spec source:

- `spec/mpm-list-add-lock-features-spec.md` Section 9b Phase 2
  ("Delete the legacy `catalog/` directory tree (single task; runs
  after all of Phase 1 is complete)").
- `spec/mpm-list-add-lock-features-spec.md` Section 9b "Work-unit
  traceability" paragraph (one Phase-2 task at the end, gated on
  every Phase-1 task being done).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E11 | Private-mpm Phase 1 -- per-entry `<catalog-metadata>` migration | in-queue |
