# E1-F2-S1: Canonicalize repo URL helper

## Status: in-queue

## Description

Ship `core/url.py::canonicalize_repo_url` and its unit tests in one
atomic Task (T1). The story has a single task because the helper is a
pure function and source+test atomicity (`source-test-atomicity.md`)
requires them in one manifest for AC-FINAL-014 (100% coverage) to be
satisfiable.

Downstream consumers (E1's lockfile, E3's conflict detection, E4's
`mpm why`) import `canonicalize_repo_url` from this module; they do
not depend on each other through this helper.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 4.0
"Repo URL canonicalization" subsection.

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
