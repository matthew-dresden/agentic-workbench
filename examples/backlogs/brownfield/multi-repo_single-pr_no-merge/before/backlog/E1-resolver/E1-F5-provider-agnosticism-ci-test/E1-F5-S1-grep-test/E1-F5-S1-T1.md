# E1-F5-S1-T1: Provider-agnosticism CI test (`tests/functional/test_provider_agnostic.py`)

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e1-f5-s1-t1`

## Description

Author the spec-required CI test that enforces mpm's
provider-agnostic invariant by greping the entire tracked source tree
for provider-specific CLI invocations and provider-specific REST or
GraphQL hostnames.

The test is a pytest module under `tests/functional/` so it runs as
part of the standard `pytest tests/functional -v` invocation. CI picks
it up automatically -- no separate workflow file is needed.

### What the test does

1. Enumerate the tracked file set via
   `subprocess.check_output(["git", "ls-files"], cwd=<repo_root>)`,
   decoding bytes to UTF-8 and splitting on `"\n"`. Use the
   `git ls-files` plumbing form so untracked working-tree files do not
   pollute the scan.
2. Compute the allowlist:
   - Always-allowlisted path prefixes (built into the test, not the
     allowlist file): `docs/`, `tests/fixtures/`, `.git/`. These cover
     the spec's blanket exemptions for docs files and fixtures.
   - The allowlist file `tests/integration/provider_allowlist.txt` is
     read and parsed line-by-line. Each non-empty, non-`#`-prefixed
     line MUST have the shape `<path>:<justification>` (a colon
     separator with non-empty justification text). Malformed lines
     raise `ValueError` naming the line number; the test fails fast.
   - The allowlist file is itself in the allowlist (the file contains
     the forbidden tokens as part of its documented format).
3. For each non-allowlisted tracked file: read the bytes, decode
   UTF-8 with `errors="replace"`, and scan line-by-line for the
   forbidden token set.
4. Forbidden CLI tokens are matched via the regex
   `r"\b(gh|glab|bb|tea)\b"` plus the literal substrings
   `"aws codecommit"` and `"az repos"`. Word-boundary matching ensures
   `right`, `weight`, `rubber`, `tearing`, and similar do not falsely
   fire.
5. Forbidden hostnames are matched via the literal substrings
   `api.github.com`, `gitlab.com/api`, `bitbucket.org/!api`, and
   `dev.azure.com/_apis`.
6. On any match: the test assertion fails with a message naming the
   file path (repo-relative), the 1-based line number, and the matched
   token, plus a remediation line:
   `Remediation: remove the provider-specific reference, OR add an
   exemption line to tests/integration/provider_allowlist.txt with a
   justification comment.`
7. When the allowlist file is empty (the day-one state of this Task),
   the test passes against the current tree because no
   provider-specific tokens exist in production code today.

### What the test does NOT do

This test does not shell out to any provider. It uses only `git
ls-files` (which is a local git plumbing command, not a provider call)
and the local filesystem.

### Test file structure

`tests/functional/test_provider_agnostic.py` parametrises the
forbidden-token cases so each token has its own test ID in the report:

```python
@pytest.mark.parametrize("token", FORBIDDEN_CLI_TOKENS + FORBIDDEN_HOST_TOKENS)
def test_no_forbidden_token_in_tracked_source(token, ...): ...
```

Plus a single integration-style test that enumerates all tracked
non-allowlisted files once and asserts zero findings across the
combined forbidden-token set, so the failure message reports all
matches in one run rather than one parametrised case at a time.

The allowlist parser is a private helper in the same module
(`_load_allowlist(repo_root: Path) -> set[str]`) with its own unit-
shaped tests that exercise:

- Empty file.
- File with only comments.
- File with `<path>:<justification>` lines.
- File with malformed line (no colon) -- raises `ValueError`.
- File with whitespace-only justification -- raises `ValueError`.

### Allowlist file format

`tests/integration/provider_allowlist.txt` (NEW, empty except for a
header comment):

```
# Provider-agnosticism allowlist.
#
# Format: <path>:<justification>
#   - <path>: repo-relative path of the file to exempt from the
#     provider-agnostic grep test.
#   - <justification>: non-empty free-text reason a human reviewer
#     accepted this exemption.
#
# Lines starting with `#` and blank lines are ignored.
#
# Adding an entry here requires a code review. The intent is that
# multi-provider parity test fixtures (per spec Section 10) and similar
# explicitly-labeled cross-provider cases live here with a clear
# justification. Production source code MUST NOT appear in this list.
```

### Docs updates

- `docs/security-model.md`: add a `## Provider-agnosticism CI test`
  subsection cross-referencing this test and the allowlist file,
  noting that the test enforces spec Section 3.6 and Section 10 line
  1023.
- `docs/contributing.md`: add a `### How to add a multi-provider
  parity test` subsection that names the allowlist file and the
  format. If this file does not yet exist in the mpm repo, create
  it with this subsection and a brief preamble pointing contributors
  at `docs/security-model.md` for the full trust-model context.

### Definition of Ready

- [ ] All dependency work units are `done` (this Task has none).
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`.
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`, `git`).
- [ ] The branch `backlog/e1-f5-s1-t1` does not already exist (or is from a prior attempt).
- [ ] The spec and acceptance criteria are unambiguous -- no open questions.

### Depends On This

| ID | Title | Status |
|----|-------|--------|

### Approach

1. **TDD RED (parser unit-style tests):** Add unit-shaped tests in
   `tests/functional/test_provider_agnostic.py` for the private
   `_load_allowlist` helper covering empty file, comments-only,
   well-formed lines, malformed (no colon), and whitespace-only
   justification. Run `uv run pytest tests/functional/test_provider_agnostic.py -v`;
   tests FAIL because the helper does not yet exist.
2. **TDD RED (forbidden-token cases):** Add the parametrised
   forbidden-token cases. Each case constructs a small in-test
   tracked-file scenario (a temp file inside the repo's `git ls-files`
   output is NOT necessary; the cases assert behaviour on a synthetic
   in-memory string passed to a scanning helper
   `_scan_lines(lines: Iterable[str], path: str) -> list[Finding]`).
   Tests FAIL because the scanner does not yet exist.
3. **TDD RED (case-sensitivity / word-boundary cases):** Add cases
   that assert `right`, `weight`, `rubber`, `tearing`, and
   `freight` do NOT match `\bgh\b` / `\bglab\b` / `\bbb\b` / `\btea\b`.
   These cases FAIL until the regex is implemented correctly.
4. **TDD RED (integration case against current tree):** Add the
   end-to-end test that calls `git ls-files`, loads the allowlist,
   scans every non-allowlisted file, and asserts zero findings. The
   test FAILS because the module is not yet importable.
5. **TDD GREEN:** Implement
   `tests/functional/test_provider_agnostic.py` with the constants,
   the `_load_allowlist` parser, the `_scan_lines` scanner, and the
   parametrised + integration test bodies. Run the cycle again;
   tests PASS.
6. **Create the allowlist file:** Author
   `tests/integration/provider_allowlist.txt` with only the header
   comment block. The file is referenced by path from the test, so
   its absence would cause the integration test to fail with a clear
   `FileNotFoundError`.
7. **Docs:** Update `docs/security-model.md` with the
   `## Provider-agnosticism CI test` subsection. Update
   `docs/contributing.md` (create if missing) with the multi-provider
   parity guidance.
8. **Verify:** Full suite:
   `uv run pytest tests/ -v && uv run ruff check src tests && uv run ruff format --check src tests && uv run mypy src && uv run bandit -r src -ll`.
9. **TDD Cycle Log:** Record RED-GREEN-REFACTOR exit codes.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 0 header line 6 (provider-agnostic invariant declared at the spec preface).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 3.6 (trust model -- provider-agnostic is one of seven invariants).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 10 line 1023 (CI grep test requirement; the test in this Task implements that line).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 12 acceptance item 20 (provider-agnostic enforcement via tree-wide grep).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 14 (`--help` snapshots referencing provider-agnostic behaviour in error messages).

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Acceptance Criteria

- [ ] AC-FUNC-001 `tests/functional/test_provider_agnostic.py` exists, is importable as a pytest module, and is collected by `pytest tests/functional -v` without errors.
- [ ] AC-FUNC-002 The test enumerates the tracked file set via `subprocess.check_output(["git", "ls-files"], cwd=<repo_root>)` (or equivalent `git ls-files` invocation) and decodes the output as UTF-8 split on `"\n"`.
- [ ] AC-FUNC-003 The forbidden CLI token set is `{r"\bgh\b", r"\bglab\b", r"\bbb\b", r"\btea\b", "aws codecommit", "az repos"}`; every member is exercised by at least one parametrised case that asserts a synthetic line containing the token is flagged.
- [ ] AC-FUNC-004 The forbidden hostname set is `{"api.github.com", "gitlab.com/api", "bitbucket.org/!api", "dev.azure.com/_apis"}`; every member is exercised by at least one parametrised case that asserts a synthetic line containing the host is flagged.
- [ ] AC-FUNC-005 The always-allowlisted path prefixes are exactly `docs/` (ref), `tests/fixtures/` (ref), and `.git/`; a test asserts that a synthetic file path under each of these prefixes is excluded from scanning even when it contains a forbidden token.
- [ ] AC-FUNC-006 `tests/integration/provider_allowlist.txt` exists and is itself excluded from scanning; the test asserts that the allowlist file's own forbidden-token-bearing header comment does not produce a finding against itself.
- [ ] AC-FUNC-007 The allowlist parser `_load_allowlist` accepts lines of shape `<path>:<justification>`, skips blank lines and `#`-comment lines, and raises `ValueError` (with line number in the message) for any malformed line (missing colon, empty justification, or whitespace-only justification).
- [ ] AC-FUNC-008 Case-sensitivity / word-boundary: the strings `right`, `weight`, `rubber`, `tearing`, and `freight` do NOT match `\bgh\b`, `\bglab\b`, `\bbb\b`, or `\btea\b` in any parametrised case. A dedicated test asserts these negative cases.
- [ ] AC-FUNC-009 When the allowlist file is empty (the day-one state), the integration test passes against the current tracked tree because no production source file contains a forbidden token.
- [ ] AC-FUNC-010 On any match, the failure message names the repo-relative file path, the 1-based line number, the matched token, and includes the remediation string `Remediation: remove the provider-specific reference, OR add an exemption line to tests/integration/provider_allowlist.txt with a justification comment.`
- [ ] AC-TEST-001 All parametrised cases listed in the Approach are added to `tests/functional/test_provider_agnostic.py` BEFORE the implementation lands (TDD RED then GREEN). The TDD Cycle Log records both phases with exit codes.
- [ ] AC-TEST-002 The scanner helper `_scan_lines(lines: Iterable[str], path: str)` is exercised by parametrised in-memory tests (no filesystem dependency) covering: (a) clean line -> empty findings, (b) line with each forbidden CLI token -> exactly one finding with the correct token and 1-based line number, (c) line with each forbidden hostname -> exactly one finding, (d) negative word-boundary cases (`right`, `weight`, `rubber`, `tearing`, `freight`) -> empty findings.
- [ ] AC-TEST-003 The allowlist-parser tests cover: empty file, comments-only file, well-formed `<path>:<justification>` line, malformed line missing colon -> `ValueError`, malformed line with whitespace-only justification -> `ValueError`. Each case has its own assertion that can fail.
- [ ] AC-CYCLE-001 End-to-end cycle: run `uv run pytest tests/functional/test_provider_agnostic.py -v` against the current tracked tree; assert exit code zero and assert the integration test ID appears in the report as `PASSED`. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/ (ref)security-model.md` (ref) is updated in the same commit with a `## Provider-agnosticism CI test` subsection that names `tests/functional/test_provider_agnostic.py` and `tests/integration/provider_allowlist.txt` and cross-references spec Section 3.6 and Section 10 line 1023.
- [ ] AC-DOC-002 `docs/ (ref)contributing.md` (ref) is updated (or created if not present) in the same commit with a `### How to add a multi-provider parity test` subsection that documents the allowlist format `<path>:<justification>` and points contributors at `docs/ (ref)security-model.md`.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero (the new test module is ruff-clean).
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 The test itself does not shell out to any provider, does not call any provider HTTP API, and does not read any provider-specific environment variable. Its only subprocess call is `git ls-files`. A test asserts (by inspecting the module source via `pathlib.Path(__file__).read_text()`) that no forbidden token appears in the module's own source EXCEPT inside the documented forbidden-token-constant definitions and the comment block describing them.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/ (ref)mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/ (ref)mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files. (The new test module itself is exercised by its own tests; coverage is computed against `src/` (ref) only, so this AC reduces to "no regression" for this Task.)
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `tests/functional/test_provider_agnostic.py` | New -- pytest module enumerating tracked files via `git ls-files`, scanning each non-allowlisted file for the forbidden CLI and hostname tokens; private `_load_allowlist` and `_scan_lines` helpers; parametrised cases plus one integration test. |
| `tests/integration/provider_allowlist.txt` | New -- empty allowlist file with a header comment explaining the format (`<path>:<justification>` per line). |
| `docs/security-model.md` | Update -- add a `## Provider-agnosticism CI test` subsection referencing the new test and allowlist file plus spec Section 3.6 and Section 10 line 1023 cross-references. |
| `docs/contributing.md` | New or update -- add a `### How to add a multi-provider parity test` subsection documenting the allowlist format. If the file does not yet exist in the mpm repo, create it with a brief preamble pointing contributors at `docs/security-model.md`. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes.
- [ ] `uv run bandit -r src -ll` passes.
- [ ] `tests/integration/provider_allowlist.txt` is present, empty of exemption entries (header comment only), and itself excluded from the scan.
- [ ] `docs/security-model.md` (ref) and `docs/contributing.md` (ref) are updated in the same commit.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] TDD Cycle Log captures RED-GREEN-REFACTOR phases with exit codes.

## TDD Cycle Log

## Comments
