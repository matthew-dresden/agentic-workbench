# E1-F1-S1: Widen bare PEP 440 detection and add loud error for zero parseable tags

## Status: in-queue

## Description

Two related edits to `src/mpm_cli/version.py` that ship in the same commit
because they touch the same function set and share the test fixture file
(`tests/unit/test_version.py`).

Story scope: implement T1 (widened bare PEP 440) followed by T2
(zero-parseable-tags loud error). The two tasks run in this order because T1
modifies `_normalize_bare_semver_to_tag` (called from `resolve_version` BEFORE
constraint resolution) and T2 modifies `_resolve_constraint_from_tags` (called
AFTER constraint detection). They cannot both edit the same line; T2 depends
on T1 only to keep the test file imports consistent.

Spec source: spec Section 4.0 resolver rules 3 (widened bare PEP 440) and
Section 0.4 (PEP 440 tag-name enforcement loud-error).

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
