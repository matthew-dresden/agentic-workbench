# E1-F4-S1-T1: Move `--catalog-source` flag definition to shared `core/cli_args.py`

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e1-f4-s1-t1`

## Description

Create a new module `src/mpm_cli/core/cli_args.py` and move the
canonical definition of the `--catalog-source` argparse flag out of
`src/mpm_cli/commands/bootstrap.py`.

The new module exposes a single public factory function:

```python
def add_catalog_source_arg(parser: argparse.ArgumentParser) -> None:
    """Add the --catalog-source flag to the given argparse parser.

    The flag accepts a <git-url>@<ref> string identifying a manifest
    repo at a specific revision. Couples to the MPM_CATALOG_SOURCE
    env var via the `default=` mechanism (the env var is read at parser
    build time; CLI flag wins when both are set, per spec Section 4
    header).
    """
```

The flag is added with exactly the same shape it has on today's
`mpm bootstrap` parser:

- `--catalog-source` (no short form).
- `dest="catalog_source"` (kept identical to today so consumers reading
  `args.catalog_source` continue to work without change).
- `default=` reads from `os.environ.get("MPM_CATALOG_SOURCE", None)`
  via the existing `CATALOG_ENV_VAR` constant in
  `src/mpm_cli/constants.py`.
- `metavar="<git-url>@<ref>"`.
- `help="..."` text is the byte-identical string used today on
  `mpm bootstrap`; preserve verbatim so `mpm bootstrap --help`
  shows the same line before and after this Task.

`src/mpm_cli/commands/bootstrap.py` is updated to import
`add_catalog_source_arg` from `mpm_cli.core.cli_args` and call it on
the bootstrap parser in place of the existing inline
`parser.add_argument('--catalog-source', ...)` block. No other changes
in `bootstrap.py`.

This Task is a NO-BEHAVIOUR-CHANGE refactor. Downstream consumers in
E2-E6 import the new factory and add the flag to their own parsers;
none of those consumers are in scope here.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 3
primitives table row 2 (`CLI flag + env var for catalog source`) and
Section 3.5 (Standards audit and tightening).

### Definition of Ready

- [ ] All dependency work units are `done` (this Task has none in E1).
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`.
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`).
- [ ] The branch `backlog/e1-f4-s1-t1` does not already exist (or is from a prior attempt).
- [ ] The spec and acceptance criteria are unambiguous -- no open questions.

### Depends On This

| ID | Title | Status |
|----|-------|--------|
| E2-F2-S1-T1 | `mpm list` -- import `add_catalog_source_arg` | in-queue |
| E2-F4-S1-T1 | `mpm add` -- import `add_catalog_source_arg` | in-queue |
| E2-F5-S1-T1 | `mpm remove` -- import `add_catalog_source_arg` | in-queue |
| E3-F3-S1-T1 | `mpm install` -- import `add_catalog_source_arg` | in-queue |
| E4-F1-S1-T1 | `mpm outdated` -- import `add_catalog_source_arg` | in-queue |
| E4-F2-S1-T1 | `mpm why` -- import `add_catalog_source_arg` | in-queue |
| E5-F1-S1-T1 | `mpm doctor` -- import `add_catalog_source_arg` | in-queue |
| E5-F2-S1-T1 | `mpm catalog audit` -- import `add_catalog_source_arg` | in-queue |
| E5-F3-S1-T1 | `mpm validate metadata` -- import `add_catalog_source_arg` | in-queue |
| E6-F1-S1-T1 | `mpm bootstrap` deprecation shim | in-queue |

### Approach

1. **TDD RED (factory unit tests):** Author
   `tests/unit/test_cli_args.py` with parametrised tests covering:
   - Calling `add_catalog_source_arg(parser)` adds an argument whose
     `dest` is `"catalog_source"`.
   - The argument's `metavar` is `"<git-url>@<ref>"`.
   - When `MPM_CATALOG_SOURCE` is set in the environment, the parser
     resolves `args.catalog_source` to that value when the CLI flag is
     absent.
   - When the CLI flag is supplied, it wins over the env var.
   - When neither is supplied, `args.catalog_source` is `None`.
   - The argument's `help` text is byte-identical to the string defined
     today on `mpm bootstrap` (loaded from the bootstrap source file
     at test time to remove drift between the two definitions).
   Run `uv run pytest tests/unit/test_cli_args.py -v`; cases FAIL
   because the module does not yet exist.
2. **TDD RED (bootstrap regression tests):** Update
   `tests/unit/test_bootstrap.py` with a new assertion that
   `mpm bootstrap --help` output contains the `--catalog-source` line
   AND that the `--help` output is byte-identical before and after this
   refactor (snapshot the current `--help` output into a fixture file
   under `tests/fixtures/help/bootstrap.txt` if one does not already
   exist; assert the captured output equals the fixture).
3. **TDD GREEN:** Author `src/mpm_cli/core/cli_args.py` with
   `add_catalog_source_arg`. Import `CATALOG_ENV_VAR` from
   `mpm_cli.constants`. Drive the `default=` via
   `os.environ.get(CATALOG_ENV_VAR)`. Use the byte-identical help text
   from today's `commands/bootstrap.py` so the fixture-snapshot test
   passes.
4. **TDD GREEN (bootstrap delegation):** Update
   `src/mpm_cli/commands/bootstrap.py` to import
   `add_catalog_source_arg` and replace its inline
   `parser.add_argument('--catalog-source', ...)` block with a single
   `add_catalog_source_arg(parser)` call. No other lines in
   `bootstrap.py` change.
5. **TDD REFACTOR:** If any other helper in `cli_args.py` would
   reasonably live alongside the factory (e.g., a `--no-color` factory
   the same set of commands needs), consider adding it. SCOPE GATE: do
   NOT add unrelated factories in this Task; defer to future Tasks per
   YAGNI. The single `add_catalog_source_arg` factory is the entire
   scope.
6. **Cycle (functional verification):** Build a real argparse parser
   in a test fixture, register `add_catalog_source_arg(parser)`, parse
   the argv `["--catalog-source", "https://h/r.git@main"]`, and assert
   `args.catalog_source == "https://h/r.git@main"`. Then parse `[]`
   with `MPM_CATALOG_SOURCE` set to `"https://h/r.git@v1"` in the
   test env and assert `args.catalog_source == "https://h/r.git@v1"`.
   Record both runs in `## TDD Cycle Log`.
7. **Docs:** Update `docs/cli-reference.md` to note that
   `--catalog-source` is provided by the shared factory in
   `mpm_cli.core.cli_args`, with a one-line cross-reference for
   contributors authoring future commands.
8. **Verify:** Full suite:
   `uv run pytest tests/ -v && uv run ruff check src tests && uv run ruff format --check src tests && uv run mypy src && uv run bandit -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 3 primitives table row 2 (CLI flag + env var for catalog source -- canonical definition must move to shared module).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 3.5 (Standards audit and tightening).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4 header (catalog-source resolution precedence -- CLI flag wins over env var).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 4.9 (`mpm bootstrap` deprecation -- the legacy home of the flag).

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Acceptance Criteria

- [ ] AC-FUNC-001 The new module `src/mpm_cli/core/cli_args.py` exists and exposes a single public function `add_catalog_source_arg(parser: argparse.ArgumentParser) -> None`.
- [ ] AC-FUNC-002 Calling `add_catalog_source_arg(parser)` registers an argument whose `dest` is `"catalog_source"`, whose `metavar` is `"<git-url>@<ref>"`, and whose `default` is the value of `os.environ.get(CATALOG_ENV_VAR)` resolved at parser-build time.
- [ ] AC-FUNC-003 The argument's `help` text is byte-identical to the help text used today on `mpm bootstrap`'s inline `--catalog-source` definition; a snapshot test loads both texts and compares them.
- [ ] AC-FUNC-004 `src/mpm_cli/commands/bootstrap.py` (ref) imports `add_catalog_source_arg` from `mpm_cli.core.cli_args` and calls it instead of defining `--catalog-source` inline; no other behavioural changes in `bootstrap.py` (ref).
- [ ] AC-FUNC-005 `mpm bootstrap --help` output is byte-identical before and after this Task (snapshot fixture under `tests/fixtures/help/bootstrap.txt` asserts the captured output).
- [ ] AC-FUNC-006 CLI flag wins over env var: when both are set, `args.catalog_source` reflects the CLI flag value (parsed via a real argparse parser in the test).
- [ ] AC-FUNC-007 When neither CLI nor env is set, `args.catalog_source` is `None` (today's behaviour).
- [ ] AC-TEST-001 All parametrised cases listed in the Approach are added to `tests/unit/test_cli_args.py` BEFORE the implementation lands (TDD RED then GREEN).
- [ ] AC-TEST-002 `tests/unit/test_bootstrap.py` (ref) is updated with the snapshot-assertion against `tests/fixtures/help/bootstrap.txt` so any future drift in the help text fails the regression test.
- [ ] AC-TEST-003 An import-from-source test asserts that `src/mpm_cli/commands/bootstrap.py` (ref) contains the line `from mpm_cli.core.cli_args import add_catalog_source_arg` (or equivalent), and that the file no longer contains the literal string `"--catalog-source"` as part of an inline `add_argument` call.
- [ ] AC-CYCLE-001 End-to-end cycle: build a real argparse parser in a test, call `add_catalog_source_arg(parser)`, parse `["--catalog-source", "https://h/r.git@main"]`, assert `args.catalog_source == "https://h/r.git@main"`. Then unset the CLI flag and set `MPM_CATALOG_SOURCE=https://h/r.git@v1` in the test env, parse `[]`, assert `args.catalog_source == "https://h/r.git@v1"`. Recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/cli-reference.md` updated in the same commit to note that `--catalog-source` is provided by the shared factory in `mpm_cli.core.cli_args`.
- [ ] AC-DOC-002 The module docstring of `src/mpm_cli/core/cli_args.py` names the factory's purpose, the env-var coupling, and the spec Section 3 reference.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No secrets, credentials, or hard-coded configuration values introduced; the env-var name `MPM_CATALOG_SOURCE` is referenced via the existing `CATALOG_ENV_VAR` constant.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/core/cli_args.py` | New -- module exposing `add_catalog_source_arg(parser)` factory; reads `CATALOG_ENV_VAR` from `constants.py` for the default value. |
| `src/mpm_cli/commands/bootstrap.py` | Update -- replace inline `parser.add_argument('--catalog-source', ...)` block with `add_catalog_source_arg(parser)`; import added. No other behavioural changes. |
| `tests/unit/test_cli_args.py` | New -- parametrised tests for the factory's dest/metavar/help/default-from-env behaviours plus the CLI-wins-over-env precedence rule. |
| `tests/unit/test_bootstrap.py` | Update -- assert `mpm bootstrap --help` output is byte-identical to the snapshot fixture; assert `bootstrap.py` no longer carries the inline flag definition. |
| `tests/fixtures/help/bootstrap.txt` | New -- snapshot of `mpm bootstrap --help` captured at this Task's authoring time; future drift fails the regression test. |
| `docs/cli-reference.md` | Update -- note that `--catalog-source` is provided by the shared `mpm_cli.core.cli_args` factory; cross-reference for contributors. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes.
- [ ] `uv run bandit -r src -ll` passes.
- [ ] 100% coverage on `src/mpm_cli/core/cli_args.py` (entire new module).
- [ ] `bootstrap.py` (ref) modifications keep its coverage at the prior baseline or better.
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] `docs/cli-reference.md` updated in the same commit.
- [ ] TDD Cycle Log captures RED-GREEN-REFACTOR phases with exit codes.

## TDD Cycle Log

## Comments
