# E1-F4-S1: Shared `core/cli_args.py` module with `add_catalog_source_arg`

## Status: in-queue

## Description

One Task (T1): create `src/mpm_cli/core/cli_args.py`, move the
`--catalog-source` argparse definition into it, update
`commands/bootstrap.py` to call the new factory, and add full unit
coverage. Source/test atomicity requires the new module's unit test in
the same Manifest.

Spec source: `spec/mpm-list-add-lock-features-spec.md` Section 3
primitives table row 2.

## Dependencies

| ID | Title | Status |
|----|-------|--------|

## Target Repository

- **Repo:** `matthew-dresden/mpm`
