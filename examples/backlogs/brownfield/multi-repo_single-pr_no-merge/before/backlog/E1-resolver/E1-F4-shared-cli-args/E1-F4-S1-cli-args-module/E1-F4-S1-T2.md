# E1-F4-S1-T2: Add global flags `--quiet`, `--verbose`, `--no-color` to shared `core/cli_args.py`

## Status: in-queue

## Target Repository

- **Repo:** `matthew-dresden/mpm`
- **Branch:** `backlog/e1-f4-s1-t2`

## Description

Extend the shared CLI-args module `src/mpm_cli/core/cli_args.py`
(introduced by E1-F4-S1-T1) with a factory for the spec-required global
flags `--quiet`, `--verbose`, and `--no-color`, and plumb them through
the root argparse parser in `src/mpm_cli/cli.py` so every subcommand
inherits the parsed values via argparse's natural namespace propagation.

The new module-level factory:

```python
def add_global_flags(parser: argparse.ArgumentParser) -> None:
    """Add the global flags --quiet, --verbose, --no-color to the given parser.

    --quiet / --verbose are MUTUALLY EXCLUSIVE; passing both raises an
    argparse error immediately (fail-fast per spec Section 7).

    --no-color disables ANSI color regardless of TTY / NO_COLOR env var
    (precedence: --no-color > NO_COLOR env var > TTY auto-detect).
    """
```

And the companion runtime helper:

```python
def _apply_global_flags(args: argparse.Namespace) -> None:
    """Translate parsed args into runtime state.

    - Sets the root logger level: WARNING if args.quiet, DEBUG if
      args.verbose, INFO otherwise.
    - Sets the module-level color-suppression state used by every
      formatter helper (constants._NO_COLOR_ACTIVE) when --no-color is
      set OR when the NO_COLOR env var is non-empty. Precedence:
      --no-color flag > NO_COLOR env var > TTY auto-detect.

    Idempotent: calling twice with the same args produces the same
    state. Fails fast (raises ValueError) if both args.quiet and
    args.verbose are True (defence-in-depth; argparse's mutually
    exclusive group should prevent this case ever reaching the
    helper).
    """
```

Spec mapping:
- `spec/mpm-list-add-lock-features-spec.md` Section 7 lines 735-746:
  `--quiet` / `--verbose` mutual exclusion; `--no-color` precedence over
  `NO_COLOR` env var; deterministic color decision (no prompting).
- Section 7 line 808 (global flag table row): `--quiet` / `--verbose`
  global, mutually exclusive; `--no-color` alternative to `NO_COLOR`.
- Section 11.2 line 1083: completion entry for `--quiet`, `--verbose`,
  `--no-color` as static no-value flags on _all_ commands.
- Section 12 item 25 (lines 934-935): functional test for both flags
  passed together raising a hard error; `NO_COLOR=1` suppresses color
  even on TTY; `--no-color` wins.
- Section 14 lines 1349-1350: `mpm --help` output documents the
  Global options block listing all three flags.

### Integration design

Two competing integration paths were considered. They are documented
here so the trade-off is auditable.

**Path A (rejected): re-wire every command parser task.**
Modify the per-command tasks in E2/E3/E4/E5/E7 to call
`add_global_flags(parser)` on their subcommand parser. This requires
editing 10+ already-authored tasks (E2-F2-S1-T1, E2-F4-S1-T1,
E2-F5-S1-T1, E3-F3-S1-T1, E4-F1-S1-T1, E4-F2-S1-T1, E5-F1-S1-T1,
E5-F2-S1-T1, E5-F3-S1-T1, E7-F1-S1-T1) and creates an artificial
dependency cycle.

**Path B (chosen): wire at the root parser only.**
Call `add_global_flags(root_parser)` once on the top-level argparse
parser in `src/mpm_cli/cli.py`. argparse propagates the parsed
namespace to every subcommand by design, so `args.quiet`,
`args.verbose`, and `args.no_color` are present on every subcommand's
`args` without per-subcommand wiring. Call `_apply_global_flags(args)`
early in `main()` before dispatching to subcommand handlers; the
side-effects (logger level, color state) then apply uniformly.

The per-command `--no-color` definitions that already exist in some
downstream command tasks become redundant after this lands. They are
NOT removed in this Task (avoids retroactive churn across already-
authored tasks); argparse will accept the duplicate definition only if
the subparser uses a fresh parser, and the global namespace wins for
the top-level flag. Downstream tasks may opt to delete their inline
definitions in a follow-up cleanup epic.

This Task ships:
1. The `add_global_flags` factory in `core/cli_args.py`.
2. The `_apply_global_flags` runtime helper in `core/cli_args.py`.
3. The root-parser wire-up in `cli.py` plus an `_apply_global_flags`
   call early in `main()`.
4. The new constants (`NO_COLOR_ENV`, `_NO_COLOR_ACTIVE`) in
   `constants.py`.
5. Source-test-atomic unit-test files for every new/modified source
   file per `workbench/docs/source-test-atomicity.md`.
6. Documentation in `docs/configuration.md` describing the global
   flags and the precedence rules.

### Definition of Ready

- [ ] All dependency work units are `done` (E1-F4-S1-T1).
- [ ] Target repository is cloned and accessible at `${JUDGE_WORKSPACE_ROOT}/mpm`.
- [ ] All prerequisite tools are installed (`uv`, `ruff`, `mypy`, `bandit`, `pytest`).
- [ ] The branch `backlog/e1-f4-s1-t2` does not already exist (or is from a prior attempt).
- [ ] The spec and acceptance criteria are unambiguous -- no open questions.

### Depends On This

The factory and root-parser wire-up are consumed transparently (via
argparse namespace propagation) by every command parser; no hard
dependency wiring is required from downstream tasks. Listed
informationally:

| ID | Title | Status |
|----|-------|--------|
| E2-F2-S1-T1 | `mpm list` parser | in-queue |
| E2-F4-S1-T1 | `mpm add` parser | in-queue |
| E2-F5-S1-T1 | `mpm remove` parser | in-queue |
| E3-F3-S1-T1 | `mpm install` parser | in-queue |
| E4-F1-S1-T1 | `mpm outdated` parser | in-queue |
| E4-F2-S1-T1 | `mpm why` parser | in-queue |
| E5-F1-S1-T1 | `mpm doctor` parser | in-queue |
| E5-F2-S1-T1 | `mpm catalog audit` parser | in-queue |
| E5-F3-S1-T1 | `mpm validate metadata` parser | in-queue |
| E7-F1-S1-T1 | `mpm completion` parser | in-queue |

### Approach

1. **TDD RED (factory unit tests):** Extend `tests/unit/test_cli_args.py`
   with parametrised cases for `add_global_flags`:
   - Calling `add_global_flags(parser)` registers exactly three
     destinations: `quiet` (bool), `verbose` (bool), `no_color` (bool).
   - `parser.parse_args(["--quiet", "--verbose"])` exits with non-zero
     and an argparse error message naming "not allowed with" both
     flags (mutual exclusion).
   - `parser.parse_args(["--quiet"])` sets `args.quiet is True` and
     `args.verbose is False`.
   - `parser.parse_args(["--verbose"])` sets `args.verbose is True`
     and `args.quiet is False`.
   - `parser.parse_args(["--no-color"])` sets `args.no_color is True`.
   - `parser.parse_args([])` leaves all three flags `False`.
   Run `uv run pytest tests/unit/test_cli_args.py -v`; cases FAIL
   because `add_global_flags` does not yet exist.
2. **TDD RED (apply-helper unit tests):** Extend
   `tests/unit/test_cli_args.py` with parametrised cases for
   `_apply_global_flags`:
   - With `args.quiet=True, args.verbose=False`, the root logger's
     effective level is `logging.WARNING`.
   - With `args.verbose=True, args.quiet=False`, the root logger's
     effective level is `logging.DEBUG`.
   - With both `False`, the root logger's effective level is
     `logging.INFO`.
   - With `args.no_color=True`, `constants._NO_COLOR_ACTIVE is True`.
   - With `args.no_color=False` and env `NO_COLOR=` (empty),
     `constants._NO_COLOR_ACTIVE is False`.
   - With `args.no_color=False` and env `NO_COLOR=1` (non-empty),
     `constants._NO_COLOR_ACTIVE is True`.
   - With `args.no_color=True` and env `NO_COLOR=` (empty),
     `constants._NO_COLOR_ACTIVE is True` (flag wins).
   - With both `args.quiet=True` AND `args.verbose=True` (defence-in-
     depth: bypassing argparse), raises `ValueError` with a message
     naming both flags.
   - Idempotency: calling `_apply_global_flags(args)` twice produces
     the same end-state.
3. **TDD RED (root-parser integration test):** Author
   `tests/unit/test_cli.py` with parametrised cases asserting that
   `mpm_cli.cli` builds a root argparse parser, calls
   `add_global_flags(root_parser)`, and that for every registered
   subcommand the parsed `args` namespace contains `quiet`, `verbose`,
   and `no_color` attributes. Subcommand list is parameterised by
   iterating the subparsers' choices map, not hard-coded.
4. **TDD RED (constants unit test):** Author
   `tests/unit/test_constants.py` asserting:
   - `constants.NO_COLOR_ENV == "NO_COLOR"`.
   - `constants._NO_COLOR_ACTIVE` exists and defaults to `False`.
   - The module exposes both names at import time.
5. **TDD GREEN:** Implement `add_global_flags(parser)` in
   `src/mpm_cli/core/cli_args.py`:
   - Build a `parser.add_mutually_exclusive_group()` and add `--quiet`
     and `--verbose` to it as `action="store_true"`.
   - Add `--no-color` as `action="store_true"` directly on the parser
     (not in the mutex group; it is independent of quiet/verbose).
   - Set `dest=` and `help=` explicitly; help text is taken from spec
     Section 14 lines 1349-1350 verbatim where applicable.
6. **TDD GREEN:** Implement `_apply_global_flags(args)` in
   `src/mpm_cli/core/cli_args.py`:
   - Read `os.environ.get(constants.NO_COLOR_ENV, "")`.
   - Decide color-active state: `args.no_color or bool(env_value)`.
   - Update `constants._NO_COLOR_ACTIVE` via direct attribute
     assignment on the module.
   - Map verbosity: WARNING if quiet, DEBUG if verbose, INFO otherwise.
     Apply via `logging.getLogger().setLevel(level)`.
   - Fail fast: raise `ValueError` if both `args.quiet` and
     `args.verbose` are True.
7. **TDD GREEN:** Add `NO_COLOR_ENV = "NO_COLOR"` and
   `_NO_COLOR_ACTIVE: bool = False` to `src/mpm_cli/constants.py`.
8. **TDD GREEN:** In `src/mpm_cli/cli.py`, call
   `add_global_flags(root_parser)` immediately after the root parser
   is constructed (BEFORE subparsers are added so the flags appear in
   `mpm --help` output) and call `_apply_global_flags(args)` early
   in `main()` after `parser.parse_args(argv)` and BEFORE dispatching
   to subcommand handlers.
9. **TDD REFACTOR:** If `add_catalog_source_arg` and
   `add_global_flags` share helper boilerplate (e.g., a common
   `_add_bool_flag(...)`), extract it ONLY if duplication is real;
   otherwise leave both factories standalone (YAGNI).
10. **Cycle (functional verification):** From a subprocess driver in
    the test, invoke `mpm --help` and assert the output contains the
    three flags. Then invoke `mpm list --quiet --verbose` and assert
    exit code is non-zero with an argparse mutual-exclusion message on
    stderr. Then set `NO_COLOR=1` in the subprocess env and invoke
    `mpm --help`; assert no ANSI escape sequences appear. Record all
    three runs in `## TDD Cycle Log`.
11. **Docs:** Update `docs/configuration.md` with a "Global options"
    subsection enumerating the three flags, the mutual-exclusion rule,
    the `NO_COLOR` env var precedence, and the TTY auto-detect
    fallback.
12. **Verify:** Full suite:
    `uv run pytest tests/ -v && uv run ruff check src tests && uv run ruff format --check src tests && uv run mypy src && uv run bandit -r src -ll`.

### Code Standards

All code in this work unit MUST comply with the following rules. These are checked by the LLM review judges (`code_review`, `test_review`, `doc_review`, `changes_manifest`, `security_review`) and trigger `REVIEW_FAIL` when violated. The work unit cannot reach `done` until every judge logs `REVIEW_PASS`.

#### Critical Rules (Violation = Automatic Rejection)

1. **NO FALLBACK LOGIC** -- If an operation can fail, it MUST fail loudly. Never catch an exception and silently continue. Never provide a default value when the real value is missing. Exceptions: the resolver's documented lockfile-catalog-source fallback for `mpm install` / `mpm doctor`, and the existing `git ls-remote` retry policy (`MPM_GIT_RETRY_COUNT` / `MPM_GIT_RETRY_DELAY`) -- both in `spec/mpm-list-add-lock-features-spec.md` Section 4 header and Section 7.
2. **NO SILENT FAILURES** -- Every error must produce a clear, actionable error message sent to stderr. Every error must result in a non-zero exit code. Never swallow exceptions. Never log-and-continue when the operation was required to succeed.
3. **FAIL FAST** -- Detect errors at the earliest possible point. Validate inputs before processing. Check prerequisites before starting work. Exit immediately on the first error with a message that tells the user exactly what went wrong and what to do about it.
4. **NO HARD-CODED VALUES** -- No URLs, paths, timeouts, retry counts, port numbers, hostnames, credentials, feature flags, or environment-specific values in source code. All constants live in `src/mpm_cli/constants.py`, never inline in source files. All configuration comes from environment variables, configuration files, or function parameters.
5. **NO TEMPORAL LOGIC** -- Never use `time.sleep()`, `asyncio.sleep()`, or any time-based delay as a synchronization mechanism. The existing `MPM_GIT_RETRY_DELAY` (wait between known-failed `git ls-remote` retries) is the ONLY allowed sleep-based wait in this codebase. Use readiness detection, event-driven callbacks, or polling with configurable timeouts everywhere else.
6. **ALL CODE MUST BE DYNAMIC AND INPUT-DRIVEN** -- No static data, no hard-coded test fixtures embedded in source, no magic numbers. All thresholds, limits, paths, and identifiers must be parameterized.
7. **NO BYPASS ANNOTATIONS** -- Never add `# noqa`, `# nosec`, `# type: ignore`, `# pragma: no cover`, or any annotation that suppresses a linter, type checker, or security scanner finding. Fix the finding instead. The mpm repo has a documented vendored carve-out for `src/mpm_cli/repo/` (the embedded repo-tool fork): mypy and bandit scope to non-vendored code only. This carve-out is a scope demarcation, NOT a bypass annotation; do not extend it to new paths.
8. **NO DASH-EMS IN CODE OR TESTS** -- Do not use the em-dash character (Unicode U+2014) in any source file, test file, or work-unit `.md` file. Use `--` (double hyphen) in comments and docstrings if a dash is needed. `workbench validate-backlog` rule 10 enforces this on work-unit files.

#### Architecture Principles

- **SOLID** -- Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion.
- **DRY** -- Extract shared logic into reusable helpers under `src/mpm_cli/utils/`, `src/mpm_cli/core/`, or `src/mpm_cli/commands/` as appropriate. No duplicate code.
- **12-Factor App** -- Config from environment or YAML. Explicit dependencies declared in `pyproject.toml`. Logs to stdout/stderr.

#### Testing Rules

- **TDD MANDATORY** -- Write failing tests BEFORE implementation. Source/test atomicity (`workbench/docs/source-test-atomicity.md`): every new `.py` source file under `src/mpm_cli/` has its matching `tests/unit/test_<basename>.py` in the SAME work unit's Changes Manifest.
- **NO STUB TESTS** -- Every test must have assertions that can actually fail. No `assert True`, no `assert obj is not None` as the only assertion, no TODO-marked test bodies.
- **TEST ERROR PATHS** -- Every error condition must have a test. Network failures, malformed inputs, missing files, permission errors, timeouts, signal interruption.
- **PARAMETRIZE** -- Use `@pytest.mark.parametrize` for multiple scenarios. One test function per logical assertion.
- **REAL TESTS ONLY** -- Integration tests run against a real fixture git server (or mocks matching `git ls-remote --refs` output verbatim). No `MagicMock` over network APIs without a real-server parity test.
- **NO SKIPS / XFAILS / XPASSED** -- AC-FINAL-013. Skipped tests hide regressions.

#### Git Rules

- **STAGE ONLY** -- Use `git add` for relevant files (only those in this work unit's `## Changes Manifest`). Do NOT commit, push, or create PRs from inside the executor. The orchestrator owns the git-ops lifecycle.
- **NO --no-verify** -- Never bypass git hooks.
- **SELECTIVE STAGING** -- Only stage files in the Changes Manifest. `git add -A` is forbidden; use explicit `git add <path>` per file.

#### Security Rules

- **NO SECRETS** -- No API keys, tokens, passwords, GitHub PATs, or SSH keys in source code, tests, or fixtures.
- **NO eval() / exec()** -- Never execute dynamic code from external input.
- **NO PROVIDER-SPECIFIC API CALLS** -- Per spec Section 3.6, mpm NEVER calls provider HTTP APIs (`api.github.com`, `gitlab.com/api`, etc.) and NEVER shells out to provider CLIs (`gh`, `glab`, `bb`, `tea`, `aws codecommit`, `az repos`). Every git interaction is via the `git` binary only.
- **NO AUTH HANDLING** -- mpm NEVER prompts for credentials, NEVER caches them, NEVER reads them from anywhere except by delegating to the operator's git client. Auth-error patterns are detected in stderr for retry-policy purposes only (`GIT_AUTH_ERROR_PATTERNS` in `constants.py`).

#### Error Handling Contract

- Raise specific exceptions (not generic `Exception`).
- Include context in error messages: file paths, variable names, expected vs actual values, the operator's likely next step.
- Never catch and discard exceptions.
- Never call `sys.exit()` from library code; only from CLI command handlers in `src/mpm_cli/commands/` or `src/mpm_cli/cli.py`.
- Error messages follow the standard shape per spec Section 4: `ERROR: <one-line summary>` then optional context lines (wrapped at 80 cols), then a remediation line when applicable.

### Related Specifications

- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 7 lines 735-746 (verbosity flags and color/TTY policy).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 7 line 808 (global flag table row for `--quiet` / `--verbose` / `--no-color`).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 11.2 line 1083 (completion entry for the three flags).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 12 item 25 lines 934-935 (functional tests for mutual exclusion and color precedence).
- **Migration spec:** `spec/mpm-list-add-lock-features-spec.md` Section 14 lines 1349-1350 (`mpm --help` Global options block).

## Dependencies

| ID | Title | Status |
|----|-------|--------|
| E1-F4-S1-T1 | Move `--catalog-source` flag definition to shared `core/cli_args.py` | in-queue |

## Acceptance Criteria

- [ ] AC-FUNC-001 `src/mpm_cli/core/cli_args.py` exposes a public function `add_global_flags(parser: argparse.ArgumentParser) -> None` that registers `--quiet`, `--verbose`, and `--no-color` on the given parser.
- [ ] AC-FUNC-002 `--quiet` and `--verbose` are registered inside a single `parser.add_mutually_exclusive_group()`; passing both via `parser.parse_args(["--quiet", "--verbose"])` exits non-zero with argparse's standard "not allowed with" error message on stderr (no fallback, no silent suppression).
- [ ] AC-FUNC-003 `--no-color` is registered as `action="store_true"` and is NOT in the mutex group (independent of quiet/verbose).
- [ ] AC-FUNC-004 The factory registers exactly three new `dest` attributes (`quiet`, `verbose`, `no_color`) and no others; default values are `False` for each.
- [ ] AC-FUNC-005 `src/mpm_cli/core/cli_args.py` exposes a runtime helper `_apply_global_flags(args: argparse.Namespace) -> None` that sets the root logger level to `logging.WARNING` when `args.quiet`, `logging.DEBUG` when `args.verbose`, `logging.INFO` otherwise.
- [ ] AC-FUNC-006 `_apply_global_flags` sets `constants._NO_COLOR_ACTIVE` to `True` when EITHER `args.no_color` is `True` OR `os.environ.get(constants.NO_COLOR_ENV, "")` is non-empty; precedence rule (flag > env > TTY auto-detect) holds.
- [ ] AC-FUNC-007 `_apply_global_flags` is idempotent: calling it twice with the same `args` produces the same end-state (logger level + `_NO_COLOR_ACTIVE` unchanged on the second call).
- [ ] AC-FUNC-008 `_apply_global_flags` fails fast (raises `ValueError` with a message naming both flags) if invoked with `args.quiet=True` AND `args.verbose=True`; this is defence-in-depth, not a fallback.
- [ ] AC-FUNC-009 `src/mpm_cli/cli.py` calls `add_global_flags(root_parser)` on the top-level argparse parser BEFORE subparsers are added, so the three flags appear in `mpm --help` and are propagated to every subcommand's `args` namespace via argparse.
- [ ] AC-FUNC-010 `src/mpm_cli/cli.py` calls `_apply_global_flags(args)` early in `main()` after `parser.parse_args(argv)` and BEFORE dispatching to subcommand handlers; the order is asserted by a test that introspects the call sequence.
- [ ] AC-FUNC-011 `src/mpm_cli/constants.py` defines `NO_COLOR_ENV = "NO_COLOR"` and `_NO_COLOR_ACTIVE: bool = False`; both are importable at module load time.
- [ ] AC-FUNC-012 Every existing subcommand parser receives `args.quiet`, `args.verbose`, and `args.no_color` in its parsed namespace without any per-subcommand wiring (argparse inheritance via the root parser).
- [ ] AC-TEST-001 All parametrised cases listed in the Approach are added to `tests/unit/test_cli_args.py` BEFORE the implementation lands (TDD RED then GREEN). Cases cover: mutex error path, single-flag-set paths, no-flags default, logger-level mapping, color precedence (flag wins, env wins when flag absent, TTY auto-detect when both absent), idempotency, defence-in-depth `ValueError`.
- [ ] AC-TEST-002 `tests/unit/test_cli.py` (new) parametrises over every registered subcommand and asserts each one's parsed namespace contains the `quiet`, `verbose`, and `no_color` attributes; subcommand list is iterated from the subparsers' choices map (not hard-coded).
- [ ] AC-TEST-003 `tests/unit/test_constants.py` (new) asserts that `NO_COLOR_ENV` equals the string `"NO_COLOR"` and that `_NO_COLOR_ACTIVE` exists at import time with default value `False`.
- [ ] AC-CYCLE-001 End-to-end cycle via subprocess: (a) `mpm --help` lists the three flags in the Global options block; (b) `mpm list --quiet --verbose` exits non-zero with an argparse mutex error on stderr; (c) `NO_COLOR=1 mpm --help` produces output containing zero ANSI escape sequences. All three subprocess invocations and their stdout/stderr/exit-code are recorded in `## TDD Cycle Log`.
- [ ] AC-DOC-001 `docs/configuration.md` (ref) is updated in the same commit with a "Global options" subsection enumerating `--quiet`, `--verbose`, `--no-color`, the mutual-exclusion rule between `--quiet` and `--verbose`, the precedence between `--no-color` and the `NO_COLOR` env var, and the TTY auto-detect fallback.
- [ ] AC-DOC-002 The module docstring of `src/mpm_cli/core/cli_args.py` is updated to name the new `add_global_flags` factory, its mutex behaviour, and the spec Section 7 reference.
- [ ] AC-LINT-001 `uv run ruff check src tests` exits zero.
- [ ] AC-LINT-002 `uv run ruff format --check src tests` exits zero.
- [ ] AC-SEC-001 No provider HTTP API calls, provider CLI shell-outs, or credential handling introduced. The `NO_COLOR` env var is read via `os.environ.get` only.
- [ ] AC-SEC-002 No security bypass annotations introduced (`nosec`, `noqa`, `type: ignore`, etc).
- [ ] AC-FINAL-001 Every AC-TEST-* and AC-CYCLE-* test defined above runs and PASSES (not merely exists). No skipped, no xfail, no xpassed, no stubs.
- [ ] AC-FINAL-002 `ruff check src tests` exits zero in the relevant service directory.
- [ ] AC-FINAL-003 `ruff format --check src tests` exits zero.
- [ ] AC-FINAL-004 `mypy src` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-005 `pytest tests/unit -v` exits zero (full unit suite green).
- [ ] AC-FINAL-006 `pytest tests/integration -v` exits zero (full integration suite green).
- [ ] AC-FINAL-007 `pytest tests/functional -v` exits zero (full functional suite green).
- [ ] AC-FINAL-008 `bandit -r src -ll` exits zero (vendored `src/mpm_cli/repo/ (ref)` (ref) carved out per CLAUDE.md).
- [ ] AC-FINAL-009 `JUDGE_CLAUDE_MODEL=$JUDGE_CLAUDE_MODEL JUDGE_WORKSPACE_ROOT=/workspaces/rpm-migration/mpm-deps-work uv run --project /workspaces/rpm-migration/workbench workbench validate-backlog` exits zero.
- [ ] AC-FINAL-010 The code under test is functionally verified end-to-end (the AC-CYCLE-* evidence above).
- [ ] AC-FINAL-011 No bypass annotations: no `# noqa`, `# nosec`, `# type: ignore`, `@SuppressWarnings`, `# pragma: no cover`, `--no-verify`, no raised lint thresholds, no added exclusions to linter configs.
- [ ] AC-FINAL-012 No em-dash characters (U+2014) introduced anywhere.
- [ ] AC-FINAL-013 No new test skips, xfails, or `pytest.mark.skip*` annotations.
- [ ] AC-FINAL-014 Coverage: `pytest --cov=src --cov-fail-under=100` exits zero for new files; baseline not regressed for modified files.
- [ ] AC-FINAL-015 The Task's Changes Manifest matches exactly the files changed by git (no extra, no missing). Paths are repo-relative (NOT prefixed with `mpm/` or any other checkout_directory).

## Changes Manifest

| File | Change |
|------|--------|
| `src/mpm_cli/core/cli_args.py` | Update -- add `add_global_flags(parser)` factory (mutex group for `--quiet` / `--verbose`, standalone `--no-color`) and `_apply_global_flags(args)` runtime helper (logging level + color-state plumbing); keep existing `add_catalog_source_arg` factory untouched. |
| `src/mpm_cli/cli.py` | Update -- call `add_global_flags(root_parser)` on the top-level argparse parser before subparsers are added, and call `_apply_global_flags(args)` early in `main()` after argv parsing and before subcommand dispatch. |
| `src/mpm_cli/constants.py` | Update -- add `NO_COLOR_ENV = "NO_COLOR"` and module-level `_NO_COLOR_ACTIVE: bool = False` (mutated by `_apply_global_flags`). |
| `tests/unit/test_cli_args.py` | Update -- extend with parametrised cases for `add_global_flags` (mutex error, single-flag paths, no-flags default) and `_apply_global_flags` (logger-level mapping, color precedence flag/env/TTY, idempotency, defence-in-depth `ValueError`). |
| `tests/unit/test_cli.py` | New -- TDD-paired unit test for `src/mpm_cli/cli.py` per source-test-atomicity; parametrises over every registered subcommand and asserts `quiet` / `verbose` / `no_color` are present in each parsed namespace; asserts `_apply_global_flags` is invoked before subcommand dispatch. |
| `tests/unit/test_constants.py` | New -- TDD-paired unit test for `src/mpm_cli/constants.py` per source-test-atomicity; asserts the new `NO_COLOR_ENV` and `_NO_COLOR_ACTIVE` symbols exist with the right values at import time. |
| `docs/configuration.md` | Update -- add a "Global options" subsection documenting `--quiet`, `--verbose`, `--no-color`, the mutual-exclusion rule, the `NO_COLOR` env var precedence, and the TTY auto-detect fallback. |

## Definition of Done

- [ ] All acceptance criteria checked.
- [ ] `uv run pytest tests/ -v` passes (unit + integration + functional).
- [ ] `uv run ruff check src tests` and `uv run ruff format --check src tests` pass.
- [ ] `uv run mypy src` passes.
- [ ] `uv run bandit -r src -ll` passes.
- [ ] 100% coverage on the lines added/changed in `src/mpm_cli/core/cli_args.py`, `src/mpm_cli/cli.py` (ref), and `src/mpm_cli/constants.py` (ref).
- [ ] Only files in Changes Manifest are staged with `git add`.
- [ ] `docs/configuration.md` (ref) updated in the same commit.
- [ ] TDD Cycle Log captures RED-GREEN-REFACTOR phases with exit codes and the three AC-CYCLE-001 subprocess transcripts.

## TDD Cycle Log

## Comments
