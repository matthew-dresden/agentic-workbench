# Workbench test fixture config -- used by tests/conftest.py
repos:
  example-org/git-repo:
    default_branch: main
    merge_strategy: squash
  matthew-dresden/agentic-workbench:
    default_branch: main2

merge_strategy: squash
max_executor_retries: 3
use_bedrock: false
bedrock_region: us-east-1

timeouts:
  gh_api: 10
  test: 60
  security_fetch: 30
  llm: 60
  command: 30
  orchestrator_poll_interval: 5
  github_check: 60

limits:
  alert_summary: 5
  output_truncation: 500
  llm_evidence_truncation: 3000
  llm_file_context: 3
  llm_file_preview_chars: 500
